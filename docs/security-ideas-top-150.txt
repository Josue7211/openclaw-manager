Top 150 Security Hardening Ideas for Mission Control

001. Treat OpenClaw as untrusted by default and enforce all sensitive actions through a broker.
002. Separate trust zones for UI, policy broker, secret broker, and external connectors.
003. Run secret broker under a different OS user than OpenClaw runtime.
004. Remove direct `bw` access from all agent processes.
005. Use a zero-secret API contract: never return plaintext credentials to agents.
006. Return only approved or denied, masked references, and audit IDs.
007. Require per-action approval for secret usage, not just session-level approval.
008. Bind approvals to exact target domain, action type, and amount.
009. Expire approvals quickly (30 to 60 seconds).
010. Enforce one-time, non-replayable approval tokens.
011. Add nonce validation for every approval challenge.
012. Reject approval reuse across actions or endpoints.
013. Add step-up auth (Face ID or Touch ID) for high-risk actions.
014. Deny by default on approval timeout.
015. Require explicit human approval for first-time merchants.
016. Require explicit human approval for first-time recipients.
017. Require explicit human approval for external-domain forwarding.
018. Require explicit human approval for bulk sends.
019. Require explicit human approval for attachment sends above threshold.
020. Require explicit human approval for reading private message content.
021. Add read modes for messages: off, metadata-only, full.
022. Add read modes for email: off, headers-only, full.
023. Set metadata-only mode as default for new installs.
024. Redact message bodies from logs and traces.
025. Redact email bodies from logs and traces.
026. Redact subject lines in high-privacy mode.
027. Block screenshot capture during secret-use windows where possible.
028. Disable clipboard access for secret operations by default.
029. If clipboard fallback is used, auto-clear in under 10 seconds.
030. Disable clipboard history integration for secret copy flows.
031. Keep Bitwarden unlocked only just in time.
032. Auto-lock Bitwarden immediately after action completion.
033. Never persist decrypted secrets to disk.
034. Never store raw secrets in process environment variables.
035. Zeroize secret buffers in memory after use.
036. Limit process core dumps and crash dump secret exposure.
037. Use append-only, hash-chained audit logs for sensitive decisions.
038. Sign audit records to detect tampering.
039. Include request context hash in each audit entry.
040. Record policy version used for each allow or deny decision.
041. Store denied attempts with reason codes for threat hunting.
042. Add a one-tap global kill switch to freeze all risky operations.
043. Add module-level kill switches for messages, email, and payments.
044. Require re-auth after kill-switch disable.
045. Split API keys by capability, never use one key for everything.
046. Use separate AgentMail keys for send-only and read-only roles.
047. Use inbox-scoped AgentMail keys instead of org-wide keys.
048. Enable AgentMail permission whitelist mode for every key.
049. Turn off AgentMail spam, blocked, and trash visibility for agent keys.
050. Remove API key creation permissions from runtime agent keys.
051. Remove webhook management permissions from runtime agent keys.
052. Rotate AgentMail keys on a fixed schedule.
053. Rotate OpenClaw gateway tokens on a fixed schedule.
054. Rotate BlueBubbles credentials if compromise is suspected.
055. Add automated key-age alerts.
056. Add dormant key detection and automatic disable.
057. Enforce egress allowlists per module and per process.
058. Deny all outbound traffic by default.
059. Allow outbound only to known hosts and ports.
060. Add HTTP method allowlists per destination.
061. Add path-level restrictions for critical APIs.
062. Require TLS for all remote connector traffic.
063. Pin certs or SPKI for the most sensitive services where feasible.
064. Re-validate destination host at execution time, not only request time.
065. Prevent DNS rebinding via resolved IP checks.
066. Block RFC1918 and loopback egress except explicit local services.
067. Add outbound request budget limits per module.
068. Add per-module circuit breakers for repeated failures.
069. Run OpenClaw in a rootless container or sandbox.
070. Use read-only root filesystem for OpenClaw containers.
071. Drop Linux capabilities (cap_drop all).
072. Set no-new-privileges for sandboxed runtimes.
073. Restrict writable mounts to dedicated temp directories.
074. Mount keychain sockets and secret stores as unavailable in agent containers.
075. Run connectors in separate containers with isolated networks.
076. Isolate BlueBubbles connector from email connector runtime.
077. Isolate payment tooling from messaging tooling.
078. Use per-connector service accounts on the host.
079. Apply seccomp profiles to reduce syscall surface.
080. Apply AppArmor or SELinux policies to connector processes.
081. Restrict process spawning capabilities for agent runtimes.
082. Disable shell and exec tools unless explicitly required.
083. Deny dangerous OpenClaw tools over HTTP by policy.
084. Keep OpenClaw HTTP endpoints private to loopback or tailnet only.
085. Never expose OpenClaw shared-secret endpoints to public internet.
086. Prefer trusted-proxy identity mode for scope-aware auth boundaries.
087. Treat token/password OpenClaw auth as full operator power.
088. Place a reverse proxy in front with additional ACL and rate controls.
089. Add mTLS between broker and internal services where practical.
090. Enforce strict request schemas at broker ingress.
091. Reject unknown fields in high-risk API payloads.
092. Add idempotency keys for send and payment actions.
093. Add replay protection for webhook events.
094. Verify AgentMail webhook signatures before processing.
095. Store webhook delivery IDs to prevent duplicate handling.
096. Separate ingress queues for trusted and untrusted event sources.
097. Add human review queue for ambiguous high-risk events.
098. Add recipient allowlists for automated outbound email.
099. Add domain allowlists for automated outbound email.
100. Add content policy checks before send (PII, secrets, legal terms).
101. Add attachment malware scanning pipeline.
102. Block executable attachment types in outbound automation.
103. Add outbound DLP checks for regulated data classes.
104. Require manual approval when DLP confidence exceeds threshold.
105. Add per-contact trust levels for message and email actions.
106. Require approval when contact trust is unknown.
107. Add contextual risk scoring for every planned action.
108. Base risk score on destination novelty, amount, content sensitivity, and history.
109. Auto-deny requests above hard risk threshold.
110. Auto-approve low-risk repetitive actions under strict policy.
111. Keep policy files versioned and code-reviewed.
112. Sign policy bundles before deployment.
113. Add policy simulation mode before enforcement changes.
114. Add policy dry-run logs to reduce breakage risk.
115. Add a policy diff UI for operator review.
116. Add break-glass roles with explicit time-limited grants.
117. Require dual approval for break-glass actions.
118. Auto-expire break-glass tokens quickly.
119. Track all break-glass actions in dedicated audit stream.
120. Add immutable retention for security logs.
121. Forward logs to a separate tamper-resistant store.
122. Detect anomalous access patterns with alerting.
123. Detect unusual read-volume spikes on messages and email.
124. Detect unusual send-volume spikes on messages and email.
125. Detect impossible-travel or odd-time high-risk activity.
126. Alert on repeated deny events from same actor.
127. Alert on repeated policy bypass attempts.
128. Add honeypot secrets to detect exfiltration attempts.
129. Add fake canary inboxes for abuse detection.
130. Add canary payment endpoints to detect rogue purchase attempts.
131. Restrict frontend secret read APIs to strict allowlists only.
132. Keep service-role and broker credentials backend-only forever.
133. Add CSRF protections even on local surfaces when browsers are involved.
134. Validate Origin and Referer on sensitive POST routes.
135. Add strict CORS with explicit origins only.
136. Add cache-control no-store headers on all sensitive responses.
137. Encrypt local caches holding message or email metadata.
138. Shorten local cache TTL for sensitive datasets.
139. Add per-user encrypted local storage keys.
140. Rotate local encryption material at session boundaries.
141. Encrypt backups and require separate restore credentials.
142. Add restore-time approval for sensitive backup contents.
143. Add periodic threat-model reviews tied to new module additions.
144. Require security checklist completion before enabling new connectors.
145. Add dependency allowlist and lockfile integrity enforcement.
146. Run SAST and secret scanning in CI and pre-commit.
147. Run regular security chaos drills for broker denial and key compromise.
148. Create incident runbooks for key leak, agent abuse, and connector compromise.
149. Add post-incident policy hardening and regression tests for each incident.
150. Build a privacy posture dashboard with current mode, active grants, and high-risk exposures.
