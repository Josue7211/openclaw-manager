---
phase: 02.2-theme-system-mode-fixes
plan: 02
subsystem: ui
tags: [wallbash, theme-engine, system-mode, event-listener, tauri-events, css-variables, platform-detection]

requires:
  - phase: 02.2-theme-system-mode-fixes
    provides: read_wallbash_colors Tauri command, read_theme_conf Tauri command, file watcher events, COUNTERPART_MAP, WallbashColors type
provides:
  - buildWallbashTheme() mapping wallbash colors to ThemeDefinition
  - setWallbashColors/setWallbashColorScheme module-level state
  - getActiveSystemTheme() UI utility for system mode rendering
  - isOsDark() export for platform-aware theme filtering
  - Event-driven wallbash sync via Tauri event listeners
  - System mode single-card UI (Linux) and filtered-preset UI (macOS/Windows)
affects: [theme-picker, settings-display, theme-engine]

tech-stack:
  added: []
  patterns: [wallbash-css-mapping, event-driven-theme-sync, platform-conditional-ui]

key-files:
  created:
    - frontend/src/lib/__tests__/theme-wallbash.test.ts
    - frontend/src/lib/__tests__/theme-system-mode.test.ts
  modified:
    - frontend/src/lib/theme-engine.ts
    - frontend/src/main.tsx
    - frontend/src/components/ThemePicker.tsx
    - frontend/src/pages/settings/SettingsDisplay.tsx
    - frontend/src/components/ui/__tests__/ThemePicker.test.tsx

key-decisions:
  - "buildWallbashTheme uses rgba compositing for bg-panel/bg-card/bg-elevated with wallbash base colors as RGB source"
  - "Linux system mode shows single active theme card; macOS/Windows shows filtered presets by OS dark/light"
  - "gsettings polling reduced from 1s to 3s since file watcher handles wallbash instant sync"
  - "getActiveSystemTheme resolves the display name from the ThemeDefinition — 'Wallbash' for wallbash-live, theme.name for presets"

patterns-established:
  - "Wallbash CSS mapping: hexToRgbStr helper converts hex to 'R, G, B' for rgba() compositing"
  - "Platform-conditional UI: getActiveSystemTheme returns null activeTheme on non-Linux to trigger filtered grid"
  - "Event-driven theme sync: listen('wallbash-colors-changed') replaces polling for wallbash file changes"

requirements-completed: [SYSMODE-01, SYSMODE-04, SYSMODE-07]

duration: 6min
completed: 2026-03-20
---

# Phase 02.2 Plan 02: Frontend Wallbash Integration + System Mode UI Summary

**buildWallbashTheme() CSS variable mapping, Tauri event-driven sync in main.tsx, and platform-conditional system mode UI (single card on Linux, filtered presets on macOS/Windows)**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-20T02:53:22Z
- **Completed:** 2026-03-20T02:59:57Z
- **Tasks:** 2 (of 3 -- Task 3 is human-verify checkpoint)
- **Files modified:** 7

## Accomplishments
- buildWallbashTheme() maps 15+ wallbash color variables to a full ThemeDefinition with rgba compositing
- Event-driven wallbash sync via listen('wallbash-colors-changed') and listen('gtk-theme-changed') for ~200ms response
- System mode on Linux shows a single "System Theme: [name]" card instead of 24 presets
- System mode on macOS/Windows filters presets by OS dark/light preference
- 32 new tests (20 wallbash + 12 system-mode) all passing

## Task Commits

Each task was committed atomically:

1. **Task 1: Wallbash theme builder + event-driven main.tsx** - `0c0cfdb` (feat)
2. **Task 2: System mode UI -- single active theme card + platform fallback** - `81e013a` (feat)
3. **Task 3: Verify system mode integration end-to-end** - checkpoint (human-verify)

## Files Created/Modified
- `frontend/src/lib/theme-engine.ts` - buildWallbashTheme(), wallbash state, getActiveSystemTheme(), isOsDark()
- `frontend/src/main.tsx` - Tauri event listeners, initial wallbash fetch, 3s polling fallback
- `frontend/src/components/ThemePicker.tsx` - System mode single-card rendering on Linux
- `frontend/src/pages/settings/SettingsDisplay.tsx` - System mode card with "Controlled by desktop theme"
- `frontend/src/lib/__tests__/theme-wallbash.test.ts` - 20 tests for wallbash color mapping
- `frontend/src/lib/__tests__/theme-system-mode.test.ts` - 12 tests for system mode UI behavior
- `frontend/src/components/ui/__tests__/ThemePicker.test.tsx` - Updated mock for new theme-engine exports

## Decisions Made
- buildWallbashTheme uses rgba compositing for bg-panel/bg-card/bg-elevated with wallbash base colors as RGB source
- Linux system mode shows single active theme card; macOS/Windows shows filtered presets by OS dark/light preference
- gsettings polling reduced from 1s to 3s since file watcher handles wallbash instant sync
- getActiveSystemTheme resolves display name from ThemeDefinition -- "Wallbash" for wallbash-live, theme.name for presets

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed ThemePicker test mock missing new exports**
- **Found during:** Task 2 (system mode UI)
- **Issue:** Existing ThemePicker test mock for `@/lib/theme-engine` didn't include `getActiveSystemTheme` or `isOsDark`, causing 7 test failures
- **Fix:** Added `getActiveSystemTheme` and `isOsDark` to the vi.mock return in ThemePicker.test.tsx
- **Files modified:** frontend/src/components/ui/__tests__/ThemePicker.test.tsx
- **Verification:** All 1397 frontend tests pass
- **Committed in:** 81e013a (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary fix to maintain test suite green. No scope creep.

## Issues Encountered
None beyond the test mock update above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Wallbash integration is complete pending human verification (Task 3 checkpoint)
- All light themes continue to pass WCAG on composited surfaces
- System mode single-card UI ready for visual verification on Linux with HyDE

## Self-Check: PASSED

All created files exist. All commit hashes verified.

---
*Phase: 02.2-theme-system-mode-fixes*
*Completed: 2026-03-20*
