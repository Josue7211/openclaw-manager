---
phase: 07-bjorn-module-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/bjorn-types.ts
  - frontend/src/lib/bjorn-static-analysis.ts
  - frontend/src/lib/bjorn-sandbox.ts
  - frontend/src/lib/__tests__/bjorn-static-analysis.test.ts
  - frontend/src/lib/__tests__/bjorn-sandbox.test.ts
autonomous: true
requirements:
  - BJORN-08
  - BJORN-09

must_haves:
  truths:
    - "Static analysis gate rejects code containing dangerous APIs (network calls, DOM escape, storage access, Tauri IPC)"
    - "Static analysis returns line numbers and snippets for each violation"
    - "Safe code passes static analysis without false positives"
    - "Sandbox HTML template includes CSP meta tag blocking all external resources"
    - "Sandbox HTML template injects current theme CSS variables for visual fidelity"
    - "BjornModule and BjornModuleVersion types define the complete data model"
  artifacts:
    - path: "frontend/src/lib/bjorn-types.ts"
      provides: "BjornModule, BjornModuleVersion, AnalysisResult, BridgeRequest, BridgeResponse types"
      exports: ["BjornModule", "BjornModuleVersion", "AnalysisResult", "BridgeRequest", "BridgeResponse"]
    - path: "frontend/src/lib/bjorn-static-analysis.ts"
      provides: "analyzeCode function with regex blocklist"
      exports: ["analyzeCode", "BLOCKLIST"]
    - path: "frontend/src/lib/bjorn-sandbox.ts"
      provides: "buildSandboxHTML and getThemeVarsCSS functions"
      exports: ["buildSandboxHTML", "getThemeVarsCSS"]
    - path: "frontend/src/lib/__tests__/bjorn-static-analysis.test.ts"
      provides: "Unit tests for static analysis gate"
    - path: "frontend/src/lib/__tests__/bjorn-sandbox.test.ts"
      provides: "Unit tests for sandbox HTML builder"
  key_links:
    - from: "frontend/src/lib/bjorn-static-analysis.ts"
      to: "frontend/src/lib/bjorn-types.ts"
      via: "imports AnalysisResult type"
      pattern: "import.*AnalysisResult.*bjorn-types"
    - from: "frontend/src/lib/bjorn-sandbox.ts"
      to: "frontend/src/lib/bjorn-types.ts"
      via: "imports types for component source handling"
      pattern: "import.*bjorn-types"
---

<objective>
Create the security foundation and shared types for the Bjorn Module Builder: the static analysis gate that validates generated code against a blocklist of dangerous APIs, the sandbox HTML template builder that produces CSP-hardened srcdoc content for preview iframes, and the shared TypeScript types used across all Bjorn plans.

Purpose: This is the security-first foundation. No generated code can render until it passes static analysis. No preview can display without the sandbox template. All downstream plans depend on these types and security primitives.
Output: 3 library files (types, static analysis, sandbox builder) + 2 test files with full coverage.
</objective>

<execution_context>
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bjorn-module-builder/07-RESEARCH.md

<interfaces>
From frontend/src/lib/widget-registry.ts:
```typescript
export interface WidgetProps {
  widgetId: string
  config: Record<string, unknown>
  isEditMode: boolean
  size: { w: number; h: number }
}

export interface WidgetConfigSchema {
  fields: Array<{
    key: string
    label: string
    type: 'text' | 'number' | 'toggle' | 'select' | 'slider'
    default: unknown
    options?: Array<{ label: string; value: unknown }>
    min?: number
    max?: number
  }>
}

export interface WidgetDefinition {
  id: string
  name: string
  description: string
  icon: string
  category: 'monitoring' | 'productivity' | 'ai' | 'media' | 'custom' | 'primitives'
  tier: 'builtin' | 'user' | 'ai'
  defaultSize: { w: number; h: number }
  minSize?: { w: number; h: number }
  maxSize?: { w: number; h: number }
  configSchema?: WidgetConfigSchema
  component: () => Promise<{ default: ComponentType<WidgetProps> }>
  metadata?: { author?: string; version?: string; requiresService?: string }
}

export function registerWidget(def: WidgetDefinition): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bjorn types + static analysis gate with tests</name>
  <files>
    frontend/src/lib/bjorn-types.ts,
    frontend/src/lib/bjorn-static-analysis.ts,
    frontend/src/lib/__tests__/bjorn-static-analysis.test.ts
  </files>
  <read_first>
    frontend/src/lib/widget-registry.ts,
    frontend/src/pages/chat/types.ts,
    .planning/phases/07-bjorn-module-builder/07-RESEARCH.md
  </read_first>
  <behavior>
    - analyzeCode('const x = 1') returns { safe: true, violations: [] }
    - analyzeCode with network call patterns returns safe: false (fetch call, XMLHttpRequest, WebSocket)
    - analyzeCode with code execution patterns returns safe: false (patterns that execute arbitrary strings)
    - analyzeCode with DOM escape patterns returns safe: false (document.cookie, window.parent, window.top)
    - analyzeCode with storage access returns safe: false (localStorage, sessionStorage)
    - analyzeCode with importScripts returns safe: false
    - analyzeCode with browser API misuse returns safe: false (navigator.sendBeacon, window.open, document.domain)
    - analyzeCode with Tauri IPC access returns safe: false (__tauri, window.__TAURI)
    - Multiple violations on different lines returns all with correct line numbers
    - 'window.requestData(...)' returns safe: true (allowed bridge API)
  </behavior>
  <action>
    1. Create `frontend/src/lib/bjorn-types.ts` with the following exported types:
       - `BjornModule` interface: id (string), userId (string), name (string), description (string), icon (string, default 'Cube'), source (string -- current version source), configSchema (WidgetConfigSchema imported from widget-registry), defaultSize ({ w: number, h: number }), version (number), enabled (boolean), createdAt (string ISO), updatedAt (string ISO), deletedAt (string | null)
       - `BjornModuleVersion` interface: id (string), moduleId (string), version (number), source (string), configSchema (WidgetConfigSchema), createdAt (string ISO)
       - `AnalysisResult` interface: safe (boolean), violations (Array of { pattern: string, line: number, snippet: string })
       - `BridgeRequest` interface: type ('data-request'), requestId (string), source (string), command (string), args (Record<string, unknown> optional)
       - `BridgeResponse` interface: type ('data-response' | 'data-error'), requestId (string), data (unknown optional), error (string optional)
       - `BjornGenerationState` type: 'idle' | 'generating' | 'analyzing' | 'previewing' | 'approved' | 'rejected'

    2. Create `frontend/src/lib/bjorn-static-analysis.ts`:
       - Export `BLOCKLIST` as `ReadonlyArray<RegExp>` containing 17 regex patterns that detect dangerous APIs. See the 07-RESEARCH.md "Pattern 1: Static Analysis Gate" section for the exact list of patterns. This is a SECURITY GATE -- it performs regex matching on source text strings to BLOCK dangerous patterns from being rendered. It does NOT execute any code.
       - Export `analyzeCode(source: string): AnalysisResult` that splits source on '\n', iterates each line against every regex in BLOCKLIST, collects violations with line number (1-indexed), pattern source string, and snippet (line content trimmed to 80 chars max)
       - Return `{ safe: violations.length === 0, violations }`

    3. Create test file `frontend/src/lib/__tests__/bjorn-static-analysis.test.ts`:
       - Test each of the 17 blocklist patterns individually (one it() per dangerous API category)
       - Test safe code passes without violations
       - Test multi-line code reports correct line numbers
       - Test window.requestData is NOT blocked (it is the allowed data bridge)
       - Use `describe('analyzeCode', ...)` grouping

    Follow project conventions: no semicolons, single quotes, 2-space indent, import types with `type` keyword.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run src/lib/__tests__/bjorn-static-analysis.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/lib/bjorn-types.ts` exists and exports at least 6 types/interfaces
    - File `frontend/src/lib/bjorn-static-analysis.ts` exports analyzeCode and BLOCKLIST
    - BLOCKLIST contains at least 17 regex patterns
    - All tests in bjorn-static-analysis.test.ts pass
    - analyzeCode on safe code returns safe: true
    - analyzeCode on code with dangerous API calls returns safe: false with correct line numbers
  </acceptance_criteria>
  <done>All 17+ blocklist patterns tested, safe code passes, multi-line violation detection works, types exported for downstream plans</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sandbox HTML builder with theme injection and tests</name>
  <files>
    frontend/src/lib/bjorn-sandbox.ts,
    frontend/src/lib/__tests__/bjorn-sandbox.test.ts
  </files>
  <read_first>
    frontend/src/lib/bjorn-types.ts,
    frontend/src/lib/bjorn-static-analysis.ts,
    frontend/src/globals.css,
    .planning/phases/07-bjorn-module-builder/07-RESEARCH.md
  </read_first>
  <behavior>
    - buildSandboxHTML('const x = 1', ':root { --accent: red }') returns string containing 'DOCTYPE html'
    - buildSandboxHTML output contains CSP meta tag with "default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'"
    - buildSandboxHTML output contains the theme vars CSS string in a style tag
    - buildSandboxHTML output contains the component source in a script tag
    - buildSandboxHTML output contains div#root
    - buildSandboxHTML output contains the requestData bridge helper
    - buildSandboxHTML output does NOT contain 'allow-same-origin'
    - getThemeVarsCSS() returns a string containing ':root {' when CSS variables exist on documentElement
    - requestData bridge code includes 10-second timeout
  </behavior>
  <action>
    1. Create `frontend/src/lib/bjorn-sandbox.ts`:

       Export `getThemeVarsCSS(): string` that reads all CSS custom properties from `document.documentElement` computed style. Guard with `typeof document !== 'undefined'` for test/SSR safety -- return empty string if unavailable. Iterate properties, filter those starting with '--', build a `:root { ... }` CSS string.

       Export `buildSandboxHTML(componentSource: string, themeVarsCSS: string): string` that returns an HTML string with:
       - DOCTYPE html declaration
       - `<meta charset="utf-8">`
       - CSP meta tag: `<meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'">`
       - `<style>` block with: CSS reset (*, margin:0, padding:0, box-sizing:border-box), body font-family (system-ui, -apple-system, sans-serif), and the injected themeVarsCSS parameter
       - `<div id="root"></div>`
       - `<script>` block containing:
         a. The `window.requestData` bridge helper: returns a Promise, generates unique requestId, listens for postMessage response, sends data-request to parent via `window.parent.postMessage(msg, '*')`, 10-second timeout.
         b. A minimal `h(tag, props, ...children)` DOM builder function for preview rendering.
         c. The componentSource parameter injected directly.
         d. A try/catch wrapper calling BjornWidget with preview props, rendering to #root, showing errors as red text.

    2. Create `frontend/src/lib/__tests__/bjorn-sandbox.test.ts`:
       - Test buildSandboxHTML returns valid HTML structure
       - Test CSP meta tag presence and exact content string
       - Test theme vars injection into style block
       - Test requestData bridge code presence (search for 'data-request' in output)
       - Test 10000 timeout value present in output
       - Test getThemeVarsCSS behavior in JSDOM
       - Test output does not contain 'allow-same-origin' string

    Follow project conventions: no semicolons, single quotes.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run src/lib/__tests__/bjorn-sandbox.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/lib/bjorn-sandbox.ts` exists and exports buildSandboxHTML and getThemeVarsCSS
    - Output of buildSandboxHTML contains CSP meta tag with "default-src 'none'"
    - Output contains 'data-request' bridge protocol string
    - Output contains requestData helper function
    - Output contains 10000 timeout value
    - Output does NOT contain string 'allow-same-origin'
    - All tests pass
  </acceptance_criteria>
  <done>Sandbox HTML builder produces CSP-hardened srcdoc with theme injection, requestData bridge, and minimal render helper. Tests verify security properties.</done>
</task>

</tasks>

<verification>
```bash
cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run src/lib/__tests__/bjorn-static-analysis.test.ts src/lib/__tests__/bjorn-sandbox.test.ts -x
```
</verification>

<success_criteria>
- 3 library files exist: bjorn-types.ts, bjorn-static-analysis.ts, bjorn-sandbox.ts
- Static analysis catches all 17 blocklist patterns
- Sandbox HTML includes CSP, theme injection, and data bridge
- All tests pass
- No new dependencies added (pure TypeScript)
</success_criteria>

<output>
After completion, create `.planning/phases/07-bjorn-module-builder/07-01-SUMMARY.md`
</output>
