---
phase: 06-module-primitives-library
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - frontend/src/components/primitives/ListView.tsx
  - frontend/src/components/primitives/DataTable.tsx
  - frontend/src/components/primitives/register.ts
  - frontend/src/components/primitives/__tests__/ListView.test.tsx
  - frontend/src/components/primitives/__tests__/DataTable.test.tsx
autonomous: true
requirements: [PRIM-04, PRIM-05]

must_haves:
  truths:
    - "ListView renders a sortable, filterable, paginated list from config.items array"
    - "DataTable renders sortable columns with sticky header, row actions, and pagination"
    - "Both handle empty data with EmptyState"
    - "Both are pure render components -- data comes from config, not internal fetching"
    - "Pagination controls show page numbers and prev/next navigation"
  artifacts:
    - path: "frontend/src/components/primitives/ListView.tsx"
      provides: "Sortable filterable paginated list"
      exports: ["default", "configSchema"]
    - path: "frontend/src/components/primitives/DataTable.tsx"
      provides: "Sortable table with row actions and pagination"
      exports: ["default", "configSchema"]
  key_links:
    - from: "frontend/src/components/primitives/ListView.tsx"
      to: "frontend/src/components/primitives/shared.tsx"
      via: "config helpers for defensive parsing"
      pattern: "configString|configNumber|configArray|configBool"
    - from: "frontend/src/components/primitives/DataTable.tsx"
      to: "frontend/src/components/primitives/shared.tsx"
      via: "config helpers"
      pattern: "configString|configNumber|configArray"
---

<objective>
Build the data display primitives: ListView and DataTable. These handle arrays of structured data with sort, filter, and pagination -- common patterns for dashboard data presentation.

Purpose: Data lists and tables are the workhorses of any dashboard. They make arbitrary data from Bjorn or the user displayable in the widget grid with built-in interaction (sort, filter, page).

Output: 2 data primitives registered in Widget Registry with full test coverage.
</objective>

<execution_context>
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-module-primitives-library/06-CONTEXT.md
@.planning/phases/06-module-primitives-library/06-RESEARCH.md
@.planning/phases/06-module-primitives-library/06-01-SUMMARY.md

@frontend/src/components/primitives/shared.tsx
@frontend/src/components/primitives/register.ts
@frontend/src/lib/widget-registry.ts
@frontend/src/components/ui/EmptyState.tsx
@frontend/src/globals.css

<interfaces>
<!-- From shared.tsx: -->
```typescript
export function configString(config: Record<string, unknown>, key: string, fallback: string): string
export function configNumber(config: Record<string, unknown>, key: string, fallback: number): number
export function configBool(config: Record<string, unknown>, key: string, fallback: boolean): boolean
export function configArray<T>(config: Record<string, unknown>, key: string): T[]
export function resolveColor(key: string): string
```

<!-- Expected data shapes (opaque config, not in configSchema -- see Pitfall 6 in RESEARCH): -->
```typescript
// ListView items
type ListItem = { id: string; label: string; value?: string; icon?: string }

// DataTable structure
type TableColumn = { key: string; label: string; sortable?: boolean }
type TableRow = Record<string, unknown>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ListView primitive</name>
  <files>
    frontend/src/components/primitives/ListView.tsx,
    frontend/src/components/primitives/__tests__/ListView.test.tsx,
    frontend/src/components/primitives/register.ts
  </files>
  <behavior>
    - Renders items from config.items array (each: {id, label, value?, icon?})
    - Shows EmptyState with List icon when config.items is empty/missing
    - Filter: text input at top filters items by label (case-insensitive substring match) when config.searchable is true
    - Sort: clicking sort button toggles ascending/descending by label
    - Pagination: displays config.pageSize items per page (default 10), prev/next buttons, page indicator
    - Each item renders as a row with label (left) and optional value (right)
    - Items styled with hover-bg utility, border-bottom var(--border)
    - Exports configSchema with: title (text), pageSize (number, min 5, max 50, default 10), searchable (toggle, default true)
  </behavior>
  <action>
    **ListView.tsx:**
    - React.memo wrapped, implements WidgetProps
    - Co-exports configSchema with 3 fields: title (text), pageSize (number), searchable (toggle)
    - Internal state: useState for searchTerm, sortDirection ('asc'/'desc'), currentPage
    - Data flow: extract config.items via configArray, filter by search term, sort, then paginate
    - Filter input: only shown when config.searchable is true. Styled with var(--bg-base) background, var(--border) border, var(--text-primary) color. MagnifyingGlass icon from Phosphor.
    - Sort button: SortAscending/SortDescending icon from Phosphor, toggles sort direction on click
    - Item row: flex row, label left (var(--text-primary)), value right (var(--text-muted)), border-bottom 1px solid var(--border), padding 10px 12px, hover-bg class
    - Pagination bar at bottom: "Page X of Y", prev/next buttons (CaretLeft/CaretRight icons). Disable prev on first page, next on last page.
    - Reset currentPage to 0 when search term changes (useEffect)
    - Title rendered at top if config.title is provided

    **register.ts:**
    - Add registerWidget for prim-list-view: category 'primitives', defaultSize {w:3, h:4}, minSize {w:2, h:3}, icon 'List'

    **Tests:**
    - Renders item labels from config.items
    - Shows EmptyState when items is empty
    - Filter input filters items by label
    - Sort toggles between ascending/descending
    - Pagination shows correct page of items
    - Pagination buttons disable at boundaries
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run src/components/primitives/__tests__/ListView.test.tsx -x</automated>
  </verify>
  <done>
    - ListView renders items with filter, sort, and pagination
    - Search filters by label substring (case-insensitive)
    - Pagination controls work correctly
    - Empty items shows EmptyState
    - Registered in Widget Registry
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build DataTable primitive</name>
  <files>
    frontend/src/components/primitives/DataTable.tsx,
    frontend/src/components/primitives/__tests__/DataTable.test.tsx,
    frontend/src/components/primitives/register.ts
  </files>
  <behavior>
    - Renders HTML table from config.columns (TableColumn[]) and config.rows (Record<string, unknown>[])
    - Shows EmptyState with Table icon when rows is empty/missing
    - Sortable columns: clicking column header sorts rows by that column (asc/desc toggle)
    - Sort indicator: CaretUp/CaretDown icon in header cell for active sort column
    - Sticky header: thead stays fixed when table body scrolls
    - Pagination: config.pageSize items per page (default 10), page controls at bottom
    - Exports configSchema with: title (text), pageSize (number, min 5, max 100, default 10), striped (toggle, default true)
  </behavior>
  <action>
    **DataTable.tsx:**
    - React.memo wrapped, implements WidgetProps
    - Co-exports configSchema with 3 fields: title (text), pageSize (number), striped (toggle)
    - Internal state: useState for sortColumn (string|null), sortDirection ('asc'/'desc'), currentPage
    - Data flow: extract config.columns via configArray<TableColumn>, config.rows via configArray<TableRow>. Sort by sortColumn, then paginate.
    - Table structure: HTML table element (not div grid) for semantics and accessibility. style: width 100%, border-collapse collapse.
    - thead: sticky (position: sticky, top: 0, z-index: 1, background: var(--bg-card-solid)). th cells: padding 8px 12px, text-align left, font-size var(--text-xs), color var(--text-muted), text-transform uppercase, letter-spacing 0.05em, border-bottom 2px solid var(--border). Sortable columns get cursor: pointer and hover underline.
    - tbody: td cells: padding 8px 12px, font-size var(--text-sm), color var(--text-primary), border-bottom 1px solid var(--border). Striped rows: even rows get background var(--hover-bg) when config.striped is true.
    - Sort: Click th toggles sort. Compare values using localeCompare for strings, numeric comparison for numbers. Cycle: unsorted -> asc -> desc -> unsorted.
    - Pagination: same pattern as ListView -- page indicator, prev/next buttons at bottom.
    - Title at top if provided
    - Scrollable container: table wrapper div with overflow-x auto, overflow-y auto, flex 1

    **register.ts:**
    - Add registerWidget for prim-data-table: category 'primitives', defaultSize {w:4, h:4}, minSize {w:3, h:3}, icon 'Table'

    **Tests:**
    - Renders table with correct column headers from config.columns
    - Renders row data from config.rows
    - Shows EmptyState when rows is empty
    - Clicking sortable column header sorts rows
    - Sort direction toggles on repeated clicks
    - Pagination controls show correct page
    - Striped rows have alternating background when striped=true
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run src/components/primitives/__tests__/DataTable.test.tsx -x</automated>
  </verify>
  <done>
    - DataTable renders columns and rows from config
    - Column sort works with direction toggle
    - Sticky header, striped rows, pagination all functional
    - Empty rows shows EmptyState
    - Registered in Widget Registry
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/components/primitives/__tests__/ListView.test.tsx src/components/primitives/__tests__/DataTable.test.tsx -x` all pass
- `cd frontend && npx tsc --noEmit` compiles clean
- register.ts has entries for prim-list-view and prim-data-table
</verification>

<success_criteria>
1. ListView renders filterable, sortable, paginated list from config.items
2. DataTable renders sortable table with sticky header and pagination from config.columns/rows
3. Both handle empty data gracefully with EmptyState
4. Both are pure render components (no internal data fetching)
5. All styling via CSS variables
6. Both registered in Widget Registry under 'primitives' category
</success_criteria>

<output>
After completion, create `.planning/phases/06-module-primitives-library/06-04-SUMMARY.md`
</output>
