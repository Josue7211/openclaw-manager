---
phase: 02-theming-system
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - frontend/src/components/FontPicker.tsx
  - frontend/src/components/BrandingSettings.tsx
  - src-tauri/Cargo.toml
  - src-tauri/src/main.rs
  - src-tauri/src/routes/fonts.rs
  - frontend/src/lib/google-fonts.ts
autonomous: true
requirements: [THEME-04, THEME-05]

must_haves:
  truths:
    - "FontPicker renders 4 font slot dropdowns (Body, Heading, Monospace, UI) with live preview"
    - "FontPicker includes system fonts enumerated via Tauri command"
    - "FontPicker includes bundled fonts (Inter, JetBrains Mono, Fira Code) always available"
    - "Base font size slider ranges from 80% to 120% in 5% increments"
    - "BrandingSettings allows customizing app title, logo, sidebar header text, login tagline"
    - "Branding changes propagate to document.title"
  artifacts:
    - path: "frontend/src/components/FontPicker.tsx"
      provides: "4-slot font picker with system/bundled/Google sources, base size slider"
      min_lines: 80
    - path: "frontend/src/components/BrandingSettings.tsx"
      provides: "App title, logo upload, sidebar text, login tagline fields"
      min_lines: 60
    - path: "src-tauri/src/routes/fonts.rs"
      provides: "Tauri command listing system fonts via font-kit"
      exports: ["list_system_fonts"]
    - path: "frontend/src/lib/google-fonts.ts"
      provides: "Static list of top 100 Google Fonts + loadGoogleFont helper"
      exports: ["GOOGLE_FONTS", "loadGoogleFont"]
  key_links:
    - from: "frontend/src/components/FontPicker.tsx"
      to: "frontend/src/lib/theme-store.ts"
      via: "setFontOverride, setFontScale, setGlobalFontOverride"
      pattern: "import.*from.*theme-store"
    - from: "frontend/src/components/FontPicker.tsx"
      to: "frontend/src/lib/google-fonts.ts"
      via: "import GOOGLE_FONTS, loadGoogleFont"
      pattern: "import.*from.*google-fonts"
---

<objective>
Build the font customization system (4 slots with system/bundled/Google sources, base size slider, global override toggle) and custom branding settings (app title, logo, sidebar text, login tagline). Add system font enumeration Tauri command.

Purpose: Font customization and branding are key personalization features that make the app feel like the user's own.
Output: FontPicker, BrandingSettings components, font-kit Tauri command, Google Fonts static list.
</objective>

<execution_context>
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-theming-system/02-CONTEXT.md
@.planning/phases/02-theming-system/02-RESEARCH.md
@.planning/phases/02-theming-system/02-UI-SPEC.md
@.planning/phases/02-theming-system/02-01-SUMMARY.md
@.planning/phases/02-theming-system/02-02-SUMMARY.md

<interfaces>
From frontend/src/lib/theme-store.ts:
```typescript
export function setFontOverride(slot: string, fontFamily: string): void
export function setFontScale(scale: number): void
export function setGlobalFontOverride(enabled: boolean): void
export function useThemeState(): ThemeState
```

From frontend/src/lib/theme-engine.ts:
```typescript
export function applyFonts(fonts, globalOverride): void
export function applyFontScale(scale): void
```

From frontend/src/lib/sidebar-settings.ts:
```typescript
export function getSidebarTitleText(): string
export function setSidebarTitleText(v: string): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add font-kit Tauri command + Google Fonts static list</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/main.rs, src-tauri/src/routes/fonts.rs, frontend/src/lib/google-fonts.ts</files>
  <read_first>
    - src-tauri/Cargo.toml (current dependencies, feature flags)
    - src-tauri/src/main.rs (tauri::Builder setup, .invoke_handler() registration)
    - .planning/phases/02-theming-system/02-RESEARCH.md (System Font Enumeration Rust code, font-kit = "0.14", Pitfall #4 Google Fonts API key)
    - .planning/phases/02-theming-system/02-UI-SPEC.md (Font Picker spec, font sources)
  </read_first>
  <action>
1. Add `font-kit = "0.14"` to `[dependencies]` in `src-tauri/Cargo.toml`.

2. Create `src-tauri/src/routes/fonts.rs`:
   ```rust
   use font_kit::source::SystemSource;

   #[tauri::command]
   pub fn list_system_fonts() -> Vec<String> {
       let source = SystemSource::new();
       match source.all_families() {
           Ok(mut families) => {
               families.sort_unstable();
               families.dedup();
               families
           }
           Err(e) => {
               tracing::warn!("Failed to enumerate system fonts: {}", e);
               vec![]
           }
       }
   }
   ```

3. Update `src-tauri/src/main.rs`:
   - Add `mod routes;` if not already present (or add the fonts module)
   - Add `routes::fonts::list_system_fonts` to the `.invoke_handler(tauri::generate_handler![..., routes::fonts::list_system_fonts])` list
   - If the routes module structure doesn't match, create the file at the appropriate location and register it. Check how existing Tauri commands are registered (look for `tauri::generate_handler` call).

4. Create `frontend/src/lib/google-fonts.ts`:
   - Export `GOOGLE_FONTS`: Static array of top 100 Google Fonts by popularity. Each entry:
     ```typescript
     interface GoogleFont { family: string; category: 'sans-serif' | 'serif' | 'monospace' | 'display' | 'handwriting' }
     ```
   - Include fonts like: Roboto, Open Sans, Lato, Montserrat, Oswald, Raleway, Poppins, Merriweather, Nunito, Playfair Display, Source Code Pro, Ubuntu Mono, Fira Sans, PT Sans, Work Sans, Quicksand, Barlow, Cabin, Karla, Josefin Sans, DM Sans, Manrope, Space Grotesk, Libre Franklin, IBM Plex Sans, IBM Plex Mono, Inconsolata, Source Sans 3, Noto Sans, Roboto Mono, etc.
   - Export `loadGoogleFont(family: string): void`:
     - Check if font is already loaded (check `document.fonts` or a local Set)
     - Create `<link>` element with `href="https://fonts.googleapis.com/css2?family=${encodeURIComponent(family)}:wght@300;400;500;600;700&display=swap"` (no API key needed for CSS endpoint, per RESEARCH.md Pitfall #4)
     - Append to document.head
     - Track loaded fonts to avoid duplicate link elements
   - Export `isGoogleFontLoaded(family: string): boolean`
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/Cargo.toml contains `font-kit = "0.14"` (or compatible version)
    - fonts.rs file exists with `#[tauri::command] pub fn list_system_fonts() -> Vec<String>`
    - main.rs invoke_handler includes list_system_fonts
    - google-fonts.ts exports GOOGLE_FONTS array with at least 50 entries
    - google-fonts.ts exports loadGoogleFont function that creates a <link> element
    - google-fonts.ts loadGoogleFont URL uses fonts.googleapis.com/css2 (no API key)
    - cargo check succeeds
  </acceptance_criteria>
  <done>System font enumeration via Tauri command works, Google Fonts static list bundled, font loading via CSS2 API (no key needed)</done>
</task>

<task type="auto">
  <name>Task 2: Create FontPicker and BrandingSettings components</name>
  <files>frontend/src/components/FontPicker.tsx, frontend/src/components/BrandingSettings.tsx</files>
  <read_first>
    - frontend/src/lib/theme-store.ts (setFontOverride, setFontScale, setGlobalFontOverride, useThemeState)
    - frontend/src/lib/google-fonts.ts (just created -- GOOGLE_FONTS, loadGoogleFont)
    - frontend/src/lib/sidebar-settings.ts (getSidebarTitleText, setSidebarTitleText -- existing sidebar title)
    - .planning/phases/02-theming-system/02-UI-SPEC.md (Font Picker layout, Custom Branding spec, all copy text)
    - frontend/src/pages/settings/shared.ts (row, sectionLabel style helpers)
  </read_first>
  <action>
1. Create `frontend/src/components/FontPicker.tsx`:

   **Structure per UI-SPEC Font Picker layout:**
   - 4 font slot rows, each with:
     - Label: "Body", "Heading", "Monospace", "UI" (using `--text-sm` weight 500)
     - Custom dropdown (not native `<select>` for styling): Shows current font name rendered in THAT font's typeface
     - Preview text: "The quick brown fox jumps over the lazy dog" in the selected font at `--text-base` size
   - Font source sections in dropdown:
     - **Bundled** (always first): Inter, JetBrains Mono, Fira Code
     - **System Fonts** (from Tauri command): Call `invoke<string[]>('list_system_fonts')` on component mount. Cache in state. Show loading spinner while fetching. If Tauri IPC unavailable (browser mode), show empty section with "System fonts available in desktop app" message.
     - **Google Fonts** (from static list): Show all 100+ fonts. When user selects one, call `loadGoogleFont(family)` to inject the CSS link.
   - Each font item in dropdown renders its name in its own typeface (via inline `style={{ fontFamily: font }}`)
   - Selecting a font calls `setFontOverride(slot, font)` which triggers theme-store -> theme-engine -> CSS variable update
   - The slot's font changes immediately (live preview) per UI-SPEC

   **Base size slider:**
   - `<input type="range">` with `min={80}` `max={120}` `step={5}` `value={currentScale * 100}`
   - `role="slider"`, `aria-valuemin={80}`, `aria-valuemax={120}`, `aria-valuenow={currentScale * 100}`, `aria-label="Base font size"`
   - Display current value as percentage: "100%"
   - On change: `setFontScale(value / 100)`

   **Global override checkbox:**
   - `<label>` with `<input type="checkbox">` and text "Keep my fonts when switching themes"
   - Checked state: `state.globalFontOverride`
   - On change: `setGlobalFontOverride(checked)`

2. Create `frontend/src/components/BrandingSettings.tsx`:

   **Fields** (from UI-SPEC Custom Branding spec):
   - **App title**: `<input type="text" placeholder="OpenClaw Manager" maxLength={40}>` with `<label htmlFor="branding-title">App Title</label>`
     - On change: Store in localStorage key `branding-app-title`, also update `document.title`
   - **App logo**: File upload button (`<input type="file" accept="image/png,image/svg+xml,image/jpeg">`) with 512KB max check
     - 32px height preview next to upload button
     - Store as base64 data URL in localStorage key `branding-logo`
     - Clear button to reset to default logo
   - **Sidebar header text**: `<input type="text" placeholder="OpenClaw" maxLength={20}>` with label
     - On change: Call `setSidebarTitleText(value)` from sidebar-settings.ts
   - **Login page tagline**: `<input type="text" placeholder="Your personal command center" maxLength={80}>` with label
     - Store in localStorage key `branding-tagline`

   - All inputs: associated `<label>` via `htmlFor`/`id`, clear (X) button to reset each field, changes auto-persist (no save button)
   - Use `row` and `sectionLabel` styles from settings/shared.ts for consistent layout
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - FontPicker.tsx renders 4 font slot rows with labels "Body", "Heading", "Monospace", "UI"
    - FontPicker.tsx imports invoke from '@tauri-apps/api/core' for system font enumeration
    - FontPicker.tsx imports GOOGLE_FONTS, loadGoogleFont from '@/lib/google-fonts'
    - FontPicker.tsx contains range input with min={80} max={120} step={5} for base size
    - FontPicker.tsx contains "Keep my fonts when switching themes" checkbox
    - FontPicker.tsx has aria-label="Base font size" on the slider
    - BrandingSettings.tsx has 4 input fields: app title, logo, sidebar header, login tagline
    - BrandingSettings.tsx has maxLength constraints: 40, N/A, 20, 80
    - BrandingSettings.tsx updates document.title when app title changes
    - All label elements use htmlFor with matching input ids
    - Existing tests pass (no regressions)
  </acceptance_criteria>
  <done>FontPicker provides 4-slot font customization with 3 font sources and base size scaling. BrandingSettings enables full app title/logo/text customization. Both auto-persist.</done>
</task>

</tasks>

<verification>
cd frontend && npx vitest run --reporter=verbose && cd ../src-tauri && cargo check
</verification>

<success_criteria>
- font-kit compiles and list_system_fonts is registered as Tauri command
- Google Fonts loaded from static list (no API key exposure)
- FontPicker has 3 font sources in dropdown per slot
- Font scale slider only affects --text-* variables (not html font-size)
- BrandingSettings fields auto-persist with clear-to-default buttons
- All ARIA requirements met per UI-SPEC accessibility checklist
</success_criteria>

<output>
After completion, create `.planning/phases/02-theming-system/02-05-SUMMARY.md`
</output>
