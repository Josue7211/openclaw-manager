---
phase: 01-responsive-layout-visual-polish
plan: 05
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - frontend/src/pages/Messages.tsx
  - frontend/src/pages/messages/ConversationList.tsx
  - frontend/src/pages/messages/MessageThread.tsx
  - frontend/src/pages/Chat.tsx
  - frontend/src/pages/chat/ChatThread.tsx
  - frontend/src/pages/Email.tsx
  - frontend/src/pages/email/ManagePanel.tsx
  - frontend/src/pages/Reminders.tsx
  - frontend/src/pages/notes/Notes.tsx
  - frontend/src/pages/notes/FileTree.tsx
  - frontend/src/pages/Memory.tsx
  - frontend/src/pages/Pomodoro.tsx
  - frontend/src/pages/pomodoro/SessionSidebar.tsx
  - frontend/src/pages/CronJobs.tsx
  - frontend/src/pages/Capture.tsx
  - frontend/src/pages/MediaRadar.tsx
  - frontend/src/pages/Agents.tsx
  - frontend/src/pages/settings/SettingsUser.tsx
  - frontend/src/pages/settings/SettingsModules.tsx
  - frontend/src/pages/settings/SettingsStatus.tsx
  - frontend/src/pages/settings/SettingsKeybindings.tsx
  - frontend/src/pages/settings/SettingsConnections.tsx
  - frontend/src/pages/settings/SettingsNotifications.tsx
  - frontend/src/pages/settings/SettingsPrivacy.tsx
  - frontend/src/pages/settings/SettingsData.tsx
  - frontend/src/pages/knowledge/AddEntryModal.tsx
  - frontend/src/pages/knowledge/SlidePanel.tsx
  - frontend/src/pages/dashboard/IdeaBriefingCard.tsx
  - frontend/src/pages/dashboard/IdeaDetailPanel.tsx
autonomous: true
gap_closure: true
requirements: [POLISH-03, POLISH-06, POLISH-07]

must_haves:
  truths:
    - "Every data-fetching page in Plan 05's scope displays ErrorState with retry when data fetch fails, not inline error text"
    - "Every list/data page in Plan 05's scope displays EmptyState with contextual icon and copy when there is no data, not inline strings"
    - "Button component is used for primary and secondary actions in Plan 05's Settings and Modal pages, replacing all inline styled buttons"
  artifacts:
    - path: "frontend/src/pages/messages/ConversationList.tsx"
      provides: "EmptyState for no conversations"
      contains: "import.*EmptyState.*from.*@/components/ui/EmptyState"
    - path: "frontend/src/pages/chat/ChatThread.tsx"
      provides: "EmptyState for no messages"
      contains: "import.*EmptyState.*from.*@/components/ui/EmptyState"
    - path: "frontend/src/pages/Email.tsx"
      provides: "ErrorState for email fetch failures"
      contains: "import.*ErrorState.*from.*@/components/ui/ErrorState"
    - path: "frontend/src/pages/settings/SettingsUser.tsx"
      provides: "Button component for all user settings actions"
      contains: "import.*Button.*from.*@/components/ui/Button"
    - path: "frontend/src/pages/pomodoro/SessionSidebar.tsx"
      provides: "EmptyState for no pomodoro sessions"
      contains: "import.*EmptyState.*from.*@/components/ui/EmptyState"
  key_links:
    - from: "frontend/src/pages/messages/ConversationList.tsx"
      to: "frontend/src/components/ui/EmptyState.tsx"
      via: "import and render"
      pattern: "import.*EmptyState"
    - from: "frontend/src/pages/Email.tsx"
      to: "frontend/src/components/ui/ErrorState.tsx"
      via: "import and render on error"
      pattern: "import.*ErrorState"
    - from: "frontend/src/pages/settings/SettingsUser.tsx"
      to: "frontend/src/components/ui/Button.tsx"
      via: "import and render for actions"
      pattern: "import.*Button.*from.*@/components/ui/Button"
---

<objective>
Adopt shared EmptyState, ErrorState, and Button components across the second half of app pages: Messages, Chat, Email, Reminders, Notes, Memory, Pomodoro, CronJobs, Capture, MediaRadar, Agents, all Settings sub-pages, Knowledge modals, and remaining Dashboard cards.

Purpose: Complete the gap closure by ensuring ALL pages in the app use shared design system components instead of ad-hoc inline styling. Combined with Plan 04, this achieves full adoption of EmptyState, ErrorState, and Button across the entire codebase.

Output: 29 page files updated to use shared UI components from components/ui/.
</objective>

<execution_context>
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-responsive-layout-visual-polish/01-02-SUMMARY.md
@.planning/phases/01-responsive-layout-visual-polish/01-VERIFICATION.md
@.planning/phases/01-responsive-layout-visual-polish/01-UI-SPEC.md

<interfaces>
<!-- Shared UI components created in Plan 02. Executor uses these directly. -->

From frontend/src/components/ui/Button.tsx:
```typescript
type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'danger'
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ButtonVariant  // default: 'primary'
  children: React.ReactNode
}
export const Button = React.memo(function Button({ variant, children, ...rest }: ButtonProps) { ... })
// Supports all standard button HTML attributes (onClick, disabled, className, style, type, aria-label, etc.)
```

From frontend/src/components/ui/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon: React.ElementType      // Phosphor icon component
  title: string                // e.g. "No conversations yet"
  description?: string         // e.g. "Your messages will appear here."
  action?: { label: string; onClick: () => void }
}
export const EmptyState = React.memo(function EmptyState({ icon, title, description, action }: EmptyStateProps) { ... })
```

From frontend/src/components/ui/ErrorState.tsx:
```typescript
interface ErrorStateProps {
  message?: string             // Custom error message (overrides default)
  resource?: string            // e.g. "emails" -> "We couldn't load your emails."
  onRetry?: () => void         // Retry callback (shows "Try Again" button)
}
export const ErrorState = React.memo(function ErrorState({ message, resource, onRetry }: ErrorStateProps) { ... })
// Always shows "Reload Page" button. Shows "Try Again" only if onRetry provided.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt EmptyState and ErrorState across Messages, Chat, Email, Notes, Memory, Pomodoro, and remaining pages</name>
  <files>
    frontend/src/pages/Messages.tsx
    frontend/src/pages/messages/ConversationList.tsx
    frontend/src/pages/messages/MessageThread.tsx
    frontend/src/pages/Chat.tsx
    frontend/src/pages/chat/ChatThread.tsx
    frontend/src/pages/Email.tsx
    frontend/src/pages/email/ManagePanel.tsx
    frontend/src/pages/Reminders.tsx
    frontend/src/pages/notes/Notes.tsx
    frontend/src/pages/notes/FileTree.tsx
    frontend/src/pages/Memory.tsx
    frontend/src/pages/Pomodoro.tsx
    frontend/src/pages/pomodoro/SessionSidebar.tsx
    frontend/src/pages/CronJobs.tsx
    frontend/src/pages/Capture.tsx
    frontend/src/pages/MediaRadar.tsx
    frontend/src/pages/Agents.tsx
    frontend/src/pages/dashboard/IdeaBriefingCard.tsx
    frontend/src/pages/dashboard/IdeaDetailPanel.tsx
  </files>
  <read_first>
    frontend/src/components/ui/EmptyState.tsx
    frontend/src/components/ui/ErrorState.tsx
    frontend/src/components/ui/Button.tsx
    frontend/src/pages/Messages.tsx
    frontend/src/pages/messages/ConversationList.tsx
    frontend/src/pages/chat/ChatThread.tsx
    frontend/src/pages/Email.tsx
    frontend/src/pages/email/ManagePanel.tsx
    frontend/src/pages/Reminders.tsx
    frontend/src/pages/notes/Notes.tsx
    frontend/src/pages/notes/FileTree.tsx
    frontend/src/pages/Memory.tsx
    frontend/src/pages/Pomodoro.tsx
    frontend/src/pages/pomodoro/SessionSidebar.tsx
    frontend/src/pages/CronJobs.tsx
    frontend/src/pages/Capture.tsx
    frontend/src/pages/MediaRadar.tsx
    frontend/src/pages/Agents.tsx
    frontend/src/pages/dashboard/IdeaBriefingCard.tsx
    frontend/src/pages/dashboard/IdeaDetailPanel.tsx
  </read_first>
  <action>
For each file, replace inline empty state strings with the shared EmptyState component, and replace inline error displays with the shared ErrorState component. Import from `@/components/ui/EmptyState` and `@/components/ui/ErrorState`.

**EmptyState replacements:**

1. `messages/ConversationList.tsx` line ~316: Replace `"No conversations yet"` div with `<EmptyState icon={ChatDots} title="No conversations yet" description="Your messages will appear here." />`
2. `chat/ChatThread.tsx` line ~89: Replace `"No messages yet"` div with `<EmptyState icon={ChatCircle} title="No messages yet" description="Start a conversation with your AI assistant." />`
3. `email/ManagePanel.tsx` line ~242: Replace `"No accounts yet. Add one above."` with `<EmptyState icon={EnvelopeSimple} title="No accounts yet" description="Add an email account above." />`
4. `Memory.tsx` line ~159: Replace `'No matches'` / `'No files found'` with `<EmptyState icon={Brain} title={q ? 'No matches' : 'No files found'} />`
5. `Memory.tsx` line ~184: Replace `'No matches'` / `'No logs found'` with `<EmptyState icon={Scroll} title={q ? 'No matches' : 'No logs found'} />`
6. `pomodoro/SessionSidebar.tsx` line ~119: Replace `"No sessions yet — start the timer"` with `<EmptyState icon={Timer} title="No sessions yet" description="Start the timer to track your focus." />`
7. `notes/FileTree.tsx`: If there are inline empty states for no files, replace with EmptyState
8. `settings/SettingsModules.tsx` line ~1211: Replace `"Recycle bin is empty"` with `<EmptyState icon={Recycle} title="Recycle bin is empty" />`
9. `settings/SettingsStatus.tsx` line ~314: Replace `"No peers found (tailscale may not be installed)"` with `<EmptyState icon={WifiHigh} title="No peers found" description="Tailscale may not be installed or running." />`

**ErrorState replacements:**

1. `Messages.tsx` line ~646: Replace `if (error)` block with `<ErrorState resource="messages" onRetry={refetch} />`
2. `Email.tsx` line ~304: Replace the inline error display with `<ErrorState resource="emails" onRetry={refetch} />`
3. `Reminders.tsx`: If there is an error display for reminders data fetch, replace with `<ErrorState resource="reminders" onRetry={refetch} />`
4. `Chat.tsx` line ~64: If the `historyError` display is an inline div, replace with `<ErrorState resource="chat history" onRetry={refetchHistory} />`
5. `HomeLab.tsx`: Already handled in Plan 04 (not in this plan's scope)

**Important rules:**
- Import appropriate Phosphor icons: `ChatDots`, `ChatCircle`, `EnvelopeSimple`, `Brain`, `Scroll`, `Timer`, `Recycle`, `WifiHigh`, etc. Only import what each file actually needs. Many files already import Phosphor icons from Plan 03.
- For compact contexts (sidebar panels, cards), wrap EmptyState in a smaller container if needed: `<div style={{ padding: '16px 0' }}><EmptyState ... /></div>`
- For Messages and Chat pages which use full-bleed layout (`position: absolute; inset: 0`), ensure EmptyState/ErrorState render within the existing layout container, not breaking the absolute positioning.
- Do NOT change data fetching logic, WebSocket connections, SSE handlers, or any business logic.
- Do NOT modify test files.
- For files that already use inline error styling with useful retry logic (like email form errors), keep the form-level error display and only replace page-level data fetch errors with ErrorState.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "import.*EmptyState.*from.*@/components/ui/EmptyState" frontend/src/pages/messages/ConversationList.tsx frontend/src/pages/chat/ChatThread.tsx frontend/src/pages/email/ManagePanel.tsx frontend/src/pages/Memory.tsx frontend/src/pages/pomodoro/SessionSidebar.tsx frontend/src/pages/settings/SettingsStatus.tsx | wc -l returns >= 6
    - grep -rn "import.*ErrorState.*from.*@/components/ui/ErrorState" frontend/src/pages/Messages.tsx frontend/src/pages/Email.tsx | wc -l returns >= 2
    - npx vitest run exits with code 0
  </acceptance_criteria>
  <done>
    - At least 6 page files in Plan 05 scope import and render EmptyState
    - At least 2 page files import and render ErrorState for data fetch failures
    - All inline empty state strings in Plan 05 scope files have been replaced with EmptyState
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Adopt Button component across Settings sub-pages, Knowledge modals, and remaining pages with inline styled buttons</name>
  <files>
    frontend/src/pages/settings/SettingsUser.tsx
    frontend/src/pages/settings/SettingsKeybindings.tsx
    frontend/src/pages/settings/SettingsConnections.tsx
    frontend/src/pages/settings/SettingsNotifications.tsx
    frontend/src/pages/settings/SettingsPrivacy.tsx
    frontend/src/pages/settings/SettingsData.tsx
    frontend/src/pages/knowledge/AddEntryModal.tsx
    frontend/src/pages/knowledge/SlidePanel.tsx
    frontend/src/pages/messages/ConversationList.tsx
    frontend/src/pages/messages/MessageThread.tsx
  </files>
  <read_first>
    frontend/src/components/ui/Button.tsx
    frontend/src/pages/settings/SettingsUser.tsx
    frontend/src/pages/settings/SettingsKeybindings.tsx
    frontend/src/pages/settings/SettingsConnections.tsx
    frontend/src/pages/settings/SettingsNotifications.tsx
    frontend/src/pages/settings/SettingsPrivacy.tsx
    frontend/src/pages/settings/SettingsData.tsx
    frontend/src/pages/knowledge/AddEntryModal.tsx
    frontend/src/pages/knowledge/SlidePanel.tsx
    frontend/src/pages/messages/ConversationList.tsx
    frontend/src/pages/messages/MessageThread.tsx
  </read_first>
  <action>
Replace inline styled `<button>` elements with the shared `<Button>` component from `@/components/ui/Button`. Map each button to the correct variant based on its purpose:

**Variant mapping rules:**
- **primary:** Main page CTA, save, confirm, submit buttons (solid accent background)
- **secondary:** Cancel, dismiss, secondary action buttons (outlined border)
- **ghost:** Toolbar actions, inline actions, icon-only buttons (transparent background)
- **danger:** Delete, remove, destructive confirmation buttons (red background)

**Settings pages (highest concentration of inline styled buttons):**

1. `SettingsUser.tsx` (~18 inline buttons): This page has the most buttons. Replace:
   - Save/Update profile buttons -> `<Button variant="primary">`
   - Cancel buttons -> `<Button variant="secondary">`
   - Delete/Remove actions -> `<Button variant="danger">`
   - Small action buttons (edit, toggle) -> `<Button variant="ghost">`
   - Transfer all existing `onClick`, `disabled`, `aria-label`, `className`, and other attributes to the Button component props

2. `SettingsKeybindings.tsx` (~6 inline buttons): Replace:
   - Reset/Save buttons -> `<Button variant="primary">` or `<Button variant="secondary">`
   - Small edit buttons -> `<Button variant="ghost">`

3. `SettingsConnections.tsx` (~3 inline buttons): Replace:
   - Test connection / Save buttons -> `<Button variant="primary">`
   - Cancel buttons -> `<Button variant="secondary">`

4. `SettingsNotifications.tsx` (~3 inline buttons): Replace:
   - Save/Test buttons -> `<Button variant="primary">`

5. `SettingsPrivacy.tsx` (~2 inline buttons): Replace:
   - Export/Import buttons -> `<Button variant="secondary">`

6. `SettingsData.tsx` (~2 inline buttons): Replace:
   - Export/Import buttons -> `<Button variant="secondary">`
   - Delete buttons -> `<Button variant="danger">`

**Knowledge modals:**

7. `knowledge/AddEntryModal.tsx` (~2 inline buttons): Replace:
   - Save/Add button -> `<Button variant="primary">`
   - Cancel button -> `<Button variant="secondary">`

8. `knowledge/SlidePanel.tsx` (~2 inline buttons): Replace:
   - Action buttons -> `<Button variant="primary">` or `<Button variant="ghost">`

**Messages (for buttons with inline styles only):**

9. `messages/ConversationList.tsx` (~3 inline styled buttons): Replace styled filter/action buttons with `<Button variant="ghost">` where appropriate. Leave message-specific interactive elements (like conversation row clicks) as-is.

10. `messages/MessageThread.tsx` (~1 inline styled button): Replace any styled action button with appropriate `<Button>` variant.

**Important rules:**
- Import: `import { Button } from '@/components/ui/Button'`
- Transfer ALL existing props: `onClick`, `disabled`, `aria-label`, `title`, `className`, `data-*` attributes
- If a button has custom `style` overrides beyond what the variant provides (e.g., custom width, margin), pass those via the `style` prop: `<Button variant="primary" style={{ marginTop: '8px' }}>`
- Do NOT remove any existing functionality, event handlers, or conditional logic
- Do NOT replace buttons that are part of third-party libraries or highly custom interactive components (toggles, radio buttons, close X buttons in modals)
- Do NOT replace the Toggle component in settings (it has its own role="switch" pattern)
- Leave icon-only buttons that are very small (< 24px) as-is if they would look wrong with Button padding
- If a button has `type="submit"` in a form, keep that: `<Button variant="primary" type="submit">`
- If a file was already modified in Task 1 (e.g., ConversationList.tsx), ensure imports are merged (both EmptyState and Button on the same file)
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "import.*Button.*from.*@/components/ui/Button" frontend/src/pages/settings/SettingsUser.tsx frontend/src/pages/settings/SettingsKeybindings.tsx frontend/src/pages/settings/SettingsConnections.tsx frontend/src/pages/knowledge/AddEntryModal.tsx | wc -l returns >= 4
    - grep -c '<button[^>]*style=' frontend/src/pages/settings/SettingsUser.tsx returns a number lower than 5 (was 10+ before, some may remain for very custom buttons)
    - npx vitest run exits with code 0
  </acceptance_criteria>
  <done>
    - At least 8 page files import and render the shared Button component
    - SettingsUser.tsx inline styled button count is reduced by at least 50%
    - All knowledge modal buttons use shared Button component
    - All existing test suites pass
    - Button hierarchy (primary/secondary/ghost/danger) is visually consistent across all Settings pages
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "import.*EmptyState.*from.*@/components/ui/EmptyState" frontend/src/pages/ | wc -l` should return >= 8 (Plan 05 scope)
2. `grep -rn "import.*ErrorState.*from.*@/components/ui/ErrorState" frontend/src/pages/ | wc -l` should return >= 2 (Plan 05 scope)
3. `grep -rn "import.*Button.*from.*@/components/ui/Button" frontend/src/pages/ | wc -l` should return >= 8 (Plan 05 scope)
4. `cd frontend && npx vitest run` should exit with code 0
5. The following ad-hoc patterns should be eliminated in Plan 05 scope:
   - `grep -rn "No conversations yet\|No messages yet\|No accounts yet\|No sessions yet\|No peers found\|Recycle bin is empty" frontend/src/pages/messages/ConversationList.tsx frontend/src/pages/chat/ChatThread.tsx frontend/src/pages/email/ManagePanel.tsx frontend/src/pages/pomodoro/SessionSidebar.tsx frontend/src/pages/settings/SettingsStatus.tsx frontend/src/pages/settings/SettingsModules.tsx` should return 0 matches
</verification>

<success_criteria>
- Shared EmptyState is imported and rendered by at least 8 page files in Plan 05's scope
- Shared ErrorState is imported by at least 2 page files for data fetch error handling
- Shared Button component is imported by at least 8 page files, primarily in Settings sub-pages
- Zero inline empty state ad-hoc strings remain in Plan 05 scope files
- Inline styled button count across Settings pages is reduced by at least 50%
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-responsive-layout-visual-polish/01-05-SUMMARY.md`
</output>
