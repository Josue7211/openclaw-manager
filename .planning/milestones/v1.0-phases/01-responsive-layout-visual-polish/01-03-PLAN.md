---
phase: 01-responsive-layout-visual-polish
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/package.json
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/GlobalSearch.tsx
  - frontend/src/components/CommandPalette.tsx
  - frontend/src/components/NotificationCenter.tsx
  - frontend/src/components/OnboardingWelcome.tsx
  - frontend/src/components/KeyboardShortcutsModal.tsx
  - frontend/src/components/Lightbox.tsx
  - frontend/src/components/ConnectionStatus.tsx
  - frontend/src/components/DemoModeBanner.tsx
  - frontend/src/components/BackendErrorBanner.tsx
  - frontend/src/components/messages/ContactAvatar.tsx
  - frontend/src/components/messages/MessageMenu.tsx
  - frontend/src/components/messages/AudioWaveform.tsx
  - frontend/src/lib/nav-items.ts
  - frontend/src/pages/Messages.tsx
  - frontend/src/pages/Settings.tsx
  - frontend/src/pages/Pomodoro.tsx
  - frontend/src/pages/Personal.tsx
  - frontend/src/pages/Todos.tsx
  - frontend/src/pages/Missions.tsx
  - frontend/src/pages/Reminders.tsx
  - frontend/src/pages/Search.tsx
  - frontend/src/pages/Calendar.tsx
  - frontend/src/pages/MediaRadar.tsx
  - frontend/src/pages/Status.tsx
  - frontend/src/pages/Email.tsx
  - frontend/src/pages/CronJobs.tsx
  - frontend/src/pages/KnowledgeBase.tsx
  - frontend/src/pages/NotFound.tsx
  - frontend/src/pages/Capture.tsx
  - frontend/src/pages/dashboard/NetworkCard.tsx
  - frontend/src/pages/dashboard/AgentsCard.tsx
  - frontend/src/pages/dashboard/DashboardHeader.tsx
  - frontend/src/pages/dashboard/IdeaDetailPanel.tsx
  - frontend/src/pages/dashboard/SessionsCard.tsx
  - frontend/src/pages/dashboard/HeartbeatCard.tsx
  - frontend/src/pages/dashboard/MemoryCard.tsx
  - frontend/src/pages/dashboard/IdeaBriefingCard.tsx
  - frontend/src/pages/dashboard/AgentStatusCard.tsx
  - frontend/src/pages/dashboard/MissionsCard.tsx
  - frontend/src/pages/settings/SettingsConnections.tsx
  - frontend/src/pages/settings/SettingsModules.tsx
  - frontend/src/pages/settings/SettingsUser.tsx
  - frontend/src/pages/settings/SettingsStatus.tsx
  - frontend/src/pages/settings/SettingsDisplay.tsx
  - frontend/src/pages/messages/ConversationList.tsx
  - frontend/src/pages/messages/MessageThread.tsx
  - frontend/src/pages/messages/ComposePanel.tsx
  - frontend/src/pages/personal/MorningBrief.tsx
  - frontend/src/pages/personal/TodoSection.tsx
  - frontend/src/pages/personal/HomelabSection.tsx
  - frontend/src/pages/personal/DailyReviewWidget.tsx
  - frontend/src/pages/chat/ChatThread.tsx
  - frontend/src/pages/chat/ChatInput.tsx
  - frontend/src/pages/chat/NotConfiguredBanner.tsx
  - frontend/src/pages/missions/AccordionBody.tsx
  - frontend/src/pages/missions/MissionCard.tsx
  - frontend/src/pages/missions/utils.tsx
  - frontend/src/pages/pomodoro/TimerDisplay.tsx
  - frontend/src/pages/pomodoro/TimerControls.tsx
  - frontend/src/pages/pomodoro/SessionSidebar.tsx
  - frontend/src/pages/email/ManagePanel.tsx
  - frontend/src/pages/email/EmailList.tsx
  - frontend/src/pages/email/AccountSwitcher.tsx
  - frontend/src/pages/knowledge/SlidePanel.tsx
  - frontend/src/pages/knowledge/EntryCard.tsx
  - frontend/src/pages/knowledge/AddEntryModal.tsx
  - frontend/src/pages/notes/Notes.tsx
  - frontend/src/pages/notes/FileTree.tsx
  - frontend/src/pages/calendar/MonthView.tsx
  - frontend/src/pages/crons/JobList.tsx
  - frontend/src/pages/crons/FrequentBar.tsx
  - frontend/src/pages/pipeline/PipelineStale.tsx
  - frontend/src/pages/pipeline/FilterDropdown.tsx
  - frontend/src/pages/pipeline/PipelineShipLog.tsx
  - frontend/src/pages/pipeline/PipelineIdeas.tsx
  - frontend/src/pages/pipeline/types.ts
autonomous: true
requirements:
  - POLISH-01
  - POLISH-08

must_haves:
  truths:
    - "Zero files import from 'lucide-react' — all icons come from @phosphor-icons/react"
    - "Zero hardcoded hex color literals (#xxx, #xxxxxx) remain in JSX/TS files (excluding globals.css, themes.ts, test files)"
    - "Zero hardcoded rgba() color literals remain in JSX/TS files (excluding globals.css, themes.ts, test files)"
    - "All icons use Phosphor with weight='regular' as default, weight='bold' for active states, weight='fill' for filled indicators"
    - "Traffic light button colors (#ff5f57, #febc2e, #28c840) in LayoutShell.tsx are intentionally kept as hardcoded values (documented with comments)"
  artifacts:
    - path: "frontend/package.json"
      provides: "@phosphor-icons/react dependency installed"
      contains: "@phosphor-icons/react"
  key_links:
    - from: "All 78 files"
      to: "@phosphor-icons/react"
      via: "Import statements switched from lucide-react to @phosphor-icons/react"
      pattern: "from '@phosphor-icons/react'"
---

<objective>
Migrate all 78 files from lucide-react to @phosphor-icons/react and audit all hardcoded color values in JSX/TS files, replacing them with CSS variables.

Purpose: POLISH-08 requires consistent icon style across all modules. POLISH-01 requires zero hardcoded colors — this is a hard prerequisite for Phase 2 (theming). Both are mechanical bulk migrations that touch many files but follow simple patterns.

Output: Zero lucide-react imports, zero hardcoded colors in TSX/TS files (except intentional exemptions), @phosphor-icons/react installed.
</objective>

<execution_context>
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-responsive-layout-visual-polish/01-CONTEXT.md
@.planning/phases/01-responsive-layout-visual-polish/01-RESEARCH.md
@.planning/phases/01-responsive-layout-visual-polish/01-UI-SPEC.md
@.planning/phases/01-responsive-layout-visual-polish/01-01-SUMMARY.md

<interfaces>
<!-- Icon name mapping from RESEARCH.md -->

Lucide -> Phosphor icon name mapping (most common):
| Lucide            | Phosphor          | Notes |
|-------------------|--------------------|-------|
| ChevronRight/Down/Up | CaretRight/Down/Up | Same concept |
| Settings          | Gear               | |
| Plus              | Plus               | Same name |
| Search            | MagnifyingGlass    | |
| X                 | X                  | Same name |
| Trash2            | Trash              | |
| Check             | Check              | Same name |
| Home              | House              | |
| MessageCircle     | ChatCircle         | |
| Bell              | Bell               | Same name |
| FileText          | FileText           | Same name |
| Mail              | Envelope           | |
| AlertTriangle     | Warning            | |
| Copy              | Copy               | Same name |
| Play/Pause        | Play/Pause         | Same names |
| StickyNote        | Note               | |
| CheckSquare       | CheckSquare        | Same name |
| Lightbulb         | Lightbulb          | Same name |
| Flag              | Flag               | Same name |
| ArrowUp/Down      | ArrowUp/ArrowDown  | Same names |
| Pencil            | PencilSimple       | |
| FolderPlus        | FolderPlus         | Same name |
| EyeOff            | EyeSlash           | |

Phosphor weight mapping (per UI-SPEC):
- `regular` (default) for most icons — equivalent to lucide default stroke
- `bold` for active/selected states
- `fill` for filled indicators
- Sizes: 20px sidebar/nav, 16px inline, 24px page headers

Lucide `size` prop maps directly to Phosphor `size` prop.
Lucide `strokeWidth` has no Phosphor equivalent — remove it.
Lucide `className` maps to Phosphor `className`.

For icons NOT in the mapping table above, search the Phosphor catalog at https://phosphoricons.com/.
If no exact match exists, use the closest conceptual equivalent and add a comment noting the substitution.

<!-- CSS variable mapping for hardcoded colors -->

Common hardcoded color -> CSS variable mapping:
| Hardcoded Value | CSS Variable | Context |
|-----------------|-------------|---------|
| rgba(245, 158, 11, X) | var(--warning) / var(--warning-aXX) | Warning/amber indicators |
| rgba(239, 68, 68, X) | var(--red-500-aXX) / var(--red-500) | Error/danger indicators |
| rgba(74, 222, 128, X) | var(--green-400-aXX) / var(--green-400) | Success/online indicators |
| rgba(155, 132, 236, X) | var(--purple-aXX) | Accent decorations |
| rgba(167, 139, 250, X) | var(--accent-aXX) | Accent highlights |
| rgba(255, 255, 255, X) | var(--bg-white-XX) / var(--hover-bg) | White overlays |
| rgba(0, 0, 0, X) | var(--overlay) / var(--overlay-light) | Dark overlays |
| #ff5f57, #febc2e, #28c840 | KEEP AS-IS | macOS traffic light buttons (intentional) |
| #1e1e1e, #2a2a2a | var(--border-subtle) / var(--border-strong) | Dark borders |
| #0a0a0c | var(--bg-base) | Base background |
| rgba(0,0,0,0.6) on tl-icon spans | KEEP AS-IS | Traffic light icon color |

Exempted files (do NOT modify colors in these):
- frontend/src/globals.css (defines the variables themselves)
- frontend/src/lib/themes.ts (defines accent presets — raw hex values are the data)
- frontend/src/**/__tests__/** (test files can have hardcoded values)
- frontend/src/lib/demo-data.ts (demo data may contain display colors)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @phosphor-icons/react and migrate all 78 files from lucide-react to Phosphor Icons</name>
  <files>frontend/package.json, all 78 files listed in files_modified</files>
  <read_first>
    - frontend/package.json (current dependencies)
    - frontend/src/components/Sidebar.tsx (largest lucide import — 15 icons on line 4)
    - frontend/src/lib/nav-items.ts (nav icon imports)
    - .planning/phases/01-responsive-layout-visual-polish/01-RESEARCH.md (Pattern 3: Page-by-Page Icon Migration, Pitfall 2: icon name mismatches, Pitfall 6: bundle size)
    - .planning/phases/01-responsive-layout-visual-polish/01-UI-SPEC.md (Icon Migration section — weights, sizes)
  </read_first>
  <action>
**1. Install @phosphor-icons/react:**

```bash
cd frontend && npm install @phosphor-icons/react
```

**2. Migrate all 78 files from lucide-react to @phosphor-icons/react.**

For each file, follow this pattern:

a) Read the file to find the lucide-react import line
b) Identify each icon name imported
c) Map to the Phosphor equivalent using the mapping table in the interfaces block above
d) Replace the import statement: `from 'lucide-react'` -> `from '@phosphor-icons/react'`
e) Update icon names in JSX usage
f) Remove any `strokeWidth` props (Phosphor uses `weight` instead, default is `regular`)
g) Keep `size` props as-is (both libraries use the same prop name)
h) Add `weight="regular"` only when explicitly overriding (regular is the default, so it can be omitted)
i) For active/selected icon states, use `weight="bold"` or `weight="fill"` as appropriate

**Icon size guidelines (from UI-SPEC):**
- Sidebar/nav icons: size={20}
- Inline icons (buttons, list items): size={16}
- Page headers: size={24}
- Keep any existing custom sizes as-is (they were set intentionally)

**Special cases in Sidebar.tsx (line 4):**
```typescript
// Before:
import { ChevronRight, ChevronDown, Settings, Plus, StickyNote, CheckSquare, Lightbulb, Flag, FileText, ArrowUp, ArrowDown, Pencil, Trash2, FolderPlus, EyeOff } from 'lucide-react'

// After:
import { CaretRight, CaretDown, Gear, Plus, Note, CheckSquare, Lightbulb, Flag, FileText, ArrowUp, ArrowDown, PencilSimple, Trash, FolderPlus, EyeSlash } from '@phosphor-icons/react'
```

Then update all JSX references: `<ChevronRight` -> `<CaretRight`, `<Settings` -> `<Gear`, etc.

**Special case in nav-items.ts:**
This file likely exports icon components that are referenced by name. Update all icon references.

**Special case in pipeline/types.ts and pipeline/__tests__/types.test.ts:**
These may import icons for type definitions or test data. Update both.

**3. After all migrations, verify zero lucide imports remain:**

```bash
grep -rn "from 'lucide-react'" frontend/src/ | grep -v node_modules | grep -v __tests__
```

This MUST return 0 results. Test files are exempted only if they import lucide for mock purposes, but ideally update those too.

**4. Remove lucide-react from dependencies (optional, after verification):**

Only remove if ALL imports including test files are migrated. If any test file still imports lucide-react, keep the dependency but add a comment in package.json.

**DO NOT:**
- Use path-specific imports (e.g., `@phosphor-icons/react/dist/ssr/Gear`) — use barrel imports for now; switch to path imports only if dev server HMR becomes noticeably slow (per Research Pitfall 6)
- Change icon sizes unless they're clearly wrong (e.g., using 48px for an inline button icon)
- Add `weight` prop where `regular` (the default) is appropriate — less noise in the code
- Break existing functionality — every icon should render visually similar to before
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && echo "Lucide imports remaining:" && grep -rn "from 'lucide-react'" src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | wc -l && echo "Phosphor imports:" && grep -rn "from '@phosphor-icons/react'" src/ --include='*.tsx' --include='*.ts' | wc -l && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from 'lucide-react'" frontend/src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | wc -l` returns 0
    - `grep -rn "from '@phosphor-icons/react'" frontend/src/ --include='*.tsx' --include='*.ts' | wc -l` returns >= 70 (most of the 78 files)
    - frontend/package.json contains `"@phosphor-icons/react"` in dependencies
    - Sidebar.tsx imports CaretRight, CaretDown, Gear, Plus, Note, etc. from @phosphor-icons/react (not lucide-react)
    - No `strokeWidth` props remain on icon components (grep for `strokeWidth` in icon usage contexts)
    - `cd frontend && npx vitest run` exits 0 (no test regressions)
    - `cd frontend && npx tsc --noEmit` exits 0 (no TypeScript errors from wrong icon names)
  </acceptance_criteria>
  <done>All 78 files migrated from lucide-react to @phosphor-icons/react. Zero lucide-react imports remain in non-test source files. All icons render with correct Phosphor equivalents. Tests pass. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and migrate all hardcoded color values in JSX/TS files to CSS variables</name>
  <files>All TSX/TS files with hardcoded colors (30+ files from Research findings)</files>
  <read_first>
    - frontend/src/globals.css (full file — understand ALL available CSS variables and their values so you can map hardcoded colors to the correct variables)
    - frontend/src/components/LayoutShell.tsx (has traffic light colors #ff5f57, #febc2e, #28c840 that are INTENTIONALLY hardcoded — do NOT change these)
    - .planning/phases/01-responsive-layout-visual-polish/01-RESEARCH.md (Pitfall 3: hardcoded colors that should stay hardcoded)
    - .planning/phases/01-responsive-layout-visual-polish/01-UI-SPEC.md (Color section, Color Audit Scope)
    - frontend/src/components/Sidebar.tsx (contains hardcoded colors for editing states)
    - frontend/src/components/NotificationCenter.tsx (notification type colors)
    - frontend/src/pages/messages/MessageThread.tsx (message bubble colors)
    - frontend/src/pages/messages/ConversationList.tsx (unread indicators)
  </read_first>
  <action>
**Systematic color audit across all TSX/TS files (excluding globals.css, themes.ts, test files, demo-data.ts).**

**Step 1: Scan for all hardcoded colors.**

Run these commands to find all hardcoded colors:
```bash
# Hex colors in TSX/TS (excluding exempted files)
grep -rn '#[0-9a-fA-F]\{3,8\}' frontend/src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | grep -v globals.css | grep -v themes.ts | grep -v demo-data.ts

# rgba colors in TSX/TS (excluding exempted files)
grep -rn 'rgba(' frontend/src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | grep -v globals.css | grep -v themes.ts | grep -v demo-data.ts
```

**Step 2: For each hardcoded color, categorize:**

**(a) Replace with existing CSS variable:**
Most hardcoded colors have a direct CSS variable equivalent. Example mappings:

| Found | Replace with |
|-------|-------------|
| `rgba(245, 158, 11, 0.1)` | `var(--warning-a08)` or create `var(--warning-a10)` |
| `rgba(245, 158, 11, 0.3)` | `var(--warning-a30)` |
| `rgba(245, 158, 11, 0.9)` | `var(--warning)` |
| `rgba(239, 68, 68, ...)` | `var(--red-500)` / `var(--red-500-aXX)` |
| `rgba(74, 222, 128, ...)` | `var(--green-400)` / `var(--green-400-aXX)` |
| `rgba(155, 132, 236, ...)` | `var(--purple-aXX)` |
| `rgba(167, 139, 250, ...)` | `var(--accent-aXX)` |
| `rgba(255, 255, 255, 0.05)` | `var(--hover-bg)` |
| `rgba(255, 255, 255, 0.08)` | `var(--hover-bg-bright)` |
| `rgba(255, 255, 255, 0.02)` | `var(--bg-white-02)` |
| `rgba(255, 255, 255, 0.03)` | `var(--bg-white-03)` |
| `rgba(255, 255, 255, 0.04)` | `var(--bg-white-04)` |
| `rgba(255, 255, 255, 0.06)` | `var(--border)` |
| `rgba(255, 255, 255, 0.1)` | `var(--border-hover)` |
| `rgba(0, 0, 0, 0.4)` | `var(--overlay-light)` |
| `rgba(0, 0, 0, 0.5)` | `var(--overlay)` |
| `rgba(0, 0, 0, 0.6)` | `var(--overlay-heavy)` |
| `#e4e4ec` | `var(--text-primary)` |
| `#8b8fa3` | `var(--text-muted)` |
| `#9898a8` | `var(--text-secondary)` |
| `#0a0a0c` | `var(--bg-base)` |
| `#16161c` | `var(--bg-card-solid)` |
| `#a78bfa` | `var(--accent)` |
| `#ef4444` | `var(--red-500)` |
| `#22c55e` | `var(--green-500)` |
| `#4ade80` | `var(--green-400)` |
| `#60a5fa` | `var(--blue)` |
| `#f59e0b` | `var(--amber)` |
| `#fb923c` | `var(--orange)` |

**(b) Create new CSS variable if no semantic match exists:**
If a hardcoded value has no matching CSS variable, create a new one in globals.css following the naming pattern `--{semantic}-{modifier}`. For example:
- A message bubble background might become `--bg-message-sent` / `--bg-message-received`
- A status indicator might become `--status-active` / `--status-idle`
- Add the new variable to both `:root` (dark) and `[data-theme="light"]` blocks

**(c) Intentionally keep hardcoded (document with comment):**
These colors are platform-specific constants, NOT theme tokens:
- `#ff5f57` (traffic light close — macOS) — add comment: `/* macOS traffic light — intentionally hardcoded */`
- `#febc2e` (traffic light minimize — macOS)
- `#28c840` (traffic light maximize — macOS)
- `rgba(0,0,0,0.6)` on traffic light icon spans (macOS native styling)
- Any skeleton gradient background values that use `var()` already (Skeleton.tsx)

**Step 3: Apply changes file by file.**

Work through each file systematically. For each file:
1. Read the file
2. Identify all hardcoded color values
3. Map each to the correct CSS variable
4. Make all replacements
5. Verify the file still compiles

**Priority files (highest hardcoded color concentration per Research):**
1. `components/Sidebar.tsx` — traffic light colors (keep), editing states (migrate)
2. `components/LayoutShell.tsx` — title bar bg, offline banner (migrate non-traffic-light colors)
3. `components/NotificationCenter.tsx` — notification type colors (migrate)
4. `pages/messages/MessageThread.tsx` — message bubble colors (migrate)
5. `pages/messages/ConversationList.tsx` — unread indicators (migrate)
6. `pages/dashboard/*.tsx` — status colors, chart colors (migrate)
7. `pages/pipeline/*.tsx` — status badge colors (migrate)
8. `pages/missions/*.tsx` — event type colors (migrate)

**Step 4: Final verification scan.**

After all files are migrated, run the scan again to confirm zero remaining:
```bash
grep -rn '#[0-9a-fA-F]\{3,8\}' frontend/src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | grep -v globals.css | grep -v themes.ts | grep -v demo-data.ts | grep -v '\/\* macOS traffic light'
```

Any remaining results that are NOT traffic light colors or exempted files indicate missed migrations.

**DO NOT:**
- Change colors in globals.css (those ARE the variables)
- Change colors in themes.ts (those define accent presets — the raw values are the data)
- Change colors in test files (they're exempted)
- Change traffic light colors (#ff5f57, #febc2e, #28c840) — these are intentional
- Alter the visual appearance of any component — the CSS variables map to the same actual colors. This is a refactor, not a redesign.
- Use `color-mix()` or `hsl()` or computed colors — use the existing pre-defined CSS variables
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && echo "Remaining hex in TSX:" && grep -rn '#[0-9a-fA-F]\{3,8\}' src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | grep -v globals.css | grep -v themes.ts | grep -v demo-data.ts | grep -v 'macOS traffic light' | grep -v 'intentionally hardcoded' | wc -l && echo "Remaining rgba in TSX:" && grep -rn 'rgba(' src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | grep -v globals.css | grep -v themes.ts | grep -v demo-data.ts | grep -v 'macOS traffic light' | grep -v 'intentionally hardcoded' | wc -l && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Hex color scan returns 0 results (excluding exempted files and intentionally hardcoded comments)
    - rgba color scan returns 0 results (excluding exempted files and intentionally hardcoded comments)
    - Traffic light colors (#ff5f57, #febc2e, #28c840) in LayoutShell.tsx have `/* macOS traffic light — intentionally hardcoded */` comments
    - Any new CSS variables added to globals.css follow `--{semantic}-{modifier}` naming pattern
    - Any new CSS variables added to `:root` also have corresponding `[data-theme="light"]` overrides
    - `cd frontend && npx vitest run` exits 0 (visual appearance unchanged, just refactored to use variables)
    - `cd frontend && npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>All hardcoded hex and rgba color values in TSX/TS files replaced with CSS variables. Traffic light colors documented as intentionally hardcoded. Zero remaining inline color literals (verified by grep scan). Tests pass. App looks visually identical — this is a refactor only.</done>
</task>

</tasks>

<verification>
1. Icon migration complete:
   - `grep -rn "from 'lucide-react'" frontend/src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | wc -l` returns 0
   - `grep -c '@phosphor-icons/react' frontend/package.json` returns >= 1

2. Color audit complete:
   - `grep -rn '#[0-9a-fA-F]\{3,8\}' frontend/src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | grep -v globals.css | grep -v themes.ts | grep -v demo-data.ts | grep -v 'intentionally hardcoded' | wc -l` returns 0
   - `grep -rn 'rgba(' frontend/src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__ | grep -v globals.css | grep -v themes.ts | grep -v demo-data.ts | grep -v 'intentionally hardcoded' | wc -l` returns 0

3. All tests pass: `cd frontend && npx vitest run` exits 0
4. TypeScript clean: `cd frontend && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- @phosphor-icons/react installed and all 78 files migrated from lucide-react
- Zero lucide-react imports remain in production source files
- Zero hardcoded hex/rgba color values remain in TSX/TS files (except documented intentional exemptions)
- Traffic light colors (#ff5f57, #febc2e, #28c840) documented with comments as intentionally hardcoded
- All icons use Phosphor with consistent weights (regular default, bold for active, fill for filled)
- App looks visually identical before and after (refactor, not redesign)
- All tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-responsive-layout-visual-polish/01-03-SUMMARY.md`
</output>
