---
phase: 03-setup-wizard-onboarding
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - frontend/src/components/wizard/WizardTailscale.tsx
  - frontend/src/components/wizard/WizardSupabase.tsx
  - frontend/src/components/wizard/WizardOpenClaw.tsx
  - frontend/src/components/wizard/WizardMacServices.tsx
  - frontend/src/components/wizard/WizardServerServices.tsx
autonomous: true
requirements:
  - WIZARD-02
  - WIZARD-07

must_haves:
  truths:
    - "Tailscale step auto-detects Tailscale status via Tauri IPC command"
    - "Supabase step accepts URL + anon key and tests connection via backend endpoint"
    - "OpenClaw step accepts URL + API key and tests connection via backend endpoint"
    - "Mac Services step groups BlueBubbles + Mac Bridge as collapsible cards"
    - "Server Services step shows CouchDB configuration"
    - "Required steps (Tailscale, Supabase, OpenClaw) block Next button until test passes"
    - "Optional steps (Mac Services, Server Services) allow skip"
    - "Secret fields have show/hide toggle with Eye/EyeSlash icons"
    - "Error messages include specific fix suggestions per service"
  artifacts:
    - path: "frontend/src/components/wizard/WizardTailscale.tsx"
      provides: "Tailscale connectivity check step"
    - path: "frontend/src/components/wizard/WizardSupabase.tsx"
      provides: "Supabase URL + anon key connection step"
    - path: "frontend/src/components/wizard/WizardOpenClaw.tsx"
      provides: "OpenClaw URL + API key connection step"
    - path: "frontend/src/components/wizard/WizardMacServices.tsx"
      provides: "BlueBubbles + Mac Bridge combined step"
    - path: "frontend/src/components/wizard/WizardServerServices.tsx"
      provides: "CouchDB configuration step"
  key_links:
    - from: "frontend/src/components/wizard/WizardTailscale.tsx"
      to: "@tauri-apps/api/core"
      via: "invoke('check_tailscale')"
      pattern: "invoke.*check_tailscale"
    - from: "frontend/src/components/wizard/WizardSupabase.tsx"
      to: "frontend/src/components/wizard/WizardConnectionTest.tsx"
      via: "Shared connection test component"
      pattern: "WizardConnectionTest"
    - from: "All service steps"
      to: "frontend/src/lib/wizard-store.ts"
      via: "updateWizardField for credential storage"
      pattern: "updateWizardField"
---

<objective>
Build all 5 service connection step components: Tailscale detection, Supabase, OpenClaw (required), Mac Services, and Server Services (optional). Each step uses the shared WizardConnectionTest and WizardGuidePanel from Plan 03.

Purpose: Service connections are the core value of the wizard -- users configure their infrastructure here. Without these steps, the wizard cannot onboard users to the app's services.

Output: 5 fully functional service step components that test connections, store credentials in wizard state, and provide setup guidance.
</objective>

<execution_context>
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-setup-wizard-onboarding/03-CONTEXT.md
@.planning/phases/03-setup-wizard-onboarding/03-UI-SPEC.md
@.planning/phases/03-setup-wizard-onboarding/03-01-SUMMARY.md
@.planning/phases/03-setup-wizard-onboarding/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From frontend/src/lib/wizard-store.ts:
```typescript
export function useWizardState(): WizardState
export function updateWizardField(field: string, value: any): void
export function updateTestResult(service: string, result: TestResult): void
export function markStepCompleted(step: number): void
export function markStepStatus(step: number, status: StepStatus): void
export const STEP_NAMES: string[]
```

<!-- From Plan 02 outputs -->
Backend endpoint: POST /api/wizard/test-connection
Body: { service: string, url: string, key?: string, password?: string, username?: string }
Response: { status: "ok", latency_ms: number } | { status: "error", error: string }

Tauri IPC: invoke('check_tailscale')
Returns: { connected: boolean, self_ip: string | null, peer_count: number, error: string | null }

<!-- From Plan 03 outputs -->
From frontend/src/components/wizard/WizardConnectionTest.tsx:
```typescript
// Props: { service: string; url: string; credentials: Record<string, string>; onSuccess?: (latencyMs: number) => void; onError?: (error: string) => void }
```

From frontend/src/components/wizard/WizardGuidePanel.tsx:
```typescript
// Props: { title?: string; children: ReactNode }
// Expandable panel with slide animation
```

From frontend/src/components/ui/Button.tsx:
```typescript
// 4 variants: primary, secondary, ghost, danger
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Required service steps -- Tailscale, Supabase, OpenClaw</name>
  <files>frontend/src/components/wizard/WizardTailscale.tsx, frontend/src/components/wizard/WizardSupabase.tsx, frontend/src/components/wizard/WizardOpenClaw.tsx</files>
  <read_first>
    - .planning/phases/03-setup-wizard-onboarding/03-UI-SPEC.md (Service Steps section, all 3 layouts, error message patterns, connection test behavior)
    - .planning/phases/03-setup-wizard-onboarding/03-RESEARCH.md (Pattern 2, Example 3, Pitfall 1, Pitfall 5, Pitfall 6)
    - frontend/src/pages/settings/SettingsConnections.tsx (existing connection UI patterns for reference -- field names, labels)
    - frontend/src/components/wizard/WizardConnectionTest.tsx (Plan 03 shared component)
    - frontend/src/components/wizard/WizardGuidePanel.tsx (Plan 03 shared component)
    - frontend/src/lib/wizard-store.ts (updateWizardField, useWizardState, markStepCompleted, markStepStatus)
    - src-tauri/src/secrets.rs (keychain key names: supabase.url, supabase.anon-key, openclaw.api-url, openclaw.api-key)
  </read_first>
  <action>
**Create `frontend/src/components/wizard/WizardTailscale.tsx`:**

- Heading: "Connect Tailscale" (20px, weight 600)
- Description: "Tailscale provides the secure network that connects all your services. It must be running on this machine." (15px, var(--text-secondary))
- WizardGuidePanel with steps:
  1. Install Tailscale on this machine
  2. Run `tailscale up` to connect
  3. Verify with `tailscale status`
  (Use `<code>` elements with var(--bg-card-solid) bg and var(--radius-sm) for inline code)
- Auto-detect on mount: If `window.__TAURI_INTERNALS__` exists, call `invoke('check_tailscale')`:
  - If connected=true: show self_ip in a read-only input field, auto-fill, mark step as success
  - If connected=false: show error message from TailscaleCheck.error
- Manual fallback for browser mode: Show text input for Tailscale IP with label "Tailscale IP". No auto-detect in browser mode.
- WizardConnectionTest not used here (Tailscale is detected locally, not tested via HTTP). Instead, show status inline:
  - Connected: CheckCircle icon + "Tailscale connected ({peer_count} peers)" in var(--green)
  - Not connected: XCircle icon + error message in var(--red-500) + instructions
- On success: call `markStepCompleted(1)`, `markStepStatus(1, 'success')`
- Input fields use monospace font (JetBrains Mono at 13px) for the IP address

**Create `frontend/src/components/wizard/WizardSupabase.tsx`:**

- Heading: "Connect Supabase" (20px, weight 600)
- Description: "Your self-hosted PostgreSQL database and authentication backend." (15px, var(--text-secondary))
- WizardGuidePanel with numbered setup steps for self-hosted Supabase
- Two input fields:
  1. "Supabase URL" -- text input, placeholder "https://supabase.example.com"
  2. "Anon Key" -- password input with Eye/EyeSlash toggle button, placeholder "eyJhbGciOiJI..."
- Each input:
  - `<label htmlFor="wizard-supabase-url">Supabase URL</label>`
  - Input with `id`, value from wizardState.supabaseUrl, onChange calls updateWizardField
  - Monospace font for URL/key fields
- WizardConnectionTest below inputs:
  ```tsx
  <WizardConnectionTest
    service="supabase"
    url={wizardState.supabaseUrl}
    credentials={{ key: wizardState.supabaseAnonKey }}
    onSuccess={(latencyMs) => { markStepCompleted(2); markStepStatus(2, 'success') }}
    onError={() => markStepStatus(2, 'error')}
  />
  ```
- Secret field masking: Show/Hide toggle is a ghost `<button>` with `aria-label="Show anon key"` / `aria-label="Hide anon key"`, Eye/EyeSlash Phosphor icon at the right end of the input

**Create `frontend/src/components/wizard/WizardOpenClaw.tsx`:**

- Heading: "Connect OpenClaw"
- Description: "The AI gateway that powers Chat, Agents, and Mission Control's intelligence."
- WizardGuidePanel with OpenClaw setup steps
- Two input fields:
  1. "OpenClaw URL" -- text input, placeholder "https://openclaw.example.com"
  2. "API Key" -- password input with Eye/EyeSlash toggle
- WizardConnectionTest with service="openclaw", credentials={{ key: wizardState.openclawApiKey }}
- On success: markStepCompleted(3), markStepStatus(3, 'success')

**Shared patterns for all three:**
- All use var(--space-4) gap between form fields
- All inputs have 1px solid var(--border), var(--radius-md) border-radius, var(--bg-card-solid) background
- All use `<label>` elements with htmlFor (not aria-label on inputs)
- Field values read from/write to wizard state via useWizardState() and updateWizardField()
- URL inputs have type="url", key/password inputs have type="password" (with toggle)
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/components/wizard/WizardTailscale.tsx
    - test -f frontend/src/components/wizard/WizardSupabase.tsx
    - test -f frontend/src/components/wizard/WizardOpenClaw.tsx
    - grep "Connect Tailscale" frontend/src/components/wizard/WizardTailscale.tsx returns a match
    - grep "check_tailscale" frontend/src/components/wizard/WizardTailscale.tsx returns a match
    - grep "Connect Supabase" frontend/src/components/wizard/WizardSupabase.tsx returns a match
    - grep "WizardConnectionTest" frontend/src/components/wizard/WizardSupabase.tsx returns a match
    - grep "Connect OpenClaw" frontend/src/components/wizard/WizardOpenClaw.tsx returns a match
    - grep "WizardConnectionTest" frontend/src/components/wizard/WizardOpenClaw.tsx returns a match
    - grep -c 'aria-label.*password\|aria-label.*key\|aria-label.*Show\|aria-label.*Hide' frontend/src/components/wizard/WizardSupabase.tsx returns >= 1
    - grep -c "htmlFor\|<label" frontend/src/components/wizard/WizardSupabase.tsx returns >= 2
  </acceptance_criteria>
  <done>Three required service steps render with correct copy, guide panels, connection testing, secret field masking, and proper ARIA. Tailscale auto-detects via Tauri IPC. Supabase and OpenClaw test via backend endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Optional service steps -- Mac Services and Server Services</name>
  <files>frontend/src/components/wizard/WizardMacServices.tsx, frontend/src/components/wizard/WizardServerServices.tsx</files>
  <read_first>
    - .planning/phases/03-setup-wizard-onboarding/03-UI-SPEC.md (Mac Services Step, Server Services Step sections)
    - .planning/phases/03-setup-wizard-onboarding/03-CONTEXT.md (Mac Services and Server Services decisions)
    - frontend/src/components/wizard/WizardConnectionTest.tsx (shared test component)
    - frontend/src/components/wizard/WizardGuidePanel.tsx (shared guide panel)
    - frontend/src/lib/wizard-store.ts (updateWizardField, markStepCompleted, markStepStatus)
    - src-tauri/src/secrets.rs (bluebubbles.host, bluebubbles.password, mac-bridge.host, mac-bridge.api-key, couchdb.url, couchdb.user, couchdb.password)
  </read_first>
  <action>
**Create `frontend/src/components/wizard/WizardMacServices.tsx`:**

- Heading: "Mac Services" (20px, weight 600)
- Description: "These services require a Mac running on your Tailscale network." (15px, var(--text-secondary))
- Platform banner: On non-macOS systems (detect via navigator.userAgent or navigator.platform), show info banner at top: "These services require a Mac with BlueBubbles and Mac Bridge running on your Tailscale network." in Caption (12px), var(--text-muted), with Info icon from Phosphor. Use `1px solid var(--border)` background var(--bg-elevated), padding var(--space-2).

- Two collapsible service cards (both expanded by default):

  **BlueBubbles card:**
  - Card header: "BlueBubbles (iMessage)" with toggle to collapse
  - Fields:
    - URL: text input, placeholder "http://100.x.x.x:1234"
    - Password: password input with Eye/EyeSlash toggle
  - WizardConnectionTest with service="bluebubbles", credentials={{ password: wizardState.blueBubblesPassword }}
  - On success: call markStepStatus(4, 'success') if both services pass or at least one configured

  **Mac Bridge card:**
  - Card header: "Mac Bridge (Reminders, Notes, Contacts)" with toggle to collapse
  - Fields:
    - URL: text input, placeholder "http://100.x.x.x:4100"
    - API Key: password input with Eye/EyeSlash toggle
  - WizardConnectionTest with service="mac-bridge", credentials={{ key: wizardState.macBridgeApiKey }}

- Card styling per UI-SPEC:
  - Background: var(--bg-card-solid), 1px solid var(--border), var(--radius-lg) border-radius
  - Internal padding: var(--space-4)
  - Collapsible via chevron icon + header click

- This is an optional step: markStepCompleted(4) is called on Next regardless of test results. If user configures nothing, markStepStatus(4, 'skipped').

**Create `frontend/src/components/wizard/WizardServerServices.tsx`:**

- Heading: "Server Services" (20px, weight 600)
- Description: "Optional services running on your infrastructure." (15px, var(--text-secondary))

- Single service card:

  **CouchDB card:**
  - Card header: "CouchDB (Obsidian Notes Sync)"
  - Fields:
    - URL: text input, placeholder "http://100.x.x.x:5984"
    - Username: text input
    - Password: password input with Eye/EyeSlash toggle
  - WizardConnectionTest with service="couchdb", credentials={{ username: wizardState.couchdbUsername, password: wizardState.couchdbPassword }}

- Card styling same as Mac Services cards.
- Optional step: markStepCompleted(5) on Next regardless. If unconfigured, markStepStatus(5, 'skipped').

**Shared patterns:**
- All cards use `<label>` elements, proper htmlFor/id attributes
- All password fields have show/hide toggle
- All use monospace font for URL/credential inputs
- Both steps are fully skippable -- Next button is always enabled
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/components/wizard/WizardMacServices.tsx
    - test -f frontend/src/components/wizard/WizardServerServices.tsx
    - grep "Mac Services" frontend/src/components/wizard/WizardMacServices.tsx returns a match
    - grep "BlueBubbles" frontend/src/components/wizard/WizardMacServices.tsx returns a match
    - grep "Mac Bridge" frontend/src/components/wizard/WizardMacServices.tsx returns a match
    - grep "Server Services" frontend/src/components/wizard/WizardServerServices.tsx returns a match
    - grep "CouchDB" frontend/src/components/wizard/WizardServerServices.tsx returns a match
    - grep "WizardConnectionTest" frontend/src/components/wizard/WizardMacServices.tsx returns a match
    - grep "WizardConnectionTest" frontend/src/components/wizard/WizardServerServices.tsx returns a match
    - Existing test suite passes
  </acceptance_criteria>
  <done>Mac Services step shows BlueBubbles + Mac Bridge as collapsible cards with platform warning. Server Services step shows CouchDB. Both are skippable with proper field layouts and connection testing.</done>
</task>

</tasks>

<verification>
```bash
cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose
ls frontend/src/components/wizard/Wizard*.tsx | wc -l  # Should be 7+ files
```
</verification>

<success_criteria>
- All 5 service step components exist and render
- Tailscale step auto-detects via check_tailscale Tauri command
- Supabase and OpenClaw steps use WizardConnectionTest with correct service/credential props
- Mac Services groups BlueBubbles + Mac Bridge in collapsible cards
- Server Services shows CouchDB in a card
- All inputs have proper labels, secret fields have show/hide toggle
- Required steps (1,2,3) block Next until test passes; optional steps (4,5) are always skippable
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-wizard-onboarding/03-04-SUMMARY.md`
</output>
