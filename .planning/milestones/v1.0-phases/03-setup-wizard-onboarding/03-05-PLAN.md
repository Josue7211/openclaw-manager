---
phase: 03-setup-wizard-onboarding
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - frontend/src/components/wizard/WizardModules.tsx
  - frontend/src/components/wizard/WizardTheme.tsx
  - frontend/src/components/wizard/WizardSummary.tsx
  - frontend/src/components/SetupWizard.tsx
  - frontend/src/components/DemoModeBanner.tsx
  - frontend/src/pages/settings/SettingsConnections.tsx
autonomous: true
requirements:
  - WIZARD-03
  - WIZARD-04
  - WIZARD-05
  - WIZARD-06

must_haves:
  truths:
    - "User can select from 3 preset bundles (Essentials, Full, Minimal) that set module toggles"
    - "User can individually toggle modules after selecting a preset"
    - "Unavailable modules (requiring unconfigured services) are dimmed with opacity 0.45 and disabled toggle"
    - "User can pick from 8 theme presets with live WYSIWYG preview"
    - "Light/Dark/System mode selector above theme grid"
    - "Summary screen shows recap of configured services, modules, and theme"
    - "Confetti celebration fires on summary screen (full animation intensity)"
    - "Completing wizard saves credentials to OS keychain and clears wizard-state from localStorage"
    - "Demo mode sets localStorage key and navigates to dashboard with demo data"
    - "Re-run Walkthrough button exists in Settings Connections"
  artifacts:
    - path: "frontend/src/components/wizard/WizardModules.tsx"
      provides: "Module selection with preset bundles and card grid"
    - path: "frontend/src/components/wizard/WizardTheme.tsx"
      provides: "Simplified 8-preset theme picker with mode selector"
    - path: "frontend/src/components/wizard/WizardSummary.tsx"
      provides: "Configuration recap with confetti celebration"
  key_links:
    - from: "frontend/src/components/wizard/WizardModules.tsx"
      to: "frontend/src/lib/modules.ts"
      via: "APP_MODULES for module list, setEnabledModules for final save"
      pattern: "APP_MODULES|setEnabledModules"
    - from: "frontend/src/components/wizard/WizardTheme.tsx"
      to: "frontend/src/lib/theme-store.ts"
      via: "setActiveTheme and setMode for WYSIWYG preview"
      pattern: "setActiveTheme|setMode"
    - from: "frontend/src/components/wizard/WizardSummary.tsx"
      to: "canvas-confetti"
      via: "confetti() call on mount"
      pattern: "import confetti|confetti\\("
    - from: "frontend/src/components/SetupWizard.tsx"
      to: "frontend/src/lib/wizard-store.ts"
      via: "completeWizard() on Launch Dashboard click"
      pattern: "completeWizard"
---

<objective>
Build the module selection step (preset bundles + card grid), theme selection step (8 presets + mode selector), and summary screen (recap + confetti). Wire the completion flow: save credentials to keychain, clear wizard state, enable demo mode, and add Settings integration.

Purpose: These are the personalization and completion steps. Module selection determines which sidebar items are visible. Theme selection gives immediate visual customization. The summary/completion flow is the bridge from wizard to live app, including credential persistence to OS keychain.

Output: Complete wizard flow from module selection through theme to summary, with working completion that saves credentials and transitions to the dashboard.
</objective>

<execution_context>
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-setup-wizard-onboarding/03-CONTEXT.md
@.planning/phases/03-setup-wizard-onboarding/03-UI-SPEC.md
@.planning/phases/03-setup-wizard-onboarding/03-03-SUMMARY.md
@.planning/phases/03-setup-wizard-onboarding/03-04-SUMMARY.md

<interfaces>
From frontend/src/lib/wizard-store.ts:
```typescript
export function useWizardState(): WizardState
export function updateWizardField(field: string, value: any): void
export function markStepCompleted(step: number): void
export function completeWizard(): void  // Deletes wizard-state, sets setup-complete
export function resetWizard(): void
export function activateDemoMode(): void
export function deactivateDemoMode(): void
export function isWizardDemoMode(): boolean
export const PRESET_BUNDLES: Record<string, string[]>
// essentials: ['chat', 'todos', 'calendar', 'dashboard', 'notes']
// full: all 17 module IDs
// minimal: ['dashboard', 'chat']
```

From frontend/src/lib/modules.ts:
```typescript
export const APP_MODULES: AppModule[]  // 17 modules with id, name, description, icon, route, requiresConfig, platform
export function setEnabledModules(ids: string[]): void
export function getEnabledModules(): string[]
```

From frontend/src/lib/theme-store.ts:
```typescript
export function setActiveTheme(themeId: string, clickEvent?: { clientX: number; clientY: number }): void
export function setMode(mode: 'dark' | 'light' | 'system'): void
export function getThemeState(): ThemeState
```

From frontend/src/lib/theme-definitions.ts:
```typescript
export const BUILT_IN_THEMES: readonly ThemeDefinition[]
export interface ThemeDefinition {
  id: string; name: string; category: string; builtIn: boolean;
  artwork?: string; colors: Record<string, string>;
}
```

From frontend/src/lib/nav-items.ts:
```typescript
export const personalDashboardItems: NavItem[]  // 11 items
export const agentDashboardItems: NavItem[]  // 7 items
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WizardModules step with preset bundles and card grid</name>
  <files>frontend/src/components/wizard/WizardModules.tsx</files>
  <read_first>
    - .planning/phases/03-setup-wizard-onboarding/03-UI-SPEC.md (Module Selection Step section -- full layout, card anatomy, preset bundles, categories, grid layout, unavailable dimming)
    - frontend/src/lib/modules.ts (APP_MODULES array, AppModule interface)
    - frontend/src/lib/wizard-store.ts (PRESET_BUNDLES, useWizardState, updateWizardField)
    - frontend/src/lib/nav-items.ts (personalDashboardItems, agentDashboardItems -- for categorization)
    - frontend/src/pages/settings/Toggle.tsx (existing Toggle component pattern for module toggles)
  </read_first>
  <action>
Create `frontend/src/components/wizard/WizardModules.tsx`:

- Heading: "Choose Your Modules" (20px, weight 600)
- Description: "Start with a preset, then customize. You can change this anytime in Settings." (15px, var(--text-secondary))

**Preset bundle pills** (horizontal row at top):
- Three pills: "Essentials", "Full Setup", "Minimal"
- Each is a `<button>` with `role="radio"` inside a `<div role="radiogroup" aria-label="Module presets">`
- Active pill: var(--accent) bg, var(--text-on-accent) text, weight 600, --radius-full
- Inactive pill: var(--bg-elevated) bg, 1px solid var(--border), var(--text-secondary) text, weight 400
- Hover: var(--hover-bg-bright) on inactive pills
- Clicking a preset:
  1. Sets wizardState.activeBundle to the preset name
  2. Sets wizardState.enabledModules to PRESET_BUNDLES[preset]
  3. All module toggles update to match

**Module card grid** (grouped by category):
- Grid: `grid-template-columns: repeat(auto-fill, minmax(200px, 1fr))`, gap var(--space-4)
- Categories with section headers (uppercase label, 12px, weight 600, letter-spacing 0.1em, var(--text-muted)):
  - "Personal": messages, chat, todos, calendar, reminders, email, pomodoro, notes
  - "Infrastructure": dashboard, homelab, media
  - "Agents": missions, agents, memory, crons, pipeline, knowledge

**Module card anatomy:**
- Background: var(--bg-card-solid), 1px solid var(--border), --radius-lg (12px)
- Internal padding: var(--space-4)
- Layout: Icon (24px Phosphor) + Module Name (15px, weight 600) + Toggle on same row. Description (12px, var(--text-muted)) below.
- Hover: var(--bg-card-hover), translateY(-1px), medium shadow -- 200ms, var(--ease-out)
- The card is a `<label>` wrapping the toggle input for clickable area
- Toggle: existing Toggle component pattern (role="switch", aria-checked)
- Module icon: Map APP_MODULES icon string to Phosphor icon components. Use the icons from nav-items.ts for each moduleId.

**Unavailable modules** (requiring unconfigured services):
- Determine availability: If wizardState test for the required service is not 'success', the module is unavailable
  - Messages requires bluebubbles (test service "bluebubbles")
  - Reminders requires mac-bridge
  - Notes requires couchdb (optional -- mark unavailable only if couchdb step was not skipped AND test failed)
  - Home Lab requires proxmox (always show as available since proxmox config is in Settings, not wizard)
- Card: opacity 0.45
- Toggle: disabled (opacity 0.3, pointer-events none)
- Label below description: "Requires {service}" in Caption (12px), var(--text-muted), italic
- Toggle has `aria-disabled="true"` with reason in `aria-describedby`

**Deselecting active bundle:**
- If user toggles any individual module after selecting a preset, set wizardState.activeBundle to null (deselects the pill)

- On leaving step: save wizardState.enabledModules (already in wizard state). The actual `setEnabledModules()` call happens on wizard completion, not here.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/components/wizard/WizardModules.tsx
    - grep "Choose Your Modules" frontend/src/components/wizard/WizardModules.tsx returns a match
    - grep 'role="radiogroup"' frontend/src/components/wizard/WizardModules.tsx returns a match
    - grep "Essentials" frontend/src/components/wizard/WizardModules.tsx returns a match
    - grep "Full Setup" frontend/src/components/wizard/WizardModules.tsx returns a match
    - grep "Minimal" frontend/src/components/wizard/WizardModules.tsx returns a match
    - grep "auto-fill.*minmax.*200px" frontend/src/components/wizard/WizardModules.tsx returns a match
    - grep -c 'role="switch"\|role="radio"' frontend/src/components/wizard/WizardModules.tsx returns >= 2
    - grep "opacity.*0.45\|opacity: 0.45" frontend/src/components/wizard/WizardModules.tsx returns a match
  </acceptance_criteria>
  <done>WizardModules renders preset bundle pills as radiogroup, module card grid grouped by 3 categories, toggle switches, unavailable dimming at 0.45 opacity, and deselects bundle on individual toggle.</done>
</task>

<task type="auto">
  <name>Task 2: WizardTheme step with 8-preset grid and mode selector</name>
  <files>frontend/src/components/wizard/WizardTheme.tsx</files>
  <read_first>
    - .planning/phases/03-setup-wizard-onboarding/03-UI-SPEC.md (Theme Selection Step section -- 8 presets, card anatomy, mode selector, swatch circles, mini UI mockup)
    - frontend/src/lib/theme-definitions.ts (BUILT_IN_THEMES array, ThemeDefinition interface)
    - frontend/src/lib/theme-store.ts (setActiveTheme, setMode)
    - frontend/src/lib/wizard-store.ts (useWizardState, updateWizardField for selectedThemeId, selectedMode)
    - frontend/src/components/ui/ThemePicker.tsx (reference for theme card rendering patterns -- DO NOT copy wholesale, simplified version for wizard)
    - .planning/phases/03-setup-wizard-onboarding/03-RESEARCH.md (Pitfall 7: WYSIWYG theme preview)
  </read_first>
  <action>
Create `frontend/src/components/wizard/WizardTheme.tsx`:

- Heading: "Pick a Theme" (20px, weight 600)
- Description: "You can customize this further in Settings." (15px, var(--text-secondary))

**Mode selector** (segmented control, above preset grid):
- Three segments: Moon icon + "Dark", Sun icon + "Light", Laptop icon + "System"
- Container: var(--radius-md), 1px solid var(--border), var(--bg-elevated) bg
- Active segment: var(--accent) bg, var(--text-on-accent) text, weight 600
- Inactive: transparent bg, var(--text-secondary), weight 400
- Implementation: `<div role="radiogroup" aria-label="Theme mode">` with 3 `<button role="radio" aria-checked="...">`
- Clicking changes wizardState.selectedMode AND calls `setMode(mode)` from theme-store for immediate effect
- Phosphor icons: Moon (dark), Sun (light), Laptop (system)

**8-preset grid** (4 columns):
- WIZARD_PRESETS constant: Filter BUILT_IN_THEMES to these 8 IDs:
  1. 'default-dark'
  2. 'dracula'
  3. 'nord'
  4. 'catppuccin-mocha'
  5. 'default-light'
  6. 'solarized-light'
  7. 'catppuccin-latte'
  8. 'rose-pine'
  If any ID doesn't match, fall back to the first 8 BUILT_IN_THEMES.

- Theme card anatomy per UI-SPEC:
  - Width: `min(160px, calc(25% - 12px))` -- 4 columns
  - Border radius: --radius-lg (12px)
  - Selected: 2px solid var(--accent), subtle glow (0 0 12px rgba(var(--accent-rgb), 0.2))
  - Unselected: 1px solid var(--border)
  - Hover: var(--bg-card-hover), translateY(-2px), medium shadow

  - Mini UI mockup (80px height): A tiny abstract representation rendered using the theme's actual color values. Draw with CSS:
    - Background: theme.colors['bg-base'] or theme.colors.background
    - Left bar (sidebar): theme.colors['bg-sidebar'] or theme.colors['bg-card'] at 30% width
    - Main area: theme.colors['bg-base']
    - Small card rectangles: theme.colors['bg-card-solid'] or theme.colors['bg-card']
    - Accent dot: theme.colors.accent
    All rendered as divs with absolute positioning inside a relative container.

  - Theme name (15px, weight 500, centered)
  - 5 color swatches (8px circles): bg-base, bg-card-solid, accent, text-primary, green -- extracted from theme.colors

- **Live WYSIWYG preview**: Clicking a theme card:
  1. Updates wizardState.selectedThemeId
  2. Calls `setActiveTheme(themeId)` from theme-store -- this applies CSS variables globally, making the wizard itself change appearance (per CONTEXT.md WYSIWYG requirement)

- Container: `<div role="radiogroup" aria-label="Theme presets">`, each card `<button role="radio" aria-checked="...">`
- Arrow key navigation within radiogroup (left/right to move, enter to select)
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/components/wizard/WizardTheme.tsx
    - grep "Pick a Theme" frontend/src/components/wizard/WizardTheme.tsx returns a match
    - grep "setActiveTheme" frontend/src/components/wizard/WizardTheme.tsx returns a match
    - grep "setMode" frontend/src/components/wizard/WizardTheme.tsx returns a match
    - grep -c 'role="radiogroup"' frontend/src/components/wizard/WizardTheme.tsx returns >= 2
    - grep "default-dark\|dracula\|nord\|catppuccin-mocha" frontend/src/components/wizard/WizardTheme.tsx returns a match
    - grep "solarized-light\|catppuccin-latte\|rose-pine" frontend/src/components/wizard/WizardTheme.tsx returns a match
  </acceptance_criteria>
  <done>WizardTheme renders mode selector (Dark/Light/System) and 8 preset cards with mini mockups, swatches, and WYSIWYG live preview via setActiveTheme. Arrow key navigation works within radiogroups.</done>
</task>

<task type="auto">
  <name>Task 3: WizardSummary + completion flow + demo mode + Settings integration</name>
  <files>frontend/src/components/wizard/WizardSummary.tsx, frontend/src/components/SetupWizard.tsx, frontend/src/components/DemoModeBanner.tsx, frontend/src/pages/settings/SettingsConnections.tsx</files>
  <read_first>
    - .planning/phases/03-setup-wizard-onboarding/03-UI-SPEC.md (Summary Screen section, Celebration Animation, Dashboard Transition, Demo Mode section)
    - .planning/phases/03-setup-wizard-onboarding/03-RESEARCH.md (Example 4: canvas-confetti, Pitfall 2: credentials cleanup, Pitfall 3: demo mode, Open Question 3: reload-secrets)
    - frontend/src/lib/wizard-store.ts (completeWizard, resetWizard, activateDemoMode, deactivateDemoMode, isWizardDemoMode)
    - frontend/src/lib/modules.ts (setEnabledModules)
    - frontend/src/lib/api.ts (api.post for save-credentials and reload-secrets endpoints)
    - frontend/src/components/DemoModeBanner.tsx (existing demo banner to extend)
    - frontend/src/pages/settings/SettingsConnections.tsx (existing re-run setup button to update)
    - frontend/src/components/SetupWizard.tsx (Plan 03 output -- wire in real step components replacing placeholders)
  </read_first>
  <action>
**Create `frontend/src/components/wizard/WizardSummary.tsx`:**

- Heading: "You're all set!" (24px, weight 600) with scale entrance animation: scale(0.9)->scale(1) over 500ms, var(--ease-spring). If reduced motion, no scale.
- Description: "Here's what we configured:" (15px, var(--text-secondary))

- Three summary cards (var(--bg-card-solid), 1px solid var(--border), --radius-lg, padding var(--space-4), gap var(--space-4)):

  1. **Services card:**
     - Header: "Services"
     - List of 6 services (Tailscale, Supabase, OpenClaw, BlueBubbles, Mac Bridge, CouchDB)
     - Each row: icon + service name + status text
     - Connected: CheckCircle (fill), var(--green), "Connected ({latencyMs}ms)"
     - Skipped: MinusCircle (regular), var(--text-muted), "Skipped"
     - Failed: XCircle (fill), var(--red-500), "Failed"
     - Container: `role="list"`, each row `role="listitem"`, icons have `aria-label`

  2. **Modules card:**
     - Header: "Modules"
     - "{N} modules enabled" count
     - Comma-separated list of enabled module names (truncated if > 6, showing "...and N more")

  3. **Theme card:**
     - Header: "Theme"
     - Theme name + mode: "Catppuccin Mocha (Dark mode)"

- **Confetti celebration**: On mount (useEffect), fire canvas-confetti:
  ```typescript
  import confetti from 'canvas-confetti'

  useEffect(() => {
    if (shouldAnimate()) {
      const accentColor = getComputedStyle(document.documentElement).getPropertyValue('--accent').trim()
      confetti({
        particleCount: 100,
        spread: 70,
        origin: { x: 0.5, y: 0.3 },
        colors: [accentColor, '#34d399', '#60a5fa', '#f472b6'],
        gravity: 1.2,
        ticks: 200,
        disableForReducedMotion: true,
      })
    }
  }, [])
  ```
  Confetti canvas has `aria-hidden="true"`.

- Two buttons at bottom:
  - "Launch Dashboard" (primary) -- triggers completion flow
  - "Take a Quick Tour" (secondary) -- triggers completion + starts tour

**Wire completion flow in SetupWizard.tsx:**

When "Launch Dashboard" is clicked:
1. Save credentials to OS keychain via `api.post('/api/wizard/save-credentials', { credentials: { ... } })` mapping wizard state fields to keychain keys:
   - supabaseUrl -> 'supabase.url'
   - supabaseAnonKey -> 'supabase.anon-key'
   - openclawUrl -> 'openclaw.api-url'
   - openclawApiKey -> 'openclaw.api-key'
   - blueBubblesUrl -> 'bluebubbles.host' (only if non-empty)
   - blueBubblesPassword -> 'bluebubbles.password' (only if non-empty)
   - macBridgeUrl -> 'mac-bridge.host' (only if non-empty)
   - macBridgeApiKey -> 'mac-bridge.api-key' (only if non-empty)
   - couchdbUrl -> 'couchdb.url' (only if non-empty)
   - couchdbUsername -> 'couchdb.user' (only if non-empty)
   - couchdbPassword -> 'couchdb.password' (only if non-empty)
2. Call `api.post('/api/wizard/reload-secrets')` to refresh backend AppState
3. Call `setEnabledModules(wizardState.enabledModules)` to persist module selection
4. Call `completeWizard()` which deletes wizard-state and sets setup-complete
5. Wizard exit animation: opacity 1->0, scale 1->0.95, 300ms. If reduced motion, instant.
6. Call onComplete callback (LayoutShell hides wizard)

**Update SetupWizard.tsx to use real step components:**
Replace placeholder divs with actual lazy-loaded step components for steps 1-8. Import from `./wizard/WizardTailscale`, etc.

**Wire demo mode:**
When "Try Demo" is clicked in SetupWizard:
1. Save current wizard progress to localStorage (already persisted by wizard-store)
2. Call activateDemoMode() -- sets demo-mode: 'true' in localStorage
3. Call completeWizard() to set setup-complete (so wizard doesn't reappear)
4. Navigate to dashboard with demo data showing

**Modify `frontend/src/components/DemoModeBanner.tsx`:**
- Add "Run Setup Wizard" button that calls resetWizard() and reloads the page (or sets state to show wizard)
- Keep existing demo banner functionality
- Import resetWizard from wizard-store

**Modify `frontend/src/pages/settings/SettingsConnections.tsx`:**
- Update the existing "Re-run Setup" button to use `resetWizard()` from wizard-store (instead of old resetSetupWizard from OnboardingWelcome)
- Add confirmation dialog per UI-SPEC: "This will restart the setup wizard. Your current services and modules won't change." with "Restart" (primary) and "Cancel" (secondary)
- Add "Re-run Walkthrough" button (separate from re-run setup) that triggers the guided tour without resetting setup
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/components/wizard/WizardSummary.tsx
    - grep "You're all set" frontend/src/components/wizard/WizardSummary.tsx returns a match
    - grep "canvas-confetti\|confetti" frontend/src/components/wizard/WizardSummary.tsx returns a match
    - grep "Launch Dashboard" frontend/src/components/wizard/WizardSummary.tsx returns a match
    - grep "Take a Quick Tour" frontend/src/components/wizard/WizardSummary.tsx returns a match
    - grep "save-credentials" frontend/src/components/SetupWizard.tsx returns a match
    - grep "reload-secrets" frontend/src/components/SetupWizard.tsx returns a match
    - grep "setEnabledModules" frontend/src/components/SetupWizard.tsx returns a match
    - grep "completeWizard" frontend/src/components/SetupWizard.tsx returns a match
    - grep "resetWizard\|Run Setup Wizard" frontend/src/components/DemoModeBanner.tsx returns a match
    - grep "resetWizard\|Re-run" frontend/src/pages/settings/SettingsConnections.tsx returns a match
    - Existing test suite passes
  </acceptance_criteria>
  <done>WizardSummary shows recap cards with confetti celebration. Completion flow saves credentials to keychain, reloads backend secrets, persists modules, and transitions to dashboard. Demo mode and Settings re-run buttons work. All real step components wired into SetupWizard.</done>
</task>

</tasks>

<verification>
```bash
cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose
ls frontend/src/components/wizard/Wizard*.tsx | wc -l  # Should be 10 files
```
</verification>

<success_criteria>
- WizardModules has 3 preset bundles, card grid with categories, toggle switches, unavailable dimming
- WizardTheme has mode selector and 8 preset cards with WYSIWYG preview
- WizardSummary shows service/module/theme recap with confetti
- Completion flow saves to keychain, reloads backend, persists modules, closes wizard
- Demo mode navigates to dashboard with demo data
- DemoModeBanner has "Run Setup Wizard" button
- SettingsConnections has "Re-run Walkthrough" button with confirmation dialog
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-wizard-onboarding/03-05-SUMMARY.md`
</output>
