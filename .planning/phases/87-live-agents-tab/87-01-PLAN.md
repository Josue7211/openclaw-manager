---
phase: 87-live-agents-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useAgents.ts
  - frontend/src/hooks/__tests__/useAgents.test.ts
  - frontend/src/pages/agents/types.ts
  - frontend/src/pages/agents/__tests__/AgentsPage.test.tsx
autonomous: true
requirements: [LIVE-01]

must_haves:
  truths:
    - "Agents tab lists real agents from the backend with correct fields displayed"
    - "Creating an agent via the UI results in a new agent in the database"
    - "Editing an agent persists changes through page refresh"
    - "Deleting an agent removes it from the list"
    - "Agent status indicators update via SSE without page refresh"
  artifacts:
    - path: "frontend/src/pages/agents/__tests__/AgentsPage.test.tsx"
      provides: "Smoke tests verifying AgentsPage renders with mock data"
      min_lines: 40
    - path: "frontend/src/hooks/__tests__/useAgents.test.ts"
      provides: "Existing tests remain green plus new response shape test"
      contains: "response shape"
  key_links:
    - from: "frontend/src/hooks/useAgents.ts"
      to: "/api/agents"
      via: "api.get in useQuery"
      pattern: "api\\.get.*'/api/agents'"
    - from: "frontend/src/pages/Agents.tsx"
      to: "useGatewaySSE"
      via: "SSE subscription for real-time updates"
      pattern: "useGatewaySSE"
---

<objective>
Verify the Agents tab renders real agent data from the backend, CRUD operations work end-to-end, and SSE events keep the display fresh.

Purpose: Final live verification that agent management is fully functional with real data from the Axum backend (local SQLite + gateway sync).
Output: Smoke test file for AgentsPage, verified response shape handling, all existing tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/pages/Agents.tsx
@frontend/src/hooks/useAgents.ts
@frontend/src/pages/agents/types.ts
@frontend/src/hooks/__tests__/useAgents.test.ts
@frontend/src/lib/query-keys.ts
@src-tauri/src/routes/agents.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify agent response shape and add AgentsPage smoke test</name>
  <read_first>
    - frontend/src/hooks/useAgents.ts (check API endpoint: /api/agents)
    - frontend/src/pages/agents/types.ts (check Agent interface fields)
    - src-tauri/src/routes/agents.rs (check actual JSON response shape from get_agents)
    - frontend/src/pages/Agents.tsx (check what fields are consumed)
    - frontend/src/hooks/__tests__/useAgents.test.ts (existing test patterns)
  </read_first>
  <files>
    frontend/src/pages/agents/__tests__/AgentsPage.test.tsx
    frontend/src/hooks/__tests__/useAgents.test.ts
  </files>
  <action>
    1. Compare the Agent interface in types.ts against the actual JSON response from get_agents in agents.rs.
       The backend returns: id, name, display_name, emoji, role, status, current_task, model, color, sort_order, created_at, updated_at.
       The frontend Agent type has: id, name, display_name, emoji, role, status, current_task, color, model, sort_order, created_at, updated_at.
       These should match. If any field is missing from the interface, add it.

    2. Add a test to useAgents.test.ts that verifies the response shape parsing handles the exact backend response format:
       - Test that a response with all fields populated maps correctly
       - Test that null optional fields (current_task: null, color: null) don't cause errors

    3. Create AgentsPage.test.tsx smoke test:
       - Mock useAgents to return a list of 2 agents with realistic data
       - Mock useQuery for health check to return { ok: true }
       - Mock useGatewaySSE, useTableRealtime, isDemoMode
       - Render AgentsPage inside QueryClientProvider + MemoryRouter
       - Assert: page renders without throwing
       - Assert: both agent names appear in the document
       - Assert: "Select an agent to view settings" empty state text appears (no agent selected)

    Follow existing test patterns from useAgents.test.ts (vi.mock, createWrapper, etc.).
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && npx vitest run src/hooks/__tests__/useAgents.test.ts src/pages/agents/__tests__/AgentsPage.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "response shape" frontend/src/hooks/__tests__/useAgents.test.ts (new test exists)
    - grep -q "AgentsPage" frontend/src/pages/agents/__tests__/AgentsPage.test.tsx (smoke test exists)
    - grep -q "renders without" frontend/src/pages/agents/__tests__/AgentsPage.test.tsx (render test exists)
    - All tests pass with zero failures
  </acceptance_criteria>
  <done>AgentsPage smoke test passes, useAgents response shape test passes, Agent interface matches backend JSON shape exactly</done>
</task>

<task type="auto">
  <name>Task 2: Verify all existing agent tests pass and CRUD endpoints respond correctly</name>
  <read_first>
    - frontend/src/hooks/__tests__/useAgents.test.ts
    - frontend/src/pages/agents/__tests__/types.test.ts
    - src-tauri/src/routes/agents.rs (check test module at bottom)
  </read_first>
  <files>
    frontend/src/hooks/useAgents.ts
  </files>
  <action>
    1. Run the full test suites for agents:
       - Frontend: vitest run on useAgents.test.ts, agents/types.test.ts, AgentsPage.test.tsx
       - Backend: cargo test in src-tauri filtering for agents tests

    2. Verify the useAgents hook correctly calls all 4 CRUD endpoints:
       - GET /api/agents (list)
       - POST /api/agents (create)
       - PATCH /api/agents (update)
       - DELETE /api/agents (delete)
       - POST /api/agents/action (action)

    3. Verify the useGatewaySSE integration in useAgents.ts subscribes to 'agent' events
       and invalidates queryKeys.agents. This was added in Phase 85.

    4. If any test fails, fix the issue. If all pass, no code changes needed for this task.
       The useAgents.ts file is listed in files_modified only in case a fix is needed.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && npx vitest run src/hooks/__tests__/useAgents.test.ts src/pages/agents/__tests__/types.test.ts src/pages/agents/__tests__/AgentsPage.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All frontend agent tests pass (0 failures)
    - grep -q "useGatewaySSE" frontend/src/hooks/useAgents.ts (SSE integration present)
    - grep -q "agent.*queryKeys.agents" frontend/src/hooks/useAgents.ts (query invalidation wired)
  </acceptance_criteria>
  <done>All agent frontend tests pass, CRUD endpoints verified, SSE real-time updates confirmed wired</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run src/hooks/__tests__/useAgents.test.ts src/pages/agents/__tests__/AgentsPage.test.tsx src/pages/agents/__tests__/types.test.ts` -- all pass
2. `grep -c "useGatewaySSE" frontend/src/hooks/useAgents.ts frontend/src/pages/Agents.tsx` -- both files contain SSE wiring
3. Agent type interface fields match backend response shape (id, name, display_name, emoji, role, status, current_task, color, model, sort_order, created_at, updated_at)
</verification>

<success_criteria>
- AgentsPage smoke test exists and passes
- All existing useAgents tests pass
- Agent response shape verified to match backend
- SSE agent event wiring confirmed in both hook and page
</success_criteria>

<output>
After completion, create `.planning/phases/87-live-agents-tab/87-01-SUMMARY.md`
</output>
