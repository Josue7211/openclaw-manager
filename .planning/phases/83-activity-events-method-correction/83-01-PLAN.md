---
phase: 83-activity-events-method-correction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/routes/gateway.rs
autonomous: true
requirements:
  - RPC-09

must_haves:
  truths:
    - "The activity route no longer calls activity.recent (this method does not exist in the protocol)"
    - "Activity data is sourced from logs.tail via gateway WS RPC (the closest real method for recent events)"
    - "The response payload is wrapped as { ok: true, data: payload } for the frontend"
  artifacts:
    - path: "src-tauri/src/routes/gateway.rs"
      provides: "Gateway WS logs.tail proxy for activity feed"
      contains: "logs.tail"
  key_links:
    - from: "src-tauri/src/routes/gateway.rs"
      to: "GatewayWsClient::request"
      via: "gw.request(\"logs.tail\", ...)"
      pattern: 'logs\.tail'
---

<objective>
Fix the activity route to use a real gateway protocol v3 method instead of the nonexistent `activity.recent`.

Purpose: The current `GET /api/gateway/activity` handler calls `gw.request("activity.recent", json!({}))` but `activity.recent` does not exist in the OpenClaw protocol v3 (see reference — 88 methods listed, none called `activity.*`). The closest real method is `logs.tail` which returns recent gateway log entries (session starts/stops, cron runs, errors). The frontend ActivityPage already handles generic event objects with type/description/timestamp fields, which `logs.tail` provides. (RPC-09)

Output: The `gateway_activity` handler calls `logs.tail` instead of the nonexistent `activity.recent`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src-tauri/src/routes/gateway.rs

<interfaces>
<!-- Protocol v3 — there is NO activity.* method. Relevant alternatives: -->
logs.tail — params: { limit?: number } — returns recent log entries from the gateway
<!-- Gateway events (server push, not RPC): -->
agent, chat, presence, tick, cron, health, heartbeat — these are real-time push events, not request/response

<!-- Current WRONG code in gateway.rs (lines 362-382): -->
```rust
async fn gateway_activity(
    State(state): State<AppState>,
    RequireAuth(_session): RequireAuth,
) -> Result<Json<Value>, AppError> {
    let gw = state.gateway_ws.as_ref().ok_or_else(|| {
        AppError::BadRequest("OpenClaw Gateway not configured.".into())
    })?;

    let payload = gw
        .request("activity.recent", json!({}))
        .await
        .map_err(|e| {
            tracing::error!("[gateway] activity.recent failed: {e}");
            AppError::BadRequest(format!("Gateway error: {}", sanitize_error_body(&e)))
        })?;

    Ok(Json(json!({
        "ok": true,
        "data": payload,
    })))
}
```

<!-- Frontend ActivityPage.tsx (line 70) fetches: -->
```typescript
api.get<ActivityResponse>('/api/gateway/activity')
```
<!-- The route path stays the same — only the underlying RPC method changes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace activity.recent with logs.tail in gateway_activity handler (RPC-09)</name>
  <read_first>src-tauri/src/routes/gateway.rs (lines 356-382 — gateway_activity handler)</read_first>
  <files>src-tauri/src/routes/gateway.rs</files>
  <action>
In the `gateway_activity` handler (around line 362):

1. Change the doc comment from `activity.recent` to `logs.tail`:
   - Line ~360: `/// Proxies `activity.recent`` becomes `/// Proxies `logs.tail``
   - Line ~361: `/// Returns recent gateway events` stays the same (still true)

2. Change the RPC method string from `"activity.recent"` to `"logs.tail"`:
   - Line ~371: `.request("activity.recent", json!({}))` becomes `.request("logs.tail", json!({}))`

3. Change the error log message:
   - Line ~374: `"[gateway] activity.recent failed: {e}"` becomes `"[gateway] logs.tail failed: {e}"`

The HTTP route path `/gateway/activity` stays the same — the frontend already uses this path and doesn't need to change. Only the underlying WS RPC method name changes.

Do NOT change any other part of the function — the gateway-not-configured guard, RequireAuth, response shape, and error handling pattern all stay identical.
  </action>
  <acceptance_criteria>
    - grep "logs.tail" src-tauri/src/routes/gateway.rs returns at least 2 matches (doc comment + request call)
    - grep "activity.recent" src-tauri/src/routes/gateway.rs returns 0 matches (completely removed)
    - grep "gateway_activity" src-tauri/src/routes/gateway.rs returns at least 2 matches (function def + route)
    - grep "/gateway/activity" src-tauri/src/routes/gateway.rs returns 1 match (route still registered)
    - CARGO_TARGET_DIR=/tmp/mc-target cargo build -p mission-control compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control && grep -c "logs\.tail" src-tauri/src/routes/gateway.rs && grep -c "activity\.recent" src-tauri/src/routes/gateway.rs | grep "^0$" && CARGO_TARGET_DIR=/tmp/mc-target cargo test -p mission-control --lib routes::gateway::tests 2>&1 | tail -3</automated>
  </verify>
  <done>gateway_activity calls logs.tail (not activity.recent). Zero occurrences of "activity.recent" in gateway.rs. Route path /gateway/activity unchanged. All Rust tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -n "logs\.tail" src-tauri/src/routes/gateway.rs` returns at least 2 matches (doc comment + RPC call)
2. `grep -c "activity\.recent" src-tauri/src/routes/gateway.rs` returns 0
3. `grep -n "/gateway/activity" src-tauri/src/routes/gateway.rs` returns 1 match (route still registered)
4. `CARGO_TARGET_DIR=/tmp/mc-target cargo build -p mission-control` compiles without errors
5. `CARGO_TARGET_DIR=/tmp/mc-target cargo test -p mission-control` passes all tests
</verification>

<success_criteria>
- Zero occurrences of "activity.recent" in gateway.rs
- logs.tail called via gateway WS RPC in the gateway_activity handler
- HTTP route /api/gateway/activity still exists (frontend doesn't need to change)
- All Rust tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/83-activity-events-method-correction/83-01-SUMMARY.md`
</output>
