---
phase: 77-chat-method-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/routes/gateway.rs
autonomous: true
requirements:
  - RPC-01
  - RPC-02

must_haves:
  truths:
    - "Session history RPC calls chat.history with sessionKey param (not sessions.history with session_id)"
    - "Session send RPC calls chat.send with sessionKey, message, deliver, and idempotencyKey params (not sessions.send with session_id)"
    - "Both calls compile and pass existing Rust tests"
  artifacts:
    - path: "src-tauri/src/routes/gateway.rs"
      provides: "Corrected chat.history and chat.send RPC method calls"
      contains: "chat.history"
  key_links:
    - from: "src-tauri/src/routes/gateway.rs"
      to: "gateway_ws.request()"
      via: "RPC method string"
      pattern: 'chat\.(history|send)'
---

<objective>
Fix the two wrong RPC method names in gateway.rs so chat history and message sending use the correct OpenClaw gateway protocol v3 methods.

Purpose: The gateway rejects `sessions.history` and `sessions.send` because those methods do not exist in protocol v3. The correct methods are `chat.history` (with `sessionKey` param) and `chat.send` (with `sessionKey`, `message`, `deliver`, `idempotencyKey` params).

Output: gateway.rs with corrected method names and param shapes that match the live gateway protocol.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src-tauri/src/routes/gateway.rs
</context>

<interfaces>
<!-- Protocol v3 method signatures from reference_openclaw_gateway_protocol.md -->

chat.history params: { sessionKey: string, limit?: number }
chat.send params: { sessionKey: string, message: string, deliver: boolean, idempotencyKey: string }

<!-- gateway_ws request signature -->
From src-tauri/src/gateway_ws.rs:
```rust
pub async fn request(&self, method: &str, params: Value) -> Result<Value, String>
```

<!-- Current WRONG code in gateway.rs (lines 304-305): -->
```rust
.request("sessions.history", json!({"session_id": session_id}))
```

<!-- Current WRONG code in gateway.rs (lines 343-345): -->
```rust
.request("sessions.send", json!({"session_id": session_id, "message": message}))
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix chat.history RPC method and params (RPC-01)</name>
  <read_first>src-tauri/src/routes/gateway.rs (lines 287-316)</read_first>
  <files>src-tauri/src/routes/gateway.rs</files>
  <action>
In `gateway_session_history` (around line 293):

1. Change the doc comment on line 291 from `sessions.history` to `chat.history`
2. Change the RPC method string on line 305 from `"sessions.history"` to `"chat.history"` (per RPC-01)
3. Change the params on line 305 from `json!({"session_id": session_id})` to `json!({"sessionKey": session_id})` -- the protocol uses `sessionKey` not `session_id`
4. Change the error log on line 308 from `sessions.history` to `chat.history`

The HTTP route path `/gateway/sessions/:id/history` stays the same -- it is our internal API, not the gateway method name.
  </action>
  <acceptance_criteria>
    - grep "chat.history" src-tauri/src/routes/gateway.rs returns exactly 2 matches (doc comment + request call)
    - grep "sessions.history" src-tauri/src/routes/gateway.rs returns 0 matches
    - grep "sessionKey" src-tauri/src/routes/gateway.rs returns at least 1 match
  </acceptance_criteria>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control && grep -c "chat\.history" src-tauri/src/routes/gateway.rs && grep -c "sessions\.history" src-tauri/src/routes/gateway.rs | grep "^0$"</automated>
  </verify>
  <done>gateway_session_history calls chat.history with sessionKey param, no references to sessions.history remain</done>
</task>

<task type="auto">
  <name>Task 2: Fix chat.send RPC method and params (RPC-02)</name>
  <read_first>src-tauri/src/routes/gateway.rs (lines 318-354)</read_first>
  <files>src-tauri/src/routes/gateway.rs</files>
  <action>
In `gateway_session_send` (around line 323):

1. Change the doc comment on line 322 from `sessions.send` to `chat.send`
2. Change the RPC method string on line 344 from `"sessions.send"` to `"chat.send"` (per RPC-02)
3. Replace the params on lines 344-345 from:
   ```rust
   json!({"session_id": session_id, "message": message})
   ```
   to:
   ```rust
   json!({
       "sessionKey": session_id,
       "message": message,
       "deliver": true,
       "idempotencyKey": crate::routes::util::random_uuid()
   })
   ```
   The protocol requires `deliver` (boolean, set to true for immediate delivery) and `idempotencyKey` (unique string per message). Use the existing `random_uuid()` helper from `crate::routes::util`.
4. Change the error log on line 349 from `sessions.send` to `chat.send`

The HTTP route path `/gateway/sessions/:id/send` stays the same.
  </action>
  <acceptance_criteria>
    - grep "chat.send" src-tauri/src/routes/gateway.rs returns exactly 2 matches (doc comment + request call)
    - grep "sessions.send" src-tauri/src/routes/gateway.rs returns 0 matches
    - grep "deliver" src-tauri/src/routes/gateway.rs returns at least 1 match
    - grep "idempotencyKey" src-tauri/src/routes/gateway.rs returns at least 1 match
  </acceptance_criteria>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control && CARGO_TARGET_DIR=/tmp/mc-target cargo test -p mission-control --lib routes::gateway::tests 2>&1 | tail -5</automated>
  </verify>
  <done>gateway_session_send calls chat.send with sessionKey, message, deliver, and idempotencyKey params. Existing tests pass. No references to sessions.send remain.</done>
</task>

</tasks>

<verification>
1. `grep -n "sessions\.history\|sessions\.send" src-tauri/src/routes/gateway.rs` returns no matches
2. `grep -n "chat\.history\|chat\.send" src-tauri/src/routes/gateway.rs` returns 4 matches (2 per method: doc comment + request call)
3. `grep -n "sessionKey" src-tauri/src/routes/gateway.rs` returns 2 matches (one per handler)
4. `grep -n "idempotencyKey" src-tauri/src/routes/gateway.rs` returns 1 match
5. `grep -n "deliver" src-tauri/src/routes/gateway.rs` returns 1 match
6. `CARGO_TARGET_DIR=/tmp/mc-target cargo test -p mission-control --lib routes::gateway::tests` passes
</verification>

<success_criteria>
- Zero occurrences of "sessions.history" or "sessions.send" in gateway.rs
- chat.history called with { sessionKey } param
- chat.send called with { sessionKey, message, deliver: true, idempotencyKey } params
- All existing Rust tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/77-chat-method-corrections/77-01-SUMMARY.md`
</output>
