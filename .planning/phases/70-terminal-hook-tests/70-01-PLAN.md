---
phase: 70-terminal-hook-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/__tests__/useTerminal.test.ts
  - frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "useTerminal tests verify WebSocket connect, message receipt, input send, resize, disconnect, and reconnect"
    - "useSessionOutput tests verify stream start, data arrival, stream end, and error handling"
    - "WebSocket mock properly simulates connect/message/close lifecycle with didOpen tracking"
    - "All tests pass via npx vitest run"
  artifacts:
    - path: "frontend/src/hooks/__tests__/useTerminal.test.ts"
      provides: "Unit tests for useTerminal hook"
      min_lines: 150
    - path: "frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts"
      provides: "Unit tests for useSessionOutput hook"
      min_lines: 100
  key_links:
    - from: "frontend/src/hooks/__tests__/useTerminal.test.ts"
      to: "frontend/src/hooks/useTerminal.ts"
      via: "import under test"
      pattern: "import.*useTerminal"
    - from: "frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts"
      to: "frontend/src/hooks/sessions/useSessionOutput.ts"
      via: "import under test"
      pattern: "import.*useSessionOutput"
---

<objective>
Write unit tests for the useTerminal and useSessionOutput hooks, covering WebSocket lifecycle edge cases.

Purpose: Fulfill TEST-02 — terminal hooks must have test coverage to prevent regressions during future gateway integration work.
Output: Two test files covering all WebSocket lifecycle states (connect, message, close, reconnect, error).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/hooks/useTerminal.ts
@frontend/src/hooks/sessions/useSessionOutput.ts
@frontend/src/lib/terminal-theme.ts
@frontend/src/lib/api.ts
@frontend/vitest.config.ts
@frontend/src/test-setup.ts

<interfaces>
<!-- Existing test patterns to follow -->

From frontend/src/lib/hooks/__tests__/useRealtimeSSE.test.ts (MockEventSource pattern):
```typescript
// Module-scope mock class with static instances array
class MockEventSource {
  static instances: MockEventSource[] = []
  readyState = 1
  onmessage: ((event: MessageEvent) => void) | null = null
  onerror: (() => void) | null = null
  url: string
  constructor(url: string) { ... MockEventSource.instances.push(this) }
  close() { this.readyState = 2 }
  simulateMessage(data: string) { ... }
}
```

From frontend/src/lib/__tests__/api.test.ts (mock pattern):
```typescript
vi.mock('@/lib/api', () => ({
  API_BASE: 'http://127.0.0.1:3000',
  api: { get: vi.fn() },
}))
```

From frontend/src/hooks/useTerminal.ts (hook signature):
```typescript
interface UseTerminalOptions { fontSize?: number }
interface UseTerminalReturn { connected: boolean; error: string | null }
export function useTerminal(
  containerRef: React.RefObject<HTMLDivElement | null>,
  options?: UseTerminalOptions
): UseTerminalReturn
```

From frontend/src/hooks/sessions/useSessionOutput.ts (hook signature):
```typescript
interface UseSessionOutputReturn { connected: boolean; error: string | null }
export function useSessionOutput(
  containerRef: React.RefObject<HTMLDivElement | null>,
  sessionId: string | null,
  options?: { fontSize?: number }
): UseSessionOutputReturn
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write useTerminal hook tests</name>
  <files>frontend/src/hooks/__tests__/useTerminal.test.ts</files>
  <read_first>
    - frontend/src/hooks/useTerminal.ts (the hook under test — 232 lines)
    - frontend/src/lib/hooks/__tests__/useRealtimeSSE.test.ts (MockEventSource pattern to mirror for MockWebSocket)
    - frontend/src/lib/__tests__/api.test.ts (api mock pattern)
  </read_first>
  <behavior>
    - Test 1: Sets connected=true when WebSocket opens
    - Test 2: Writes ArrayBuffer data to terminal on binary message
    - Test 3: Writes string data to terminal on text message
    - Test 4: Sets error when JSON error envelope received via WS message
    - Test 5: Sets connected=false on WebSocket close
    - Test 6: Sets error="Terminal connection failed" on close with code 1006 before open
    - Test 7: Sends JSON {type:'input', data} to WS when terminal onData fires
    - Test 8: Sends JSON {type:'resize', cols, rows} to WS when terminal onResize fires
    - Test 9: Sends initial resize dimensions on WS open
    - Test 10: Sets error when capacity check returns available=0
    - Test 11: Disposes terminal and closes WS on unmount
    - Test 12: Does not update state after unmount (mountedRef guard)
  </behavior>
  <action>
    Create `frontend/src/hooks/__tests__/useTerminal.test.ts`.

    **Mock infrastructure (at module scope, before vi.mock calls):**

    1. Create `MockWebSocket` class following the MockEventSource pattern from useRealtimeSSE.test.ts:
       - Static `instances: MockWebSocket[]` array for test access
       - Properties: `url`, `binaryType`, `readyState`, `onopen`, `onmessage`, `onerror`, `onclose`
       - `send(data)` method that records to `sentMessages: string[]` array
       - `close()` sets readyState=3 (CLOSED) and fires onclose with `{code: 1000}`
       - Helper `simulateOpen()` that sets readyState=1 and calls `onopen`
       - Helper `simulateMessage(data: string | ArrayBuffer)` that calls `onmessage` with `new MessageEvent('message', { data })`
       - Helper `simulateClose(code: number)` that calls `onclose` with `{ code }`
       - Static constants: CONNECTING=0, OPEN=1, CLOSING=2, CLOSED=3

    2. Create `MockTerminal` class:
       - Constructor stores instances like MockWebSocket
       - Methods (all vi.fn()): `open`, `write`, `dispose`, `loadAddon`, `paste`, `getSelection`, `attachCustomKeyEventHandler`
       - `onData(cb)` stores callback, returns `{ dispose: vi.fn() }`
       - `onBinary(cb)` stores callback, returns `{ dispose: vi.fn() }`
       - `onResize(cb)` stores callback, returns `{ dispose: vi.fn() }`
       - Mutable `options` object with `fontSize` and `theme`
       - Static `instances` array

    3. Create `MockFitAddon` class:
       - `fit: vi.fn()`
       - `proposeDimensions: vi.fn().mockReturnValue({ cols: 80, rows: 24 })`

    **vi.mock calls:**
    - `vi.mock('@xterm/xterm', () => ({ Terminal: MockTerminal }))` — using vi.hoisted if needed
    - `vi.mock('@xterm/addon-fit', () => ({ FitAddon: MockFitAddon }))`
    - `vi.mock('@xterm/addon-web-links', () => ({ WebLinksAddon: vi.fn() }))`
    - `vi.mock('@xterm/xterm/css/xterm.css', () => ({}))` — CSS import is no-op in test
    - `vi.mock('@/lib/api', () => ({ API_BASE: 'http://127.0.0.1:3000', api: { get: vi.fn().mockResolvedValue({ active: 0, max: 5, available: 5 }) } }))`
    - `vi.mock('@/lib/terminal-theme', () => ({ buildThemeFromCSS: vi.fn().mockReturnValue({ background: '#000' }) }))`

    **Mock globals in beforeEach:**
    - `vi.stubGlobal('WebSocket', MockWebSocket)`
    - `vi.stubGlobal('ResizeObserver', MockResizeObserver)` — simple class that stores callback, has `observe: vi.fn()`, `disconnect: vi.fn()`
    - `vi.stubGlobal('MutationObserver', MockMutationObserver)` — same pattern
    - `vi.stubGlobal('requestAnimationFrame', (cb: () => void) => { cb(); return 0 })` — execute synchronously
    - Reset all mock instance arrays in beforeEach

    **Test setup helper:**
    - `createContainerRef()` returns `{ current: document.createElement('div') }`
    - Use `renderHook` from `@testing-library/react` with `act` for async operations
    - Use `await vi.waitFor()` or `await act(async () => { await flushPromises() })` for async setup() to complete
    - `flushPromises = () => new Promise(r => setTimeout(r, 0))`

    **Write all 12 tests** listed in the behavior section. Each test:
    1. Renders the hook with a container ref
    2. Waits for async setup (capacity check + WS creation)
    3. Simulates the specific WebSocket event
    4. Asserts the expected state change

    For the capacity check test (Test 10): override `api.get` mock to return `{ active: 5, max: 5, available: 0 }` before rendering.

    For the unmount test (Test 12): render, simulate open, unmount, then simulate message — verify no state updates (no act warnings).
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && npx vitest run src/hooks/__tests__/useTerminal.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "it(" frontend/src/hooks/__tests__/useTerminal.test.ts returns >= 10
    - grep -q "MockWebSocket" frontend/src/hooks/__tests__/useTerminal.test.ts
    - grep -q "simulateOpen\|simulateMessage\|simulateClose" frontend/src/hooks/__tests__/useTerminal.test.ts
    - grep -q "connected.*true" frontend/src/hooks/__tests__/useTerminal.test.ts
    - grep -q "Terminal connection failed" frontend/src/hooks/__tests__/useTerminal.test.ts
    - npx vitest run src/hooks/__tests__/useTerminal.test.ts exits 0
  </acceptance_criteria>
  <done>All 12 useTerminal tests pass. MockWebSocket properly simulates connect/message/close/error lifecycle. Capacity check, input forwarding, resize, and cleanup all verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write useSessionOutput hook tests</name>
  <files>frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts</files>
  <read_first>
    - frontend/src/hooks/sessions/useSessionOutput.ts (the hook under test — 139 lines)
    - frontend/src/hooks/__tests__/useTerminal.test.ts (reuse MockWebSocket pattern from Task 1)
  </read_first>
  <behavior>
    - Test 1: Sets connected=true when WebSocket opens
    - Test 2: Writes string data to terminal on text message
    - Test 3: Writes ArrayBuffer data to terminal on binary message
    - Test 4: Sets error when JSON error envelope received
    - Test 5: Sets connected=false on WebSocket close
    - Test 6: Sets error="Session output connection failed" on close with code 1006 before open
    - Test 7: Does not create WebSocket when sessionId is null
    - Test 8: Reconnects with new WebSocket when sessionId changes
    - Test 9: Creates read-only terminal (disableStdin: true)
    - Test 10: Disposes terminal and closes WS on unmount
    - Test 11: WebSocket URL includes sessionId in path
  </behavior>
  <action>
    Create `frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts`.

    **Reuse the same mock infrastructure pattern from Task 1** — duplicate the MockWebSocket, MockTerminal, MockFitAddon, MockResizeObserver, MockMutationObserver classes (each test file must be self-contained since vitest runs files in isolation).

    **Key differences from useTerminal tests:**
    - No capacity check (no api.get mock needed for happy path)
    - No input forwarding (read-only terminal — no onData/onBinary tests)
    - No resize protocol (ResizeObserver only fits terminal, does NOT send WS message)
    - sessionId-dependent: null sessionId = no WS connection
    - WS URL is `ws://127.0.0.1:3000/api/claude-sessions/${sessionId}/ws`

    **vi.mock calls (same as Task 1 except api mock is simpler):**
    - `vi.mock('@xterm/xterm', ...)` — same MockTerminal
    - `vi.mock('@xterm/addon-fit', ...)` — same MockFitAddon
    - `vi.mock('@xterm/addon-web-links', ...)` — same no-op
    - `vi.mock('@xterm/xterm/css/xterm.css', ...)` — same no-op
    - `vi.mock('@/lib/api', () => ({ API_BASE: 'http://127.0.0.1:3000' }))` — only API_BASE needed, no api.get
    - `vi.mock('@/lib/terminal-theme', ...)` — same

    **Test for null sessionId (Test 7):** Render with `sessionId=null`, verify no MockWebSocket instances created.

    **Test for sessionId change (Test 8):**
    - Render with `sessionId='session-1'`
    - Wait for WS creation, simulate open
    - Rerender with `sessionId='session-2'` via `renderHook` rerender
    - Verify first WS was closed, new WS created with session-2 URL

    **Test for read-only (Test 9):** Verify MockTerminal constructor was called with options including `disableStdin: true`.

    **Test for WS URL (Test 11):** Render with `sessionId='abc-123'`, verify `MockWebSocket.instances[0].url` is `ws://127.0.0.1:3000/api/claude-sessions/abc-123/ws`.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && npx vitest run src/hooks/sessions/__tests__/useSessionOutput.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "it(" frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts returns >= 9
    - grep -q "MockWebSocket" frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts
    - grep -q "sessionId" frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts
    - grep -q "Session output connection failed" frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts
    - grep -q "disableStdin" frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts
    - npx vitest run src/hooks/sessions/__tests__/useSessionOutput.test.ts exits 0
  </acceptance_criteria>
  <done>All 11 useSessionOutput tests pass. Read-only terminal verified, sessionId-driven WS lifecycle verified, null sessionId guard verified, reconnect on sessionId change verified.</done>
</task>

</tasks>

<verification>
Run full test suite to ensure no regressions:
```bash
cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -10
```
Both new test files pass alongside existing 1039+ tests.
</verification>

<success_criteria>
- frontend/src/hooks/__tests__/useTerminal.test.ts exists with >= 10 test cases
- frontend/src/hooks/sessions/__tests__/useSessionOutput.test.ts exists with >= 9 test cases
- MockWebSocket simulates full lifecycle: open, message (text + binary), close (normal + 1006), error
- `npx vitest run` passes with zero failures
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/70-terminal-hook-tests/70-01-SUMMARY.md`
</output>
