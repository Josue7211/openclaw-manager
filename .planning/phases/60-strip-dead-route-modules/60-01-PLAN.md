---
phase: 60-strip-dead-route-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/routes/mod.rs
  - src-tauri/src/routes/decisions.rs
  - src-tauri/src/routes/dlp.rs
  - src-tauri/src/routes/habits.rs
autonomous: true
requirements: [RUST-03]

must_haves:
  truths:
    - "decisions.rs, dlp.rs, and habits.rs route modules are deleted from the codebase"
    - "routes/mod.rs no longer declares or merges the three dead modules"
    - "cargo build succeeds with no missing-module or dead_code errors"
    - "cargo test passes (no test regressions from removal)"
    - "workspace.rs and deploy.rs are documented as kept (have active frontend consumers)"
  artifacts:
    - path: "src-tauri/src/routes/mod.rs"
      provides: "Route registry without decisions, dlp, or habits modules"
      contains: "pub mod workspace"
    - path: "src-tauri/src/routes/decisions.rs"
      provides: "DELETED"
    - path: "src-tauri/src/routes/dlp.rs"
      provides: "DELETED"
    - path: "src-tauri/src/routes/habits.rs"
      provides: "DELETED"
  key_links:
    - from: "src-tauri/src/routes/mod.rs"
      to: "router() function"
      via: "merge/nest calls removed for dead modules"
      pattern: "decisions|dlp|habits"
---

<objective>
Remove three confirmed-dead backend route modules (decisions.rs, dlp.rs, habits.rs) that have zero frontend consumers. Document kept routes that were audit candidates but have active callers.

Purpose: Eliminate dead backend code that increases compile time and confuses contributors. The audit confirmed workspace.rs (Memory page) and deploy.rs (LiveProcesses) have frontend consumers and must stay.

Output: Three route files deleted, mod.rs cleaned, build and tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src-tauri/src/routes/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and document route consumer status</name>
  <read_first>
    - src-tauri/src/routes/mod.rs (current route registry -- 50 modules registered)
    - frontend/src/pages/Memory.tsx (workspace.rs consumer -- lines 38, 105, 117, 128)
    - frontend/src/pages/agents/LiveProcesses.tsx (deploy.rs consumer -- line 22)
  </read_first>
  <files>src-tauri/src/routes/workspace.rs, src-tauri/src/routes/deploy.rs</files>
  <action>
Add a `// Called by:` documentation comment to the two audit-candidate routes that have active consumers, so future audits can quickly verify they are not dead:

1. In `workspace.rs`, above the `pub fn router()` function (line 31), add:
   ```
   // Called by: frontend/src/pages/Memory.tsx (list_files, read_file, write_file, delete_file)
   ```

2. In `deploy.rs`, above the `pub fn router()` function (line 10), add:
   ```
   // Called by: frontend/src/pages/agents/LiveProcesses.tsx (handleDeploy)
   ```

Do NOT modify any logic. Only add documentation comments.
  </action>
  <acceptance_criteria>
    - grep -n "Called by:" src-tauri/src/routes/workspace.rs returns a match mentioning Memory.tsx
    - grep -n "Called by:" src-tauri/src/routes/deploy.rs returns a match mentioning LiveProcesses.tsx
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Called by:" src-tauri/src/routes/workspace.rs src-tauri/src/routes/deploy.rs</automated>
  </verify>
  <done>Both kept routes have "Called by:" comments documenting their frontend consumers</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead route modules and clean mod.rs</name>
  <read_first>
    - src-tauri/src/routes/mod.rs (lines to remove: `pub mod decisions;` line 16, `pub mod dlp;` line 18, `pub mod habits;` line 23, `.merge(decisions::router())` line 76, `.merge(dlp::router())` line 77, `.merge(habits::router())` line 67)
    - src-tauri/src/routes/decisions.rs (154 lines, CRUD for decisions table -- zero frontend callers)
    - src-tauri/src/routes/dlp.rs (160 lines, PATCH /restore endpoint -- zero frontend callers)
    - src-tauri/src/routes/habits.rs (293 lines, habits + habit_entries CRUD -- zero frontend callers)
  </read_first>
  <files>src-tauri/src/routes/mod.rs, src-tauri/src/routes/decisions.rs, src-tauri/src/routes/dlp.rs, src-tauri/src/routes/habits.rs</files>
  <action>
1. Delete these three files entirely:
   - src-tauri/src/routes/decisions.rs
   - src-tauri/src/routes/dlp.rs
   - src-tauri/src/routes/habits.rs

2. In src-tauri/src/routes/mod.rs, remove the following lines:
   - `pub mod decisions;` (line 16)
   - `pub mod dlp;` (line 18)
   - `pub mod habits;` (line 23)
   - `.merge(decisions::router())` (line 76)
   - `.merge(dlp::router())` (line 77)
   - `.merge(habits::router())` (line 67)

IMPORTANT: Do NOT remove table references from sync.rs SYNC_TABLES or server.rs SOFT_DELETE_TABLES.
Those arrays govern data sync and cleanup for tables that still exist in both SQLite and Supabase.
The tables remain -- only the dead API route handlers are removed. The DLP RESTORABLE_TABLES
constant in dlp.rs is deleted along with the file (it was only used inside dlp.rs).

3. Run `CARGO_TARGET_DIR=/tmp/mc-target cargo build` to confirm compilation.
4. Run `CARGO_TARGET_DIR=/tmp/mc-target cargo test` to confirm no test regressions.
   Note: dlp.rs had 5 unit tests (restorable_tables_are_non_empty, etc.) -- these are deleted with the file. No other module depends on them.
  </action>
  <acceptance_criteria>
    - test ! -f src-tauri/src/routes/decisions.rs (file deleted)
    - test ! -f src-tauri/src/routes/dlp.rs (file deleted)
    - test ! -f src-tauri/src/routes/habits.rs (file deleted)
    - grep -c "pub mod decisions" src-tauri/src/routes/mod.rs returns 0
    - grep -c "pub mod dlp" src-tauri/src/routes/mod.rs returns 0
    - grep -c "pub mod habits" src-tauri/src/routes/mod.rs returns 0
    - grep -c "decisions::router" src-tauri/src/routes/mod.rs returns 0
    - grep -c "dlp::router" src-tauri/src/routes/mod.rs returns 0
    - grep -c "habits::router" src-tauri/src/routes/mod.rs returns 0
    - grep -c "pub mod workspace" src-tauri/src/routes/mod.rs returns 1 (kept)
    - grep -c "pub mod deploy" src-tauri/src/routes/mod.rs returns 1 (kept)
    - CARGO_TARGET_DIR=/tmp/mc-target cargo build succeeds (exit 0)
    - CARGO_TARGET_DIR=/tmp/mc-target cargo test succeeds (exit 0)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control && test ! -f src-tauri/src/routes/decisions.rs && test ! -f src-tauri/src/routes/dlp.rs && test ! -f src-tauri/src/routes/habits.rs && ! grep -q "pub mod decisions\|pub mod dlp\|pub mod habits" src-tauri/src/routes/mod.rs && CARGO_TARGET_DIR=/tmp/mc-target cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && CARGO_TARGET_DIR=/tmp/mc-target cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -10</automated>
  </verify>
  <done>Three dead route files deleted, mod.rs cleaned, cargo build and cargo test pass with zero errors</done>
</task>

</tasks>

<verification>
1. `ls src-tauri/src/routes/decisions.rs src-tauri/src/routes/dlp.rs src-tauri/src/routes/habits.rs 2>&1` -- all three report "No such file"
2. `grep -E "decisions|dlp|habits" src-tauri/src/routes/mod.rs` -- no matches
3. `grep "Called by:" src-tauri/src/routes/workspace.rs src-tauri/src/routes/deploy.rs` -- both have documentation
4. `CARGO_TARGET_DIR=/tmp/mc-target cargo build --manifest-path src-tauri/Cargo.toml` -- exit 0
5. `CARGO_TARGET_DIR=/tmp/mc-target cargo test --manifest-path src-tauri/Cargo.toml` -- exit 0
6. `grep "habits" src-tauri/src/sync.rs` -- still present (sync tables untouched)
</verification>

<success_criteria>
- Three dead route modules removed (decisions.rs, dlp.rs, habits.rs = 607 lines of dead code eliminated)
- Two kept routes documented with "Called by:" comments (workspace.rs, deploy.rs)
- Sync engine and cleanup job table arrays left intact (tables still exist, data still syncs)
- Cargo build and test both pass
- No frontend functionality regressed (zero frontend consumers of removed routes)
</success_criteria>

<output>
After completion, create `.planning/phases/60-strip-dead-route-modules/60-01-SUMMARY.md`
</output>
