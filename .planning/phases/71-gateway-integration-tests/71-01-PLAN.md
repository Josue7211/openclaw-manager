---
phase: 71-gateway-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/sessions/__tests__/useGatewayStatus.test.ts
  - frontend/src/components/__tests__/GatewayStatusDot.test.tsx
  - frontend/src/hooks/__tests__/useOpenClawHealth.test.ts
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "useGatewayStatus returns connected=true when /api/gateway/status reports connected"
    - "useGatewayStatus returns connected=false and status=disconnected when /api/gateway/status reports disconnected"
    - "useGatewayStatus returns not_configured in demo mode without calling the API"
    - "GatewayStatusDot renders a green dot when gateway is connected"
    - "GatewayStatusDot renders a red dot when gateway is disconnected"
    - "GatewayStatusDot renders nothing while loading"
    - "OpenClaw health query returns ok=true when /api/openclaw/health reports connected"
    - "OpenClaw health query returns ok=false when /api/openclaw/health reports unreachable"
    - "OpenClaw health query returns ok=false with status=not_configured when gateway is not configured"
  artifacts:
    - path: "frontend/src/hooks/sessions/__tests__/useGatewayStatus.test.ts"
      provides: "Unit tests for useGatewayStatus hook"
      min_lines: 60
    - path: "frontend/src/components/__tests__/GatewayStatusDot.test.tsx"
      provides: "Unit tests for GatewayStatusDot component"
      min_lines: 50
    - path: "frontend/src/hooks/__tests__/useOpenClawHealth.test.ts"
      provides: "Integration tests for OpenClaw health check query pattern"
      min_lines: 50
  key_links:
    - from: "frontend/src/hooks/sessions/useGatewayStatus.ts"
      to: "/api/gateway/status"
      via: "api.get in useQuery"
      pattern: "api\\.get.*gateway/status"
    - from: "frontend/src/components/GatewayStatusDot.tsx"
      to: "useGatewayStatus"
      via: "hook import"
      pattern: "useGatewayStatus"
    - from: "frontend/src/pages/OpenClaw.tsx"
      to: "/api/openclaw/health"
      via: "api.get in useQuery"
      pattern: "api\\.get.*openclaw/health"
---

<objective>
Write integration tests for the gateway health and status endpoints, verifying that the frontend hooks and components correctly handle connected, disconnected, unreachable, and not-configured states from `/api/openclaw/health` and `/api/gateway/status`.

Purpose: Catch regressions in gateway connectivity reporting before they reach users. These paths are the primary way users know whether OpenClaw is reachable.
Output: 3 test files covering useGatewayStatus hook, GatewayStatusDot component, and the OpenClaw health query pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@frontend/src/hooks/sessions/useGatewayStatus.ts
@frontend/src/components/GatewayStatusDot.tsx
@frontend/src/pages/sessions/types.ts
@frontend/src/lib/query-keys.ts
@frontend/src/lib/api.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/pages/sessions/types.ts:
```typescript
export type GatewayConnectionStatus = 'connected' | 'disconnected' | 'not_configured'

export interface GatewayStatusResponse {
  connected: boolean
  status: GatewayConnectionStatus
}

export const GATEWAY_STATUS_COLORS: Record<GatewayConnectionStatus, string> = {
  connected: 'var(--green-400)',
  disconnected: 'var(--red-500)',
  not_configured: 'var(--text-muted)',
}

export const GATEWAY_STATUS_LABELS: Record<GatewayConnectionStatus, string> = {
  connected: 'Gateway connected',
  disconnected: 'Gateway disconnected',
  not_configured: 'Gateway not configured',
}
```

From frontend/src/hooks/sessions/useGatewayStatus.ts:
```typescript
interface UseGatewayStatusReturn {
  status: GatewayConnectionStatus
  connected: boolean
  isLoading: boolean
}
export function useGatewayStatus(): UseGatewayStatusReturn
```

From frontend/src/components/GatewayStatusDot.tsx:
```typescript
interface GatewayStatusDotProps {
  showLabel?: boolean
  size?: number
}
export const GatewayStatusDot: React.NamedExoticComponent<GatewayStatusDotProps>
```

From frontend/src/lib/query-keys.ts:
```typescript
export const queryKeys = {
  gatewayStatus: ['gateway', 'status'] as const,
  // ...
}
```

OpenClaw health endpoint response shapes (from src-tauri/src/routes/gateway.rs):
- Not configured: `{ "ok": false, "status": "not_configured" }`
- Connected via gateway WS: `{ "ok": true, "status": "connected", "gateway": true }`
- Connected via workspace API: `{ "ok": true, "status": "connected", "gateway": false }`
- Unreachable: `{ "ok": false, "status": "unreachable" }`

Gateway status endpoint response shapes:
- Connected: `{ "ok": true, "status": "connected", "connected": true }`
- Disconnected: `{ "ok": false, "status": "disconnected", "connected": false }`
- Not configured: `{ "ok": false, "status": "not_configured", "connected": false }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test useGatewayStatus hook and OpenClaw health query</name>
  <read_first>
    - frontend/src/hooks/sessions/useGatewayStatus.ts
    - frontend/src/pages/sessions/types.ts
    - frontend/src/lib/demo-data.ts (isDemoMode export)
    - frontend/src/lib/query-keys.ts
    - frontend/src/components/__tests__/ConnectionStatus.test.tsx (test pattern reference)
    - frontend/src/pages/OpenClaw.tsx (lines 361-366 for health query pattern)
    - frontend/src/pages/Agents.tsx (lines 23-29 for health query pattern)
  </read_first>
  <files>
    frontend/src/hooks/sessions/__tests__/useGatewayStatus.test.ts
    frontend/src/hooks/__tests__/useOpenClawHealth.test.ts
  </files>
  <behavior>
    useGatewayStatus.test.ts:
    - Test: returns { status: 'not_configured', connected: false, isLoading: false } in demo mode (isDemoMode returns true), api.get is NOT called
    - Test: returns { status: 'connected', connected: true } when API responds with { connected: true, status: 'connected' }
    - Test: returns { status: 'disconnected', connected: false } when API responds with { connected: false, status: 'disconnected' }
    - Test: returns { status: 'not_configured', connected: false } when API responds with { connected: false, status: 'not_configured' }
    - Test: defaults to { status: 'not_configured', connected: false } when API throws (network error)

    useOpenClawHealth.test.ts:
    - Test: health query returns { ok: true, status: 'connected' } when /api/openclaw/health reports connected
    - Test: health query returns { ok: false, status: 'unreachable' } when gateway is unreachable
    - Test: health query returns { ok: false, status: 'not_configured' } when gateway is not configured
    - Test: health query returns { ok: true, gateway: true } when connected via WS vs { ok: true, gateway: false } when connected via API
  </behavior>
  <action>
    Create `frontend/src/hooks/sessions/__tests__/useGatewayStatus.test.ts`:

    1. Mock `@/lib/api` with `vi.mock` to control `api.get` responses
    2. Mock `@/lib/demo-data` with `vi.mock` to control `isDemoMode()` return value
    3. Create a `createWrapper()` helper that returns a `QueryClientProvider` with `retry: false` (follow pattern from ConnectionStatus.test.tsx)
    4. Use `renderHook` from `@testing-library/react` to test the hook
    5. Write 5 test cases:
       - Demo mode: mock `isDemoMode` to return `true`, verify hook returns not_configured without calling api.get
       - Connected: mock `api.get` to resolve `{ connected: true, status: 'connected' }`, use `waitFor` to verify hook returns connected=true
       - Disconnected: mock `api.get` to resolve `{ connected: false, status: 'disconnected' }`, verify connected=false
       - Not configured: mock `api.get` to resolve `{ connected: false, status: 'not_configured' }`, verify status='not_configured'
       - Network error: mock `api.get` to reject, verify defaults to not_configured/connected=false

    Create `frontend/src/hooks/__tests__/useOpenClawHealth.test.ts`:

    1. This tests the inline `useQuery` pattern used in OpenClaw.tsx and Agents.tsx that calls `/api/openclaw/health`
    2. Create a thin wrapper function that mirrors the query pattern: `useQuery({ queryKey: ['openclaw', 'health'], queryFn: () => api.get('/api/openclaw/health') })`
    3. Mock `@/lib/api` with `vi.mock`
    4. Write 4 test cases:
       - Connected via gateway WS: mock resolves `{ ok: true, status: 'connected', gateway: true }`, verify data.ok is true and data.gateway is true
       - Connected via workspace API: mock resolves `{ ok: true, status: 'connected', gateway: false }`, verify data.ok is true and data.gateway is false
       - Unreachable: mock resolves `{ ok: false, status: 'unreachable' }`, verify data.ok is false
       - Not configured: mock resolves `{ ok: false, status: 'not_configured' }`, verify data.ok is false and data.status is 'not_configured'
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && npx vitest run src/hooks/sessions/__tests__/useGatewayStatus.test.ts src/hooks/__tests__/useOpenClawHealth.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useGatewayStatus" frontend/src/hooks/sessions/__tests__/useGatewayStatus.test.ts
    - grep -q "not_configured" frontend/src/hooks/sessions/__tests__/useGatewayStatus.test.ts
    - grep -q "connected" frontend/src/hooks/sessions/__tests__/useGatewayStatus.test.ts
    - grep -q "disconnected" frontend/src/hooks/sessions/__tests__/useGatewayStatus.test.ts
    - grep -q "demo" frontend/src/hooks/sessions/__tests__/useGatewayStatus.test.ts
    - grep -q "openclaw/health" frontend/src/hooks/__tests__/useOpenClawHealth.test.ts
    - grep -q "unreachable" frontend/src/hooks/__tests__/useOpenClawHealth.test.ts
    - grep -q "not_configured" frontend/src/hooks/__tests__/useOpenClawHealth.test.ts
    - grep -q "gateway.*true" frontend/src/hooks/__tests__/useOpenClawHealth.test.ts
  </acceptance_criteria>
  <done>
    All 9 test cases pass. useGatewayStatus correctly handles connected, disconnected, not_configured, demo mode, and network error. OpenClaw health query correctly handles connected-via-gateway, connected-via-API, unreachable, and not_configured responses.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test GatewayStatusDot component rendering</name>
  <read_first>
    - frontend/src/components/GatewayStatusDot.tsx
    - frontend/src/pages/sessions/types.ts (GATEWAY_STATUS_COLORS, GATEWAY_STATUS_LABELS)
    - frontend/src/hooks/sessions/useGatewayStatus.ts
    - frontend/src/components/__tests__/ConnectionStatus.test.tsx (test pattern reference)
  </read_first>
  <files>frontend/src/components/__tests__/GatewayStatusDot.test.tsx</files>
  <behavior>
    - Test: renders nothing (returns null) while isLoading is true
    - Test: renders a dot with title "Gateway connected" when status is 'connected'
    - Test: renders a dot with title "Gateway disconnected" when status is 'disconnected'
    - Test: renders a dot with title "Gateway not configured" when status is 'not_configured'
    - Test: renders the text label when showLabel=true
    - Test: does NOT render the text label when showLabel is omitted (defaults false)
    - Test: has aria-live="polite" for accessibility
  </behavior>
  <action>
    Create `frontend/src/components/__tests__/GatewayStatusDot.test.tsx`:

    1. Mock `@/hooks/sessions/useGatewayStatus` with `vi.mock` to control the hook return value
    2. Mock `@/pages/sessions/types` to provide `GATEWAY_STATUS_COLORS` and `GATEWAY_STATUS_LABELS` (or let them import normally since they are pure data)
    3. Create a `createWrapper()` helper with `QueryClientProvider` (component uses memo but hook uses React Query internally)
    4. Write 7 test cases:
       - Loading state: mock `useGatewayStatus` to return `{ status: 'connected', connected: true, isLoading: true }`, verify `container.innerHTML` is empty or component returns null (no rendered output)
       - Connected: mock returns `{ status: 'connected', connected: true, isLoading: false }`, verify element with `title="Gateway connected"` exists
       - Disconnected: mock returns `{ status: 'disconnected', connected: false, isLoading: false }`, verify `title="Gateway disconnected"` exists
       - Not configured: mock returns `{ status: 'not_configured', connected: false, isLoading: false }`, verify `title="Gateway not configured"` exists
       - Show label: render with `showLabel={true}`, mock connected, verify text "Gateway connected" is visible in document
       - Hide label by default: render without showLabel, verify label text is NOT in document (only title attr has it)
       - Accessibility: verify `aria-live="polite"` is present on the container span

    NOTE: Since `GatewayStatusDot` imports `useGatewayStatus` internally, mock the hook module directly rather than mocking `api.get`. This isolates the component test from the hook's implementation.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && npx vitest run src/components/__tests__/GatewayStatusDot.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "GatewayStatusDot" frontend/src/components/__tests__/GatewayStatusDot.test.tsx
    - grep -q "isLoading" frontend/src/components/__tests__/GatewayStatusDot.test.tsx
    - grep -q "Gateway connected" frontend/src/components/__tests__/GatewayStatusDot.test.tsx
    - grep -q "Gateway disconnected" frontend/src/components/__tests__/GatewayStatusDot.test.tsx
    - grep -q "aria-live" frontend/src/components/__tests__/GatewayStatusDot.test.tsx
    - grep -q "showLabel" frontend/src/components/__tests__/GatewayStatusDot.test.tsx
  </acceptance_criteria>
  <done>
    All 7 test cases pass. GatewayStatusDot renders correct dot color and title for each gateway state, shows/hides label per prop, returns null while loading, and has aria-live for accessibility.
  </done>
</task>

</tasks>

<verification>
Run the full test suite to ensure no regressions:
```bash
cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -10
```

Verify all 3 new test files exist and pass:
```bash
cd frontend && npx vitest run \
  src/hooks/sessions/__tests__/useGatewayStatus.test.ts \
  src/hooks/__tests__/useOpenClawHealth.test.ts \
  src/components/__tests__/GatewayStatusDot.test.tsx \
  --reporter=verbose
```
</verification>

<success_criteria>
- 3 new test files created
- All new tests pass via `npx vitest run`
- Tests cover: connected, disconnected, unreachable, not_configured states for both /api/openclaw/health and /api/gateway/status
- Tests cover: demo mode bypass in useGatewayStatus
- Tests cover: GatewayStatusDot renders correct visual state for each connection status
- Tests cover: network error graceful degradation
- Full existing test suite still passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/71-gateway-integration-tests/71-01-SUMMARY.md`
</output>
