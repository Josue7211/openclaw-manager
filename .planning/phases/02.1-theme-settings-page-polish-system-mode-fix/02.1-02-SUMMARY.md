---
phase: 02.1-theme-settings-page-polish-system-mode-fix
plan: 02
subsystem: ui
tags: [css-variables, theme-engine, color-hierarchy, migration, tint-derivation]

requires:
  - phase: 02-theming-system
    provides: ThemeStore, theme-engine, applyTheme, theme-definitions, migration v5

provides:
  - 3-tier color hierarchy (accent/secondary/tertiary) types and apply functions
  - deriveSecondaryTints and deriveTertiaryTints for dynamic alpha tint generation
  - Legacy CSS variable aliases (--green, --accent-blue) for zero-regression migration
  - Migration v6 renaming old secondary (blue) overrides to tertiary
  - --glow-opacity CSS variable ready for slider control
  - setTertiaryOverride in theme store

affects: [02.1-03-green-to-secondary-migration, 02.1-04-settings-display-redesign]

tech-stack:
  added: []
  patterns: [3-tier-color-hierarchy, legacy-alias-bridge, dynamic-tint-derivation]

key-files:
  created: []
  modified:
    - frontend/src/lib/themes.ts
    - frontend/src/lib/theme-engine.ts
    - frontend/src/lib/theme-definitions.ts
    - frontend/src/lib/theme-store.ts
    - frontend/src/lib/migrations.ts
    - frontend/src/globals.css
    - frontend/src/lib/__tests__/themes.test.ts
    - frontend/src/lib/__tests__/theme-engine.test.ts
    - frontend/src/lib/__tests__/migrations.test.ts
    - frontend/src/lib/__tests__/theme-store.test.ts

key-decisions:
  - "DEFAULT_SECONDARY changed from blue (#818cf8) to green (#34d399) to match 3-tier hierarchy"
  - "Legacy CSS aliases (--green, --accent-blue) set dynamically by apply functions for zero visual regression"
  - "deriveSecondaryTints generates 22 CSS variables (9 secondary + 13 legacy green/emerald aliases)"
  - "Migration v6 iterates all override entries, not just active theme, for complete rename"

patterns-established:
  - "3-tier color apply pattern: applySecondaryColor/applyTertiaryColor set primary + legacy aliases"
  - "Tint derivation pattern: TINTS array + LEGACY_TINTS array iterated in derive functions"

requirements-completed: [POLISH-11]

duration: 14min
completed: 2026-03-20
---

# Phase 02.1 Plan 02: Color Hierarchy Foundation Summary

**3-tier color hierarchy (accent/secondary/tertiary) with dynamic tint derivation, legacy aliases, migration v6, and --glow-opacity CSS variable**

## Performance

- **Duration:** 14 min
- **Started:** 2026-03-20T00:10:25Z
- **Completed:** 2026-03-20T00:24:37Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments
- Built complete 3-tier color infrastructure: types, apply functions, tint derivation, store mutations
- Zero visual regression via 18 legacy CSS variable aliases bridging old names to new hierarchy
- Migration v6 seamlessly renames existing blue secondary overrides to tertiary across all themes
- --glow-opacity variable replaces hardcoded body::before values (0.10 dark, 0.06 light)

## Task Commits

Each task was committed atomically:

1. **Task 1: Color hierarchy types, functions, and tint derivation** - `ebc4878` (feat)
2. **Task 2: Migration v6 + globals.css variable definitions** - `b015b54` (feat)

## Files Created/Modified
- `frontend/src/lib/themes.ts` - Changed DEFAULT_SECONDARY to green, added DEFAULT_TERTIARY, rewrote applySecondaryColor, added applyTertiaryColor
- `frontend/src/lib/theme-engine.ts` - Added deriveSecondaryTints (22 vars), deriveTertiaryTints (4 vars), updated applyTheme step 4/4b
- `frontend/src/lib/theme-definitions.ts` - Added tertiary field to UserThemeOverrides
- `frontend/src/lib/theme-store.ts` - Added setTertiaryOverride mutation
- `frontend/src/lib/migrations.ts` - Bumped to v6, added secondary->tertiary rename migration
- `frontend/src/globals.css` - Added --secondary-*, --tertiary-*, --glow-opacity variables; light theme fallbacks
- `frontend/src/lib/__tests__/themes.test.ts` - 30 tests for new apply functions and defaults
- `frontend/src/lib/__tests__/theme-engine.test.ts` - Tests for deriveSecondaryTints, deriveTertiaryTints, applyTheme wiring
- `frontend/src/lib/__tests__/migrations.test.ts` - 5 new tests for migration v6 (rename, multiple entries, idempotent, no-op, missing state)
- `frontend/src/lib/__tests__/theme-store.test.ts` - Updated v5 migration test to expect tertiary after v6 chain

## Decisions Made
- DEFAULT_SECONDARY changed from #818cf8 (blue) to #34d399 (green) — aligns with the user's vision where secondary=green (156 refs) and tertiary=blue (31 refs)
- Legacy aliases set dynamically in apply functions rather than CSS-only fallbacks — ensures theme-specific green/blue colors propagate to all legacy references
- deriveSecondaryTints generates both --secondary-a* and --green-400-a*, --emerald-a*, --green-a* aliases from the same hex — single source of truth
- Migration v6 iterates Object.entries(overrides) to rename across all themes, not just active theme

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed theme-store test expecting stale secondary after v6 migration**
- **Found during:** Task 2 (migration v6 implementation)
- **Issue:** Existing theme-store test checked that v5 migration created secondary field, but v6 migration now renames it to tertiary
- **Fix:** Updated assertion to expect tertiary instead of secondary after full migration chain
- **Files modified:** frontend/src/lib/__tests__/theme-store.test.ts
- **Verification:** All 1196 tests pass
- **Committed in:** b015b54 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Test update necessary for correctness after migration chain. No scope creep.

## Issues Encountered
- User's editor linter repeatedly reverted file changes when edited individually, requiring full file rewrites via Write tool for themes.ts and theme-engine.ts to establish consistent export/import state across files

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- 3-tier color infrastructure complete and tested, ready for plan 02.1-03 (green-to-secondary + blue-to-tertiary migration across 57+ component files)
- Legacy aliases ensure zero visual regression during incremental component migration
- --glow-opacity ready for slider control in plan 02.1-04 (Settings Display redesign)

---
*Phase: 02.1-theme-settings-page-polish-system-mode-fix*
*Completed: 2026-03-20*
