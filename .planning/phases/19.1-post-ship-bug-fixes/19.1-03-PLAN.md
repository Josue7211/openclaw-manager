---
phase: 19.1-post-ship-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src-tauri/src/routes/koel.rs
  - src-tauri/src/tailscale.rs
autonomous: true
requirements: [BUG-01, BUG-04, BUG-05, BUG-09]

must_haves:
  truths:
    - "koel.rs is tracked by git (committed)"
    - "Tailscale startup_verify checks OPENCLAW_API_URL against the Services VM expected host"
    - "OpenClaw health check returns ok:true when OPENCLAW_API_URL is correctly configured pointing at Services VM"
    - "The ffir error is documented with investigation findings"
  artifacts:
    - path: "src-tauri/src/routes/koel.rs"
      provides: "Music streaming proxy routes"
    - path: "src-tauri/src/tailscale.rs"
      provides: "Corrected peer verification mapping"
      contains: "OPENCLAW_EXPECTED_HOST"
  key_links:
    - from: "src-tauri/src/tailscale.rs"
      to: "OPENCLAW_API_URL secret"
      via: "startup_verify services array"
      pattern: "OPENCLAW_API_URL.*OPENCLAW_EXPECTED_HOST"
---

<objective>
Commit koel.rs, investigate the ffir error, and verify OpenClaw health check correctness now that architecture is documented correctly (Plan 02 corrected CLAUDE.md).

Purpose: Complete the remaining bug fixes -- commit orphaned code, ensure OpenClaw tabs work when Services VM is configured, and investigate the mystery ffir error.
Output: koel.rs committed, tailscale peer verification consistent with actual architecture, ffir investigation documented.
</objective>

<execution_context>
@/mnt/storage/projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/mnt/storage/projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-post-ship-bug-fixes/19.1-CONTEXT.md
@.planning/phases/19.1-post-ship-bug-fixes/19.1-RESEARCH.md
@.planning/phases/19.1-post-ship-bug-fixes/19.1-02-SUMMARY.md

<interfaces>
<!-- Key types the executor needs -->

From src-tauri/src/routes/mod.rs:
```rust
pub mod koel;  // line 25 -- already declared
.merge(koel::router())  // line 77 -- already wired
```

From src-tauri/src/tailscale.rs (startup_verify services array, ~line 261):
```rust
let services: &[(&str, &str, &str)] = &[
    ("BlueBubbles", "BLUEBUBBLES_HOST", "BLUEBUBBLES_EXPECTED_HOST"),
    ("OpenClaw", "OPENCLAW_API_URL", "OPENCLAW_EXPECTED_HOST"),
];
```

From src-tauri/src/routes/gateway.rs (health check pattern):
```rust
// GET /api/openclaw/health
// Reads OPENCLAW_API_URL from state.secret()
// Probes {url}/health with 5s timeout
// Returns { ok: bool, status: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit koel.rs and investigate ffir error</name>
  <files>src-tauri/src/routes/koel.rs</files>
  <read_first>
    - src-tauri/src/routes/koel.rs (full file -- verify it compiles and is ready to commit)
    - src-tauri/src/routes/mod.rs (confirm koel is already declared and wired)
  </read_first>
  <action>
**koel.rs commit (BUG-11):**
The file `src-tauri/src/routes/koel.rs` already exists, is declared in `routes/mod.rs` (line 25: `pub mod koel;`), and is wired into the router (line 77: `.merge(koel::router())`). It just needs to be staged and committed.

Use the SSH git pattern from project conventions:
```bash
ssh plex-vm 'cd /mnt/storage2/projects/mission-control && git add src-tauri/src/routes/koel.rs && git commit --no-verify -m "feat: commit koel music streaming proxy routes"'
```

**ffir error investigation (BUG-01):**
The string "ffir" does NOT exist anywhere in the codebase. The research confirmed this. This is NOT a code bug in mission-control.

Investigate possible sources:
1. Search the system PATH for any binary named "ffir": run `which ffir 2>/dev/null; find /usr/bin /usr/local/bin ~/.local/bin -name "ffir*" 2>/dev/null`
2. Check if it's a Tauri shell plugin issue: search Tauri's `tauri.conf.json` and capabilities for shell commands. Read `src-tauri/capabilities/` for any shell exec permissions.
3. Check if `ffmpeg` or `ffprobe` is misspelled somewhere in a config file (not source): search `*.toml`, `*.json`, `*.yaml` config files in the project root and src-tauri/ for "ff"
4. Check Hyprland/Waybar/system configs if accessible -- the error may be from a desktop integration hook

Based on findings, document the investigation in the SUMMARY.md:
- If found: fix the source
- If external to the app: document that it's a system-level issue, not an app bug, and note the likely source
- If truly unfindable: document that code analysis confirms zero "ffir" references in the codebase and recommend the user check system startup scripts, shell plugins (zsh/fish), or Hyprland event hooks

The research phase already confirmed with HIGH confidence that "ffir" is not in the codebase. The executor should verify this claim with a quick search, document findings, and move on. Do not spend more than 5 minutes on this investigation.
  </action>
  <verify>
    <automated>cd /mnt/storage/projects/mission-control && git log --oneline -3 | head -5 && cd src-tauri && cargo check --no-default-features 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - git log shows a commit containing "koel" in the message
    - src-tauri/src/routes/koel.rs appears in git ls-files (tracked)
    - cargo check passes (no compile errors)
    - SUMMARY.md documents ffir investigation findings
  </acceptance_criteria>
  <done>koel.rs committed to git. ffir error investigated and documented (likely external to the app). cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify OpenClaw health check and tailscale peer verification point at Services VM</name>
  <files>src-tauri/src/tailscale.rs</files>
  <read_first>
    - src-tauri/src/tailscale.rs (lines 250-300 -- startup_verify function with services array)
    - src-tauri/src/routes/gateway.rs (first 60 lines -- understand health check implementation)
    - src-tauri/src/routes/openclaw_data.rs (first 40 lines -- understand how tabs fetch data)
    - frontend/src/pages/OpenClaw.tsx (lines 340-370 -- understand health check query and tab gating)
    - frontend/src/pages/openclaw/ModelsTab.tsx (first 30 lines -- understand health gate pattern)
  </read_first>
  <action>
**Verify OpenClaw health check correctness (BUG-04/05):**
The root cause of Bugs 4 and 5 is that `OPENCLAW_API_URL` must point to the Services VM (where LiteLLM and the OpenClaw gateway actually run), NOT the OpenClaw VM (which only runs Moonlight/Sunshine). Plan 02 corrected the CLAUDE.md documentation. This task verifies the code is consistent.

1. Read `src-tauri/src/routes/gateway.rs` -- confirm the health check probes `{OPENCLAW_API_URL}/health`. This is correct behavior. The URL just needs to be configured to point at Services VM. No code change needed in gateway.rs.

2. Read `src-tauri/src/tailscale.rs` startup_verify (around line 261). The current services array has:
   ```rust
   ("OpenClaw", "OPENCLAW_API_URL", "OPENCLAW_EXPECTED_HOST"),
   ```
   This is correct -- `OPENCLAW_EXPECTED_HOST` is the expected Tailscale hostname for wherever `OPENCLAW_API_URL` points. The user configures both in Settings > Connections. The peer verification will warn if the URL IP doesn't match the expected hostname.

   **Verify no code change is needed** here. The verification system is generic -- it checks whatever the user configures. The documentation fix in Plan 02 tells the user the correct VM to point at.

3. Confirm that BUG-09 (Models page shows static cards) is automatically resolved: ModelsTab.tsx checks the `healthy` prop. When health returns `ok: true` (which happens when `OPENCLAW_API_URL` points at the correct Services VM and the service responds), ModelsTab fetches real data from `/api/openclaw/models`. No code change needed -- fixing the URL config fixes the data.

4. Confirm that BUG-08 (Skills/Marketplace references) requires no action: the research already confirmed zero references exist in the codebase. Verify with a quick grep.

**If tailscale.rs needs NO changes** (which is the expected outcome based on research), document this in the SUMMARY with reasoning: "The health check code is generic and correct. The bug was a misconfiguration (URL pointing at wrong VM), not a code issue. The CLAUDE.md correction in Plan 02 guides users to the correct configuration."

**If tailscale.rs DOES need a change** (e.g., adding a comment clarifying which VM OpenClaw runs on), make the minimal change.

**Action summary:** This task is primarily a verification task. Read the code, confirm correctness, document findings. If code changes are needed, make them. If not, document why no changes are needed.
  </action>
  <verify>
    <automated>cd /mnt/storage/projects/mission-control/src-tauri && cargo check --no-default-features 2>&1 | tail -5 && cargo test routes::gateway --no-default-features 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo check passes with no errors
    - gateway.rs health check probes {OPENCLAW_API_URL}/health (confirmed via code read, no change needed)
    - tailscale.rs startup_verify includes ("OpenClaw", "OPENCLAW_API_URL", "OPENCLAW_EXPECTED_HOST") tuple
    - No references to "skills", "marketplace", or "plugins" exist in frontend/src/ (grep returns 0 matches)
    - SUMMARY.md documents that BUG-04, BUG-05, BUG-08, BUG-09 are resolved (BUG-08 already clean, others are config issues fixed by documentation)
  </acceptance_criteria>
  <done>OpenClaw health check code verified correct. Bugs 4/5 are configuration issues (wrong VM URL), not code bugs. Bug 8 already clean. Bug 9 auto-resolved when health check passes. All documented in SUMMARY.</done>
</task>

</tasks>

<verification>
- `git log --oneline -5` shows koel.rs commit
- `cd src-tauri && cargo check --no-default-features` passes
- `cd frontend && npx vitest run` -- all tests pass
- `grep -ri 'skills\|marketplace\|plugins' frontend/src/` returns zero matches
- SUMMARY.md documents all investigation findings
</verification>

<success_criteria>
1. koel.rs is committed and tracked by git
2. ffir error investigated and documented (likely external to app)
3. OpenClaw health check verified correct (configuration issue, not code issue)
4. No skills/marketplace/plugins references in codebase (confirmed clean)
5. All Rust code compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-post-ship-bug-fixes/19.1-03-SUMMARY.md`
</output>
