---
phase: 65-strip-unused-file-exports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/utils.ts
  - frontend/src/lib/vault.ts
  - frontend/src/lib/constants.ts
  - frontend/src/lib/event-bus.ts
  - frontend/src/lib/demo-data.ts
  - frontend/src/lib/bjorn-types.ts
  - frontend/src/lib/modules.ts
  - frontend/src/lib/keybindings.ts
  - frontend/src/lib/themes.ts
  - frontend/src/lib/unread-store.ts
  - frontend/src/lib/wizard-store.ts
  - frontend/src/lib/webauthn.ts
  - frontend/src/components/NotificationCenter.tsx
  - frontend/src/components/messages/ReactionPills.tsx
  - frontend/src/components/primitives/shared.tsx
  - frontend/src/components/dashboard/WidgetWrapper.tsx
  - frontend/src/hooks/useUserSecrets.ts
  - frontend/src/hooks/useBudgetAlerts.ts
  - frontend/src/lib/hooks/usePageTitle.ts
  - frontend/src/pages/notes/EditorToolbar.tsx
autonomous: true
requirements: [DEAD-01]

must_haves:
  truths:
    - "knip reports zero unused exports from library utility files and component files"
    - "All existing tests pass after export removal"
    - "tsc --noEmit passes after all removals"
  artifacts:
    - path: "frontend/src/lib/vault.ts"
      provides: "Vault library with no unused exports"
    - path: "frontend/src/components/NotificationCenter.tsx"
      provides: "NotificationCenter with only addNotification exported"
    - path: "frontend/src/pages/notes/EditorToolbar.tsx"
      provides: "EditorToolbar exporting only toggleWrap and insertLink"
  key_links:
    - from: "frontend/src/hooks/messages/useMessagesSSE.ts"
      to: "frontend/src/components/NotificationCenter.tsx"
      via: "import { addNotification }"
      pattern: "addNotification"
    - from: "frontend/src/pages/notes/NoteEditor.tsx"
      to: "frontend/src/pages/notes/EditorToolbar.tsx"
      via: "import { toggleWrap, insertLink }"
      pattern: "toggleWrap.*insertLink"
---

<objective>
Remove unused exports from library utility files, component files, and hook files.

Purpose: Eliminate 57 unused exports (40 value exports + 17 type exports) from miscellaneous library and component files that knip identified as dead code.

Output: All library, component, and hook files export only symbols that are actually consumed by at least one importer.
</objective>

<execution_context>
@/home/josue/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/knip.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused exports from library utility files</name>
  <read_first>
    frontend/src/lib/utils.ts
    frontend/src/lib/vault.ts
    frontend/src/lib/constants.ts
    frontend/src/lib/event-bus.ts
    frontend/src/lib/demo-data.ts
    frontend/src/lib/bjorn-types.ts
    frontend/src/lib/modules.ts
    frontend/src/lib/keybindings.ts
    frontend/src/lib/themes.ts
    frontend/src/lib/unread-store.ts
    frontend/src/lib/wizard-store.ts
    frontend/src/lib/webauthn.ts
  </read_first>
  <files>
    frontend/src/lib/utils.ts
    frontend/src/lib/vault.ts
    frontend/src/lib/constants.ts
    frontend/src/lib/event-bus.ts
    frontend/src/lib/demo-data.ts
    frontend/src/lib/bjorn-types.ts
    frontend/src/lib/modules.ts
    frontend/src/lib/keybindings.ts
    frontend/src/lib/themes.ts
    frontend/src/lib/unread-store.ts
    frontend/src/lib/wizard-store.ts
    frontend/src/lib/webauthn.ts
  </files>
  <action>
For each file, remove the `export` keyword from the flagged items. If the item is not used internally in the file either, delete it entirely.

**utils.ts:**
- `formatDate` function -- delete entirely (other formatDate functions in email/types.ts and pipeline/utils.ts are separate, unrelated)

**vault.ts:**
- `IMAGE_EXTENSIONS` constant -- de-export or delete (check if used internally in isImageFile)
- `isImageFile` function -- de-export or delete (check if used internally)
- `getNote` function -- de-export or delete
- `renameNote` function -- de-export or delete
- `extractWikilinks` function -- de-export or delete
- `extractTags` function -- de-export or delete

**constants.ts:**
- `MISSION_STATUS` constant -- de-export (check if used internally; if not, delete)

**event-bus.ts:**
- `EventType` type -- de-export (likely used internally by AppEvent)
- `AppEvent` interface -- de-export (likely used internally)

**demo-data.ts:**
- `DemoConversation` interface -- de-export or delete
- `DemoChatMessage` interface -- de-export or delete
- `DemoAgentStatus` interface -- de-export or delete
- `DemoAgentInfo` interface -- de-export or delete

**bjorn-types.ts:**
- `BridgeRequest` interface -- de-export or delete
- `BridgeResponse` interface -- de-export or delete

**modules.ts:**
- `AppModule` interface -- de-export (likely used internally)

**keybindings.ts:**
- `ModifierKey` type -- de-export (likely used internally)
- `Keybinding` interface -- de-export (likely used internally)

**themes.ts:**
- `AccentPreset` interface -- de-export (likely used internally)

**unread-store.ts:**
- `UnreadCounts` interface -- de-export (likely used internally)

**wizard-store.ts:**
- `WizardState` interface -- de-export (likely used internally)

**webauthn.ts:**
- `WebAuthnRegistrationResponse` interface -- de-export or delete
- `WebAuthnAuthenticationResponse` interface -- de-export or delete

For each: read the file, check if the symbol is referenced internally, then either remove `export` or delete the declaration entirely. Run `tsc --noEmit` after processing all files.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "export.*function formatDate" frontend/src/lib/utils.ts returns no matches
    - grep "export.*IMAGE_EXTENSIONS\|export.*isImageFile\|export.*getNote\b\|export.*renameNote\|export.*extractWikilinks\|export.*extractTags" frontend/src/lib/vault.ts returns no matches
    - grep "export.*MISSION_STATUS" frontend/src/lib/constants.ts returns no matches
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>All 26 library utility exports de-exported or removed. tsc --noEmit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused exports from component and hook files</name>
  <read_first>
    frontend/src/components/NotificationCenter.tsx
    frontend/src/components/messages/ReactionPills.tsx
    frontend/src/components/primitives/shared.tsx
    frontend/src/components/dashboard/WidgetWrapper.tsx
    frontend/src/hooks/useUserSecrets.ts
    frontend/src/hooks/useBudgetAlerts.ts
    frontend/src/lib/hooks/usePageTitle.ts
    frontend/src/pages/notes/EditorToolbar.tsx
  </read_first>
  <files>
    frontend/src/components/NotificationCenter.tsx
    frontend/src/components/messages/ReactionPills.tsx
    frontend/src/components/primitives/shared.tsx
    frontend/src/components/dashboard/WidgetWrapper.tsx
    frontend/src/hooks/useUserSecrets.ts
    frontend/src/hooks/useBudgetAlerts.ts
    frontend/src/lib/hooks/usePageTitle.ts
    frontend/src/pages/notes/EditorToolbar.tsx
  </files>
  <action>
**NotificationCenter.tsx** -- remove `export` from (keep as internal if used within the component):
- `markRead` function (NOTE: this is different from `markRead` in unread-store.ts -- only the NotificationCenter one is unused)
- `clearNotifications` function
- `useNotifications` function
- `NotificationType` type
- `Notification` interface
- Keep `addNotification` exported -- it IS imported by `useMessagesSSE.ts`

**ReactionPills.tsx:**
- `REACTION_EMOJI` constant -- remove `export` (keep if used internally in the same file)

**primitives/shared.tsx:**
- `PrimitiveErrorFallback` function -- remove `export` or delete (check internal usage)

**WidgetWrapper.tsx:**
- `WidgetWrapperProps` interface -- remove `export` (keep as non-exported if used internally by the component)

**useUserSecrets.ts:**
- `useSecretsList` function -- remove `export` or delete
- `useSecret` function -- remove `export` or delete
- `useDeleteSecret` function -- remove `export` or delete

**useBudgetAlerts.ts:**
- `BudgetAlert` interface -- remove `export` (keep if used internally)

**usePageTitle.ts:**
- `useEditablePageTitle` function -- remove `export` or delete
- `usePageSubtitle` function -- remove `export` or delete

**EditorToolbar.tsx:**
- Change the export line from:
  `export { toggleWrap, insertLink, toggleLinePrefix, insertTable, exportMarkdown }`
  to:
  `export { toggleWrap, insertLink }`
- Keep `toggleLinePrefix`, `insertTable`, `exportMarkdown` as internal (non-exported) functions if used within the component, or delete if not used at all.

Run `tsc --noEmit` after all changes.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "export.*markRead\|export.*clearNotifications\|export.*useNotifications" frontend/src/components/NotificationCenter.tsx returns no matches (but addNotification IS still exported)
    - grep "export.*REACTION_EMOJI" frontend/src/components/messages/ReactionPills.tsx returns no matches
    - grep "export.*useSecretsList\|export.*useSecret\b\|export.*useDeleteSecret" frontend/src/hooks/useUserSecrets.ts returns no matches
    - grep "toggleLinePrefix\|insertTable\|exportMarkdown" frontend/src/pages/notes/EditorToolbar.tsx export line does NOT include those 3 names
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>All 17 component/hook exports de-exported or removed. tsc --noEmit passes.</done>
</task>

<task type="auto">
  <name>Task 3: Final knip verification and test run</name>
  <files>None (verification only)</files>
  <action>
Run the full knip unused exports scan and vitest suite to confirm zero regressions:

1. `cd frontend && npx knip --include exports,types 2>&1` -- should report zero unused exports (or only items from files NOT modified in this plan)
2. `cd frontend && npx vitest run 2>&1 | tail -20` -- all tests pass
3. `cd frontend && npx tsc --noEmit` -- final type check

If any knip items remain that are from files modified in this plan, fix them. If items remain from OTHER files not in this plan's scope, note them in the summary but do not fix (they would be from Plan 01's scope or false positives).
  </action>
  <verify>
    <automated>cd frontend && npx knip --include exports,types 2>&1 | grep -c "Unused" && cd frontend && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - npx knip --include exports,types reports zero unused items from files touched by this plan
    - npx vitest run passes with no failures
    - tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Zero knip unused export warnings from Plan 02 files. All tests green. TypeScript clean.</done>
</task>

</tasks>

<verification>
```bash
cd frontend && npx knip --include exports,types 2>&1
cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -10
cd frontend && npx tsc --noEmit
```
</verification>

<success_criteria>
- tsc --noEmit passes with zero errors
- All existing tests pass (npx vitest run)
- knip reports zero unused exports from files modified in this plan
- No runtime import errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/65-strip-unused-file-exports/65-02-SUMMARY.md`
</output>
