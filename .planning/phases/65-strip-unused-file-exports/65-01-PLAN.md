---
phase: 65-strip-unused-file-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/hooks/dashboard/index.ts
  - frontend/src/lib/dashboard-store.ts
  - frontend/src/lib/dashboard-defaults.ts
  - frontend/src/lib/home-store.ts
  - frontend/src/lib/home-defaults.ts
  - frontend/src/lib/theme-engine.ts
  - frontend/src/lib/theme-store.ts
  - frontend/src/lib/theme-scheduling.ts
  - frontend/src/lib/tour-store.ts
  - frontend/src/lib/sidebar-config.ts
  - frontend/src/lib/animation-intensity.ts
  - frontend/src/lib/google-fonts.ts
autonomous: true
requirements: [DEAD-01]

must_haves:
  truths:
    - "knip reports zero unused exports from dashboard barrel file, store modules, and engine modules"
    - "All existing tests pass after export removal"
    - "tsc --noEmit passes after each batch of removals"
  artifacts:
    - path: "frontend/src/lib/hooks/dashboard/index.ts"
      provides: "Barrel file with only consumed re-exports"
    - path: "frontend/src/lib/dashboard-store.ts"
      provides: "Store with no unused exported functions"
    - path: "frontend/src/lib/home-store.ts"
      provides: "Store with no unused exported functions or types"
  key_links:
    - from: "frontend/src/components/widgets/*.tsx"
      to: "frontend/src/lib/hooks/dashboard/*.ts"
      via: "Direct file imports (not barrel)"
      pattern: "from.*hooks/dashboard/use"
---

<objective>
Remove unused exports from the dashboard barrel file, store modules, and engine modules.

Purpose: Eliminate 39 unused exports (19 barrel re-exports + 20 store/engine functions and types) that knip identified as dead code. These are functions and types exported but never imported by any consumer.

Output: Clean store/engine files with no unused exports. Dashboard barrel file trimmed to only re-exports that are actually consumed via the barrel path.
</objective>

<execution_context>
@/home/josue/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/knip.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused barrel re-exports from dashboard/index.ts</name>
  <read_first>frontend/src/lib/hooks/dashboard/index.ts</read_first>
  <files>frontend/src/lib/hooks/dashboard/index.ts</files>
  <action>
Remove these 19 unused re-exports from `index.ts`. These hooks/types are imported directly from individual files by their widget consumers, making these barrel re-exports dead:

**Hooks to remove from index.ts (keep the individual files -- only the re-export lines are dead):**
- `useAgentCache` (line 1 -- NOTE: `useAgentCacheSSE` on the same line IS used, keep it. Change to: `export { useAgentCacheSSE } from './useAgentCache'`)
- `useTodosWidget` (line 10)
- `useCalendarWidget` (line 11)
- `useRemindersWidget` (line 12)
- `useRecentNotes` (line 15)
- `usePipelineIdeas` (line 16)
- `usePipelineStatus` (line 17)
- `useInboxWidget` (line 18)
- `useHomelabWidget` (line 19)
- `useMediaWidget` (line 20)
- `useMusicWidget` (line 21)
- `useMessagesSummary` (line 22)
- `useChatSummary` (line 23)
- `useActivityFeed` (line 24)
- `useQuickCapture` (line 26)
- `useSystemInfo` (line 28)

**Types to remove from index.ts:**
- `ActivityItem` (line 25)
- `CaptureRoute` (line 27)
- `SystemService` (line 29)

After editing, the file should only contain re-exports that are consumed via the barrel path (`from '@/lib/hooks/dashboard'`):
`useAgentCacheSSE`, `useAgentStatus`, `useHeartbeat`, `useSessions`, `useSubagentData`, `useAgentsData`, `useMissions`, `useIdeas`, `useMemoryEntries`, `useKnowledgeWidget`, `usePomodoroWidget`.

Do NOT delete the individual hook files -- only trim the barrel re-export lines.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export" frontend/src/lib/hooks/dashboard/index.ts returns exactly 11
    - grep "useTodosWidget\|useCalendarWidget\|useRemindersWidget\|useRecentNotes\|usePipelineIdeas\|usePipelineStatus\|useInboxWidget\|useHomelabWidget\|useMediaWidget\|useMusicWidget\|useMessagesSummary\|useChatSummary\|useActivityFeed\|useQuickCapture\|useSystemInfo\|ActivityItem\|CaptureRoute\|SystemService\|useAgentCache[^S]" frontend/src/lib/hooks/dashboard/index.ts returns no matches
  </acceptance_criteria>
  <done>Dashboard barrel file contains only 11 consumed re-exports. tsc --noEmit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused store and engine exports</name>
  <read_first>
    frontend/src/lib/dashboard-store.ts
    frontend/src/lib/dashboard-defaults.ts
    frontend/src/lib/home-store.ts
    frontend/src/lib/home-defaults.ts
    frontend/src/lib/theme-engine.ts
    frontend/src/lib/theme-store.ts
    frontend/src/lib/theme-scheduling.ts
    frontend/src/lib/tour-store.ts
    frontend/src/lib/sidebar-config.ts
    frontend/src/lib/animation-intensity.ts
    frontend/src/lib/google-fonts.ts
  </read_first>
  <files>
    frontend/src/lib/dashboard-store.ts
    frontend/src/lib/dashboard-defaults.ts
    frontend/src/lib/home-store.ts
    frontend/src/lib/home-defaults.ts
    frontend/src/lib/theme-engine.ts
    frontend/src/lib/theme-store.ts
    frontend/src/lib/theme-scheduling.ts
    frontend/src/lib/tour-store.ts
    frontend/src/lib/sidebar-config.ts
    frontend/src/lib/animation-intensity.ts
    frontend/src/lib/google-fonts.ts
  </files>
  <action>
Remove the `export` keyword (or delete the function/type/constant entirely if it is only used locally or not at all) for each of these knip-flagged unused exports:

**dashboard-store.ts** -- remove `export` from (keep as private if used internally, delete if truly dead):
- `setDotIndicatorsEnabled` function
- `reorderPages` function
- `resetPageLayout` function
- `resetAllLayouts` function

**dashboard-defaults.ts** -- remove `export` from:
- `LayoutItem` interface (check if used internally in the same file -- if yes, keep as non-exported; if no, delete)
- `DefaultLayoutResult` interface (same check)

**home-store.ts** -- remove `export` from:
- `subscribeHome` function
- `setHomeWobbleEnabled` function
- `redoHome` function
- `resetHomeLayout` function
- Re-exported types: `DashboardState`, `DashboardPage`, `LayoutItem`, `RecycleBinItem` (these are re-exports from dashboard-store that nobody imports via home-store)

**home-defaults.ts** -- remove `export` from:
- `HOME_DEFAULT_ORDER` constant
- `HomeDefaultLayoutResult` interface

**theme-engine.ts** -- remove `export` from:
- `mapGtkThemeToPreset` function
- `applyAdvancedOverrides` function
- `SystemThemeInfo` interface

**theme-store.ts** -- remove `export` from:
- `getLastClickEvent` function
- `clearLastClickEvent` function

**theme-scheduling.ts** -- remove `export` from:
- `stopScheduleTimer` function

**tour-store.ts** -- remove `export` from:
- `getTourState` function
- `subscribeTour` function
- `isTourActive` function
- `startTour` function
- `endTour` function

**sidebar-config.ts** -- remove `export` from:
- `getCollapsedCategories` function
- `SidebarCategory` interface
- `CustomModule` interface
- `DeletedItem` interface
- `SidebarConfig` interface

**animation-intensity.ts** -- remove `export` from:
- `useAnimationIntensity` function
- `AnimationLevel` type

**google-fonts.ts** -- remove `export` from:
- `isGoogleFontLoaded` function
- `GoogleFont` interface

For each item: if the function/type IS used internally in the same file, just remove `export`. If it is NOT used internally, delete the function/type entirely. Run `tsc --noEmit` after each file group to catch breakage early.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "export.*setDotIndicatorsEnabled\|export.*reorderPages\|export.*resetPageLayout\|export.*resetAllLayouts" frontend/src/lib/dashboard-store.ts returns no matches
    - grep "export.*subscribeHome\|export.*setHomeWobbleEnabled\|export.*redoHome\|export.*resetHomeLayout" frontend/src/lib/home-store.ts returns no matches
    - grep "export.*mapGtkThemeToPreset\|export.*applyAdvancedOverrides" frontend/src/lib/theme-engine.ts returns no matches
    - grep "export.*getTourState\|export.*subscribeTour\|export.*isTourActive\|export.*startTour\|export.*endTour\b" frontend/src/lib/tour-store.ts returns no matches
    - grep "export.*getCollapsedCategories" frontend/src/lib/sidebar-config.ts returns no matches
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>All 39 store/engine exports de-exported or removed. tsc --noEmit passes. No test regressions.</done>
</task>

</tasks>

<verification>
```bash
cd frontend && npx tsc --noEmit
cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -5
cd frontend && npx knip --include exports,types 2>&1 | grep -E "dashboard/index|dashboard-store|home-store|theme-engine|theme-store|theme-scheduling|tour-store|sidebar-config|animation-intensity|google-fonts|dashboard-defaults|home-defaults"
```
</verification>

<success_criteria>
- tsc --noEmit passes with zero errors
- All existing tests pass
- knip reports zero unused exports from files modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/65-strip-unused-file-exports/65-01-SUMMARY.md`
</output>
