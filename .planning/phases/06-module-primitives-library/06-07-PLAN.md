---
phase: 06-module-primitives-library
plan: 07
type: execute
wave: 4
depends_on: [06-02, 06-03, 06-04, 06-05, 06-06]
files_modified:
  - frontend/src/components/primitives/register.ts
  - frontend/src/components/primitives/__tests__/schemas.test.ts
  - frontend/src/components/primitives/__tests__/integration.test.tsx
  - frontend/src/components/primitives/__tests__/error-handling.test.tsx
  - frontend/src/main.tsx
autonomous: true
requirements: [PRIM-12, PRIM-13, PRIM-14]

must_haves:
  truths:
    - "All 11 primitives export a valid configSchema with typed fields"
    - "All 11 primitives are registered in the Widget Registry and resolvable via getWidget()"
    - "All 11 primitives render inside WidgetWrapper without crashing (error boundary isolation)"
    - "All 11 primitives handle empty/malformed config gracefully (no thrown errors, shows fallback UI)"
    - "registerPrimitives() is called at app startup so primitives appear in Widget Picker"
  artifacts:
    - path: "frontend/src/components/primitives/__tests__/schemas.test.ts"
      provides: "Cross-cutting schema validation for all 11 primitives"
      min_lines: 30
    - path: "frontend/src/components/primitives/__tests__/integration.test.tsx"
      provides: "Widget Registry integration test for all 11 primitives"
      min_lines: 30
    - path: "frontend/src/components/primitives/__tests__/error-handling.test.tsx"
      provides: "Error handling test: empty config, malformed config, wrong types"
      min_lines: 50
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/components/primitives/register.ts"
      via: "registerPrimitives() call at app startup"
      pattern: "registerPrimitives"
    - from: "frontend/src/components/primitives/register.ts"
      to: "frontend/src/lib/widget-registry.ts"
      via: "registerWidget() for all 11 primitives"
      pattern: "registerWidget"
    - from: "frontend/src/components/primitives/__tests__/integration.test.tsx"
      to: "frontend/src/components/dashboard/WidgetWrapper.tsx"
      via: "Renders primitives inside WidgetWrapper"
      pattern: "WidgetWrapper"
---

<objective>
Wire all 11 primitives into the app, verify cross-cutting requirements (PRIM-12, PRIM-13, PRIM-14), and run integration tests confirming every primitive works end-to-end.

Purpose: This plan ensures the primitive library is a cohesive whole -- every primitive has a schema, every primitive is registered and launchable from the Widget Picker, and every primitive handles bad input gracefully. It also wires registerPrimitives() into the app entry point.

Output: Integration tests proving all cross-cutting requirements, app wiring complete.
</objective>

<execution_context>
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-module-primitives-library/06-CONTEXT.md
@.planning/phases/06-module-primitives-library/06-RESEARCH.md
@.planning/phases/06-module-primitives-library/06-01-SUMMARY.md
@.planning/phases/06-module-primitives-library/06-02-SUMMARY.md
@.planning/phases/06-module-primitives-library/06-03-SUMMARY.md
@.planning/phases/06-module-primitives-library/06-04-SUMMARY.md
@.planning/phases/06-module-primitives-library/06-05-SUMMARY.md
@.planning/phases/06-module-primitives-library/06-06-SUMMARY.md

@frontend/src/components/primitives/register.ts
@frontend/src/lib/widget-registry.ts
@frontend/src/components/dashboard/WidgetWrapper.tsx
@frontend/src/main.tsx

<interfaces>
<!-- All 11 primitive modules: -->
```typescript
// Each exports:
export const configSchema: WidgetConfigSchema
export default React.memo(function PrimitiveName(props: WidgetProps): JSX.Element)
```

<!-- Primitive IDs in registry: -->
// prim-stat-card, prim-progress-gauge, prim-markdown
// prim-line-chart, prim-bar-chart
// prim-list-view, prim-data-table
// prim-form, prim-kanban
// prim-timer, prim-image-gallery

<!-- From widget-registry.ts: -->
```typescript
export function getWidget(id: string): WidgetDefinition | undefined
export function getWidgetsByCategory(): Record<string, WidgetDefinition[]>
export function registerWidget(def: WidgetDefinition): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize register.ts and wire into main.tsx</name>
  <files>
    frontend/src/components/primitives/register.ts,
    frontend/src/main.tsx
  </files>
  <action>
    **register.ts:**
    - Verify all 11 primitives are registered. The file should have exactly 11 registerWidget() calls. Each with:
      - Unique `id` prefixed with `prim-`
      - `category: 'primitives'`
      - `tier: 'builtin'`
      - Appropriate `defaultSize` and `minSize`
      - `configSchema` imported from the respective component module
      - `component: () => import('@/components/primitives/XYZ')` for lazy loading
      - Icon name from @phosphor-icons/react
    - Complete list:
      1. `prim-stat-card` -- ChartLineUp, {w:3,h:2}, min {w:2,h:2}
      2. `prim-progress-gauge` -- Gauge, {w:3,h:2}, min {w:2,h:1}
      3. `prim-markdown` -- Notepad, {w:4,h:3}, min {w:2,h:2}
      4. `prim-line-chart` -- ChartLine, {w:4,h:3}, min {w:3,h:2}
      5. `prim-bar-chart` -- ChartBar, {w:4,h:3}, min {w:3,h:2}
      6. `prim-list-view` -- List, {w:3,h:4}, min {w:2,h:3}
      7. `prim-data-table` -- Table, {w:4,h:4}, min {w:3,h:3}
      8. `prim-form` -- TextAa, {w:3,h:4}, min {w:2,h:3}
      9. `prim-kanban` -- Kanban, {w:6,h:4}, min {w:4,h:3}
      10. `prim-timer` -- Timer, {w:2,h:2}, min {w:2,h:2}
      11. `prim-image-gallery` -- Images, {w:4,h:3}, min {w:2,h:2}
    - Import configSchema from each primitive file to pass into the registration

    **main.tsx:**
    - Add `import { registerPrimitives } from '@/components/primitives/register'` at the top (with other imports)
    - Call `registerPrimitives()` before the React root is created (before `ReactDOM.createRoot()`). This ensures primitives are in the registry before any component renders.
    - Place it near other initialization code (theme application, etc.)
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    - register.ts has 11 registerWidget calls, one per primitive
    - main.tsx calls registerPrimitives() at startup
    - TypeScript compiles with no errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-cutting test suites for PRIM-12, PRIM-13, PRIM-14</name>
  <files>
    frontend/src/components/primitives/__tests__/schemas.test.ts,
    frontend/src/components/primitives/__tests__/integration.test.tsx,
    frontend/src/components/primitives/__tests__/error-handling.test.tsx
  </files>
  <behavior>
    schemas.test.ts (PRIM-12):
    - Every primitive module exports a configSchema object
    - Every configSchema has a non-empty fields array
    - Every field has key (string), label (string), type (one of 5 valid types), default (not undefined)
    - Every select field has a non-empty options array
    - No duplicate field keys within a schema

    integration.test.tsx (PRIM-13):
    - After registerPrimitives(), all 11 primitive IDs are resolvable via getWidget()
    - Each resolved widget has category 'primitives'
    - Each resolved widget has a component function (lazy loader)
    - getWidgetsByCategory() includes a 'primitives' key with 11 entries

    error-handling.test.tsx (PRIM-14):
    - Each primitive renders without throwing when given empty config ({})
    - Each primitive renders without throwing when given malformed config (wrong types: {data: "not an array", value: {}, title: 42})
    - Each primitive shows EmptyState or PrimitiveErrorFallback (not a blank render) when given empty config
    - No primitive throws an unhandled error that would crash the parent
  </behavior>
  <action>
    **schemas.test.ts:**
    - Import configSchema from all 11 primitive modules
    - Create a test suite "Primitive Config Schemas (PRIM-12)" with tests:
      - describe.each over all 11 primitives: { name, schema }
      - "exports a configSchema object" -- schema is truthy
      - "has non-empty fields array" -- schema.fields.length > 0
      - "all fields have required properties" -- iterate fields, check key/label/type/default
      - "field types are valid" -- type is one of ['text', 'number', 'toggle', 'select', 'slider']
      - "select fields have options" -- filter by type=select, check options array length > 0
      - "no duplicate field keys" -- new Set(keys).size === keys.length

    **integration.test.tsx:**
    - Import registerPrimitives from register.ts
    - Import getWidget, getWidgetsByCategory from widget-registry
    - Call registerPrimitives() in beforeAll
    - Test suite "Primitive Widget Registration (PRIM-13)":
      - describe.each over all 11 primitive IDs
      - "is registered in Widget Registry" -- getWidget(id) is not undefined
      - "has category 'primitives'" -- getWidget(id).category === 'primitives'
      - "has a component loader function" -- typeof getWidget(id).component === 'function'
      - "has valid defaultSize" -- defaultSize.w > 0 && defaultSize.h > 0
      - "has valid minSize" -- minSize.w > 0 && minSize.h > 0
      - "getWidgetsByCategory includes all primitives" -- result['primitives'].length === 11

    **error-handling.test.tsx:**
    - Import all 11 primitive components (direct import, not lazy)
    - Import render from @testing-library/react
    - Test suite "Primitive Error Handling (PRIM-14)":
      - describe.each over all 11 primitives: { name, Component }
      - "renders without throwing on empty config" -- render(<Component widgetId="test" config={{}} isEditMode={false} size={{w:4,h:3}} />) does not throw
      - "renders without throwing on malformed config" -- render with { data: "wrong", value: {}, title: 42, items: "nope" } does not throw
      - "shows fallback UI on empty config" -- rendered output contains EmptyState text or PrimitiveErrorFallback (query for role="status" or specific fallback text)
      - "does not render blank on bad config" -- rendered container has child content (not just an empty div)
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run src/components/primitives/__tests__/schemas.test.ts src/components/primitives/__tests__/integration.test.tsx src/components/primitives/__tests__/error-handling.test.tsx -x</automated>
  </verify>
  <done>
    - schemas.test.ts: All 11 primitives have valid configSchema with properly typed fields
    - integration.test.tsx: All 11 primitives registered and resolvable, all in 'primitives' category
    - error-handling.test.tsx: All 11 primitives handle empty/malformed config gracefully
    - Zero test failures across all 3 cross-cutting suites
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/components/primitives/__tests__/ -x` -- ALL primitive tests pass (individual + cross-cutting)
- `cd frontend && npx vitest run` -- full test suite green (no regressions)
- `cd frontend && npx tsc --noEmit` -- no type errors
- Widget Picker shows 'Primitives' category with 11 entries
</verification>

<success_criteria>
1. All 11 primitives registered in Widget Registry under 'primitives' category
2. registerPrimitives() called at app startup via main.tsx
3. schemas.test.ts proves every primitive has a valid configSchema (PRIM-12)
4. integration.test.tsx proves every primitive is widget-compatible (PRIM-13)
5. error-handling.test.tsx proves every primitive handles bad config (PRIM-14)
6. Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-module-primitives-library/06-07-SUMMARY.md`
</output>
