---
phase: 66-strip-unused-npm-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.ts
  - frontend/knip.json
autonomous: true
requirements: [DEAD-02]

must_haves:
  truths:
    - "npx knip --include dependencies reports zero unused dependencies"
    - "npm run build produces a working bundle with no missing module errors"
    - "knip ignoreDependencies contains only entries knip cannot detect (CSS imports)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Clean dependency list with no dead packages"
      contains: "react"
    - path: "frontend/knip.json"
      provides: "Accurate knip config with minimal ignoreDependencies"
      contains: "tailwindcss"
  key_links:
    - from: "frontend/src/globals.css"
      to: "tailwindcss"
      via: "@import tailwindcss/preflight"
      pattern: "@import.*tailwindcss"
---

<objective>
Remove unused npm dependencies from package.json and clean up knip configuration.

Purpose: Reduce install size and attack surface by removing packages that are not imported anywhere in the source code.
Output: Clean package.json with only used dependencies, accurate knip.json config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/package.json
@frontend/knip.json
@frontend/vite.config.ts
@frontend/src/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead npm packages and clean vite config</name>
  <read_first>frontend/package.json, frontend/vite.config.ts</read_first>
  <files>frontend/package.json, frontend/vite.config.ts</files>
  <action>
    1. Remove these 2 packages from `dependencies` in package.json:
       - `@novnc/novnc` (line 35) -- stripped in Phase 63 but package.json entry was missed
       - `@types/novnc__novnc` (line 44) -- type declarations for the removed novnc package

    2. Do NOT remove `tailwindcss` -- it is used via `@import "tailwindcss/preflight"` in globals.css. Knip cannot detect CSS imports so it reports a false positive.

    3. In vite.config.ts, remove the novnc manualChunks entry (line 22):
       `if (id.includes('node_modules/@novnc')) return 'novnc'`

    4. Run `cd frontend && npm install` to regenerate package-lock.json without the removed packages.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && ! grep -q "novnc" package.json && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "novnc" frontend/package.json` returns 0
    - `grep -c "novnc" frontend/vite.config.ts` returns 0
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>@novnc/novnc and @types/novnc__novnc removed from package.json, novnc manualChunks entry removed from vite.config.ts, package-lock.json regenerated, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Clean knip ignoreDependencies and add tailwindcss</name>
  <read_first>frontend/knip.json</read_first>
  <files>frontend/knip.json</files>
  <action>
    1. Remove these 6 entries from `ignoreDependencies` in knip.json (knip now detects all of them via source imports, so the ignores are redundant):
       - `@tauri-apps/api`
       - `@tauri-apps/plugin-dialog`
       - `@tauri-apps/plugin-fs`
       - `@tauri-apps/plugin-shell`
       - `@types/dompurify`
       - `@types/lz-string`

    2. Add `tailwindcss` to `ignoreDependencies` -- this is used via `@import "tailwindcss/preflight"` in globals.css but knip cannot detect CSS-level imports.

    3. The final `ignoreDependencies` array should contain only: `["tailwindcss"]`

    4. Verify: run `cd frontend && npx knip --include dependencies` -- should report zero unused dependencies AND zero configuration hints.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && npx knip --include dependencies --no-config-hints 2>&1 | grep -c "Unused dependencies" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `npx knip --include dependencies` reports 0 unused dependencies
    - `npx knip --include dependencies` reports 0 configuration hints
    - `ignoreDependencies` in knip.json contains only `["tailwindcss"]`
  </acceptance_criteria>
  <done>knip reports zero unused dependencies and zero configuration hints. ignoreDependencies is minimal and accurate.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx knip --include dependencies` reports zero unused deps and zero config hints
2. `cd frontend && npm run build` produces a working bundle
3. `grep -c "novnc" frontend/package.json` returns 0
4. `grep -c "novnc" frontend/vite.config.ts` returns 0
5. `jq '.ignoreDependencies' frontend/knip.json` returns `["tailwindcss"]`
</verification>

<success_criteria>
- package.json has 2 fewer dependencies (@novnc/novnc, @types/novnc__novnc removed)
- vite.config.ts has no novnc manualChunks entry
- knip --include dependencies reports zero issues
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/66-strip-unused-npm-dependencies/66-01-SUMMARY.md`
</output>
