---
phase: 89-live-usage-models-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/__tests__/useOpenClawUsage.test.ts
  - frontend/src/hooks/__tests__/useOpenClawModels.test.ts
  - frontend/src/pages/openclaw/__tests__/UsageTab.test.tsx
  - frontend/src/pages/openclaw/__tests__/ModelsTab.test.tsx
autonomous: true
requirements: [LIVE-03, LIVE-04]

must_haves:
  truths:
    - "Usage tab shows real token counts and cost data from the gateway"
    - "Usage daily chart renders with actual data when available"
    - "Models tab shows all available models with provider labels"
    - "Model capabilities (max_tokens) and cost info display correctly"
    - "Both tabs handle empty/missing data gracefully without crashes"
  artifacts:
    - path: "frontend/src/pages/openclaw/__tests__/UsageTab.test.tsx"
      provides: "Smoke test verifying UsageTab renders with mock usage data"
      min_lines: 30
    - path: "frontend/src/pages/openclaw/__tests__/ModelsTab.test.tsx"
      provides: "Smoke test verifying ModelsTab renders with mock model data"
      min_lines: 30
    - path: "frontend/src/hooks/__tests__/useOpenClawUsage.test.ts"
      provides: "Tests for usage hook response shape handling"
      contains: "daily"
  key_links:
    - from: "frontend/src/hooks/useOpenClawUsage.ts"
      to: "/api/openclaw/usage"
      via: "api.get in useQuery"
      pattern: "api\\.get.*'/api/openclaw/usage'"
    - from: "frontend/src/hooks/useOpenClawModels.ts"
      to: "/api/openclaw/models"
      via: "api.get in useQuery"
      pattern: "api\\.get.*'/api/openclaw/models'"
    - from: "src-tauri/src/routes/openclaw_data.rs"
      to: "gateway_forward"
      via: "HTTP proxy to /usage and /models"
      pattern: "gateway_forward.*/(usage|models)"
---

<objective>
Verify the Usage and Models tabs render real data from the gateway, handle all response shape variations, and display correct values.

Purpose: Final live verification that usage tracking and model listing tabs are fully functional with real OpenClaw gateway data.
Output: UsageTab and ModelsTab smoke tests, usage response shape tests, all existing tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/pages/openclaw/UsageTab.tsx
@frontend/src/pages/openclaw/ModelsTab.tsx
@frontend/src/hooks/useOpenClawUsage.ts
@frontend/src/hooks/useOpenClawModels.ts
@frontend/src/pages/openclaw/types.ts
@frontend/src/hooks/__tests__/useOpenClawModels.test.ts
@src-tauri/src/routes/openclaw_data.rs

<interfaces>
From frontend/src/pages/openclaw/types.ts:
```typescript
export interface UsageData {
  total_tokens?: number
  prompt_tokens?: number
  completion_tokens?: number
  total_cost?: number
  models?: ModelUsage[]
  period?: string
  daily?: Array<{ date: string; tokens: number; cost: number }>
  [key: string]: unknown
}

export interface ModelInfo {
  id: string
  name?: string
  provider?: string
  max_tokens?: number
  input_cost_per_token?: number
  output_cost_per_token?: number
  [key: string]: unknown
}

export interface ModelsResponse {
  models?: ModelInfo[]
  data?: ModelInfo[] // LiteLLM uses "data" key
  [key: string]: unknown
}
```

From src-tauri/src/routes/openclaw_data.rs:
- GET /api/openclaw/usage -> gateway_forward(GET, "/usage") -> raw JSON passthrough
- GET /api/openclaw/models -> gateway_forward(GET, "/models") -> raw JSON passthrough
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add usage response shape tests and UsageTab smoke test</name>
  <read_first>
    - frontend/src/hooks/useOpenClawUsage.ts (simple hook, fetches UsageData)
    - frontend/src/pages/openclaw/UsageTab.tsx (renders stat cards, daily chart, model table)
    - frontend/src/pages/openclaw/types.ts (UsageData, ModelUsage interfaces)
    - frontend/src/hooks/__tests__/useOpenClawModels.test.ts (test pattern reference)
  </read_first>
  <files>
    frontend/src/hooks/__tests__/useOpenClawUsage.test.ts
    frontend/src/pages/openclaw/__tests__/UsageTab.test.tsx
  </files>
  <action>
    1. Create useOpenClawUsage.test.ts following the useOpenClawModels.test.ts pattern:
       - Mock '@/lib/api' with api.get
       - Test: "returns usage data on successful fetch" -- mock api.get with full UsageData (total_tokens: 150000, total_cost: 2.50, period: "2026-03", daily: [{ date: "2026-03-24", tokens: 50000, cost: 0.85 }], models: [{ model: "claude-sonnet-4-6", tokens: 100000, cost: 1.50, requests: 50 }])
       - Test: "returns empty usage when api returns minimal data" -- mock with just { total_tokens: 0 }
       - Test: "starts in loading state"
       - Test: "returns error on fetch failure"
       - Test: "handles daily array with multiple days" -- verify daily chart data parses correctly with 7 days

    2. Create UsageTab.test.tsx smoke test:
       - Mock useOpenClawUsage to return { usage: mockUsageData, loading: false, error: null }
       - Mock useBudgetAlerts to return { alert: null }
       - Render UsageTab with healthy={true} inside QueryClientProvider
       - Assert: renders without throwing
       - Assert: "Total Tokens" label appears
       - Assert: "Total Cost" label appears
       - Assert: token value "150,000" appears (toLocaleString formatted)
       - Also test: healthy={false} shows "OpenClaw is not configured" message
       - Also test: loading state shows "Loading..."
       - Also test: null usage shows "No usage data available"
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && npx vitest run src/hooks/__tests__/useOpenClawUsage.test.ts src/pages/openclaw/__tests__/UsageTab.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "daily" frontend/src/hooks/__tests__/useOpenClawUsage.test.ts (daily chart data tested)
    - grep -q "Total Tokens" frontend/src/pages/openclaw/__tests__/UsageTab.test.tsx (stat card verified)
    - grep -q "not configured" frontend/src/pages/openclaw/__tests__/UsageTab.test.tsx (unhealthy state tested)
    - All tests pass (0 failures)
  </acceptance_criteria>
  <done>useOpenClawUsage tests verify response shape with daily data, UsageTab smoke test covers healthy/unhealthy/loading/empty states</done>
</task>

<task type="auto">
  <name>Task 2: Add ModelsTab smoke test and verify LiteLLM response format</name>
  <read_first>
    - frontend/src/pages/openclaw/ModelsTab.tsx (renders model cards)
    - frontend/src/hooks/useOpenClawModels.ts (simple hook)
    - frontend/src/hooks/__tests__/useOpenClawModels.test.ts (existing tests)
    - frontend/src/pages/openclaw/types.ts (ModelInfo, ModelsResponse)
  </read_first>
  <files>
    frontend/src/pages/openclaw/__tests__/ModelsTab.test.tsx
    frontend/src/hooks/__tests__/useOpenClawModels.test.ts
  </files>
  <action>
    1. Create ModelsTab.test.tsx smoke test:
       - Mock useOpenClawModels to return { models: { models: [mockModel1, mockModel2] }, loading: false, error: null }
       - mockModel1: { id: 'claude-sonnet-4-6', name: 'Claude Sonnet 4.6', provider: 'anthropic', max_tokens: 200000, input_cost_per_token: 0.000003, output_cost_per_token: 0.000015 }
       - mockModel2: { id: 'gpt-4', name: 'GPT-4', provider: 'openai', max_tokens: 128000 }
       - Render ModelsTab with healthy={true} inside QueryClientProvider
       - Assert: renders without throwing
       - Assert: "Claude Sonnet 4.6" appears in the document
       - Assert: "GPT-4" appears in the document
       - Assert: provider badges appear ("anthropic", "openai")
       - Assert: max_tokens display appears ("200,000 max tokens")
       - Assert: cost info appears (input/output cost per token)
       - Test: healthy={false} shows "OpenClaw is not configured"
       - Test: empty models shows "No models available"
       - Test: LiteLLM format -- mock with { data: [model1], models: undefined }. The ModelsTab code uses `models?.models ?? models?.data ?? []` so data key should work. Assert model renders.

    2. Add a test to useOpenClawModels.test.ts verifying the hook passes through the response correctly for the ModelsTab to parse (the hook returns the raw ModelsResponse, and the tab extracts models/data). Verify that when the gateway returns { models: [...], extra_field: "value" }, the extra_field is preserved (due to [key: string]: unknown index signature).
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/frontend && npx vitest run src/pages/openclaw/__tests__/ModelsTab.test.tsx src/hooks/__tests__/useOpenClawModels.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ModelsTab" frontend/src/pages/openclaw/__tests__/ModelsTab.test.tsx (smoke test exists)
    - grep -q "anthropic\|provider" frontend/src/pages/openclaw/__tests__/ModelsTab.test.tsx (provider badge verified)
    - grep -q "LiteLLM\|data key" frontend/src/pages/openclaw/__tests__/ModelsTab.test.tsx (LiteLLM format tested)
    - grep -q "max_tokens" frontend/src/pages/openclaw/__tests__/ModelsTab.test.tsx (capabilities verified)
    - All tests pass (0 failures)
  </acceptance_criteria>
  <done>ModelsTab smoke test covers all states including LiteLLM format, model capabilities and cost display verified, all tests green</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run src/hooks/__tests__/useOpenClawUsage.test.ts src/hooks/__tests__/useOpenClawModels.test.ts src/pages/openclaw/__tests__/UsageTab.test.tsx src/pages/openclaw/__tests__/ModelsTab.test.tsx` -- all pass
2. Usage types handle daily array and model breakdown (verified by tests)
3. Models handle both `models` and `data` response keys (LiteLLM compat, verified by tests)
4. Both tabs handle unhealthy/loading/empty states gracefully (verified by tests)
</verification>

<success_criteria>
- UsageTab smoke test exists and passes with multiple states
- ModelsTab smoke test exists and passes with LiteLLM format
- useOpenClawUsage tests cover daily chart data
- All existing useOpenClawModels tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/89-live-usage-models-tabs/89-01-SUMMARY.md`
</output>
