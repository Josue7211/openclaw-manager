---
phase: 01-responsive-layout-visual-polish
plan: 04
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - frontend/src/pages/Todos.tsx
  - frontend/src/pages/Missions.tsx
  - frontend/src/pages/missions/AccordionBody.tsx
  - frontend/src/pages/KnowledgeBase.tsx
  - frontend/src/pages/Calendar.tsx
  - frontend/src/pages/calendar/MonthView.tsx
  - frontend/src/pages/HomeLab.tsx
  - frontend/src/pages/Status.tsx
  - frontend/src/pages/Search.tsx
  - frontend/src/pages/CustomPage.tsx
  - frontend/src/pages/Pipeline.tsx
  - frontend/src/pages/pipeline/PipelineIdeas.tsx
  - frontend/src/pages/pipeline/PipelineShipLog.tsx
  - frontend/src/pages/pipeline/PipelineRetros.tsx
  - frontend/src/pages/pipeline/PipelineNotes.tsx
  - frontend/src/pages/pipeline/PipelineStale.tsx
  - frontend/src/pages/Personal.tsx
  - frontend/src/pages/personal/TodoSection.tsx
  - frontend/src/pages/personal/MorningBrief.tsx
  - frontend/src/pages/personal/HomelabSection.tsx
  - frontend/src/pages/personal/DailyReviewWidget.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/pages/dashboard/SessionsCard.tsx
  - frontend/src/pages/dashboard/MissionsCard.tsx
autonomous: true
gap_closure: true
requirements: [POLISH-03, POLISH-05, POLISH-06, POLISH-07]

must_haves:
  truths:
    - "Every data-fetching page in Plan 04's scope displays ErrorState with retry when data fetch fails, not inline error text"
    - "Every list/data page in Plan 04's scope displays EmptyState with contextual icon and copy when there is no data, not inline strings"
    - "Button component is used for primary actions in Plan 04's pages, replacing inline styled buttons"
  artifacts:
    - path: "frontend/src/pages/Todos.tsx"
      provides: "EmptyState for empty todo list, ErrorState for fetch failures"
      contains: "import.*EmptyState.*from.*@/components/ui/EmptyState"
    - path: "frontend/src/pages/Missions.tsx"
      provides: "EmptyState for empty missions, ErrorState for fetch failures"
      contains: "import.*EmptyState.*from.*@/components/ui/EmptyState"
    - path: "frontend/src/pages/KnowledgeBase.tsx"
      provides: "EmptyState for empty knowledge base"
      contains: "import.*EmptyState.*from.*@/components/ui/EmptyState"
    - path: "frontend/src/pages/dashboard/SessionsCard.tsx"
      provides: "EmptyState for no sessions"
      contains: "import.*EmptyState.*from.*@/components/ui/EmptyState"
    - path: "frontend/src/pages/pipeline/PipelineShipLog.tsx"
      provides: "EmptyState for no ship log entries"
      contains: "import.*EmptyState.*from.*@/components/ui/EmptyState"
  key_links:
    - from: "frontend/src/pages/Todos.tsx"
      to: "frontend/src/components/ui/EmptyState.tsx"
      via: "import and render"
      pattern: "import.*EmptyState"
    - from: "frontend/src/pages/HomeLab.tsx"
      to: "frontend/src/components/ui/ErrorState.tsx"
      via: "import and render on error"
      pattern: "import.*ErrorState"
---

<objective>
Adopt shared EmptyState, ErrorState, and Button components across the first half of app pages: Todos, Missions, KnowledgeBase, Calendar, HomeLab, Status, Search, CustomPage, Pipeline (all sub-pages), Personal (all sub-pages), Dashboard cards.

Purpose: Close verification gaps where EmptyState/ErrorState/Button components were created in Plan 02 but never imported by any page. Pages currently use ad-hoc inline strings ("No todos yet", "No sessions found") and inline styled buttons instead of the shared design system components.

Output: 24 page files updated to use shared UI components from components/ui/.
</objective>

<execution_context>
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/home/josue/Documents/Projects/mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-responsive-layout-visual-polish/01-02-SUMMARY.md
@.planning/phases/01-responsive-layout-visual-polish/01-VERIFICATION.md
@.planning/phases/01-responsive-layout-visual-polish/01-UI-SPEC.md

<interfaces>
<!-- Shared UI components created in Plan 02. Executor uses these directly. -->

From frontend/src/components/ui/Button.tsx:
```typescript
type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'danger'
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ButtonVariant  // default: 'primary'
  children: React.ReactNode
}
export const Button = React.memo(function Button({ variant, children, ...rest }: ButtonProps) { ... })
```

From frontend/src/components/ui/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon: React.ElementType      // Phosphor icon component
  title: string                // e.g. "No todos yet"
  description?: string         // e.g. "Add a todo above to get started."
  action?: { label: string; onClick: () => void }
}
export const EmptyState = React.memo(function EmptyState({ icon, title, description, action }: EmptyStateProps) { ... })
```

From frontend/src/components/ui/ErrorState.tsx:
```typescript
interface ErrorStateProps {
  message?: string             // Custom error message (overrides default)
  resource?: string            // e.g. "todos" -> "We couldn't load your todos."
  onRetry?: () => void         // Retry callback (shows "Try Again" button)
}
export const ErrorState = React.memo(function ErrorState({ message, resource, onRetry }: ErrorStateProps) { ... })
// Always shows "Reload Page" button. Shows "Try Again" only if onRetry provided.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt EmptyState and ErrorState across standalone pages and dashboard/personal sub-components</name>
  <files>
    frontend/src/pages/Todos.tsx
    frontend/src/pages/Missions.tsx
    frontend/src/pages/KnowledgeBase.tsx
    frontend/src/pages/Calendar.tsx
    frontend/src/pages/HomeLab.tsx
    frontend/src/pages/Status.tsx
    frontend/src/pages/Search.tsx
    frontend/src/pages/CustomPage.tsx
    frontend/src/pages/Personal.tsx
    frontend/src/pages/Dashboard.tsx
    frontend/src/pages/personal/TodoSection.tsx
    frontend/src/pages/personal/MorningBrief.tsx
    frontend/src/pages/personal/HomelabSection.tsx
    frontend/src/pages/personal/DailyReviewWidget.tsx
    frontend/src/pages/dashboard/SessionsCard.tsx
    frontend/src/pages/dashboard/MissionsCard.tsx
  </files>
  <read_first>
    frontend/src/components/ui/EmptyState.tsx
    frontend/src/components/ui/ErrorState.tsx
    frontend/src/components/ui/Button.tsx
    frontend/src/pages/Todos.tsx
    frontend/src/pages/Missions.tsx
    frontend/src/pages/KnowledgeBase.tsx
    frontend/src/pages/Calendar.tsx
    frontend/src/pages/HomeLab.tsx
    frontend/src/pages/Status.tsx
    frontend/src/pages/Search.tsx
    frontend/src/pages/CustomPage.tsx
    frontend/src/pages/Personal.tsx
    frontend/src/pages/Dashboard.tsx
    frontend/src/pages/personal/TodoSection.tsx
    frontend/src/pages/personal/MorningBrief.tsx
    frontend/src/pages/personal/HomelabSection.tsx
    frontend/src/pages/personal/DailyReviewWidget.tsx
    frontend/src/pages/dashboard/SessionsCard.tsx
    frontend/src/pages/dashboard/MissionsCard.tsx
  </read_first>
  <action>
For each file, replace inline empty state strings with the shared EmptyState component, and replace inline error displays with the shared ErrorState component. Import from `@/components/ui/EmptyState` and `@/components/ui/ErrorState`. Use appropriate Phosphor icons imported from `@phosphor-icons/react`.

Use the copywriting from the UI-SPEC (01-UI-SPEC.md Copywriting Contract section) for each page. Here are the exact replacements:

**EmptyState replacements (inline string -> EmptyState component):**

1. `Todos.tsx` line ~310: Replace `"No todos yet — add one above"` with `<EmptyState icon={ListChecks} title="All clear" description="You have no tasks. Enjoy the free time." />`
2. `Missions.tsx` line ~152: Replace `'No missions yet'` / `'No ${tab} missions'` with `<EmptyState icon={Rocket} title={tab === 'all' ? 'No missions' : \`No \${tab} missions\`} description="Active missions from your agents will appear here." />`
3. `KnowledgeBase.tsx` line ~156: Replace the inline `'No entries match your filters'` with `<EmptyState icon={BookOpen} title={hasActiveFilters ? 'No entries match your filters' : 'Knowledge base is empty'} description={hasActiveFilters ? 'Try adjusting your filters.' : 'Save articles, links, and snippets here.'} action={!hasActiveFilters ? { label: 'Add Entry', onClick: openAddModal } : undefined} />`
4. `Calendar.tsx`: If there is an error display (line ~189), wrap it with ErrorState: `<ErrorState resource="calendar" onRetry={refetch} />`
5. `HomeLab.tsx` line ~57 error display + line ~204 "No VMs found": Replace error with `<ErrorState resource="homelab" onRetry={refetch} />` and empty with `<EmptyState icon={Desktop} title="No VMs found" description="Connect to your Proxmox server in Settings." />`
6. `Status.tsx` line ~151: Replace `"No peers found (tailscale may not be installed)"` with `<EmptyState icon={WifiHigh} title="No peers found" description="Tailscale may not be installed or running." />`
7. `Search.tsx` line ~185: Replace `"No results for ..."` with `<EmptyState icon={MagnifyingGlass} title={\`No results for "\${query}"\`} description="Try a different search term." />`
8. `CustomPage.tsx` line ~52: Replace `"This page is empty..."` with `<EmptyState icon={Sparkle} title="This page is empty" description="Talk to your agent to create it." />`
9. `personal/TodoSection.tsx` line ~36: Replace `"No todos yet"` div with `<EmptyState icon={ListChecks} title="All clear" description="You have no tasks." />`
10. `personal/MorningBrief.tsx` line ~83: Replace `"All clear — nothing pending"` with `<EmptyState icon={CheckCircle} title="All clear" description="Nothing pending." />`
11. `personal/MorningBrief.tsx` line ~105: Replace `"No events today"` with `<EmptyState icon={CalendarBlank} title="No events today" />`
12. `personal/HomelabSection.tsx` line ~65: Replace `"No VMs found"` with `<EmptyState icon={Desktop} title="No VMs found" />`
13. `dashboard/SessionsCard.tsx` line ~22: Replace `"No sessions found"` with `<EmptyState icon={Timer} title="No sessions yet" description="Start a pomodoro to track your focus." />`
14. `dashboard/MissionsCard.tsx`: If it has an empty state, replace with `<EmptyState icon={Rocket} title="No missions" description="Active missions will appear here." />`

**ErrorState replacements (inline error text -> ErrorState component):**
- Any page that displays a data fetch error with inline text should use `<ErrorState resource="{pageName}" onRetry={refetch} />` where `refetch` is the React Query refetch function.
- Pages with `isError` or `error &&` conditional renders: replace with ErrorState.

**Button replacements:**
- Replace any inline styled `<button>` elements that match the 4-variant hierarchy (primary CTA, secondary, ghost, danger) with `<Button variant="...">`. Only replace buttons that clearly fit one of the 4 variants. Leave highly custom buttons (e.g., toggle buttons with special state) as-is.
- `personal/TodoSection.tsx`: Replace inline styled add button with `<Button variant="ghost">`
- `Calendar.tsx`: Replace inline styled buttons with `<Button variant="ghost">` or `<Button variant="primary">` as appropriate
- `KnowledgeBase.tsx`: Replace inline styled "Add Entry" button with `<Button variant="primary">`

**Important rules:**
- Import Phosphor icons for EmptyState: `import { ListChecks, Rocket, BookOpen, Desktop, WifiHigh, MagnifyingGlass, Sparkle, CheckCircle, CalendarBlank, Timer } from '@phosphor-icons/react'` (only import what each file needs)
- For compact sub-components (personal/TodoSection, personal/MorningBrief, personal/HomelabSection, dashboard/SessionsCard, dashboard/MissionsCard), use a smaller EmptyState by wrapping it: pass a smaller icon and shorter copy since these are cards, not full pages. The EmptyState component's padding may be too large for small cards -- if so, wrap it in a div with reduced padding: `<div style={{ padding: '16px 0' }}><EmptyState ... /></div>` or override with a style wrapper.
- Do NOT modify the logic, data fetching, or functionality of any page. Only replace the visual presentation of empty/error states and buttons.
- Do NOT touch test files.
- Keep all existing accessibility attributes (aria-labels, roles, etc.). The shared components already have proper ARIA attributes.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "import.*EmptyState.*from.*@/components/ui/EmptyState" frontend/src/pages/Todos.tsx frontend/src/pages/Missions.tsx frontend/src/pages/KnowledgeBase.tsx frontend/src/pages/HomeLab.tsx frontend/src/pages/Status.tsx frontend/src/pages/Search.tsx frontend/src/pages/CustomPage.tsx frontend/src/pages/personal/TodoSection.tsx frontend/src/pages/personal/MorningBrief.tsx frontend/src/pages/personal/HomelabSection.tsx frontend/src/pages/dashboard/SessionsCard.tsx | wc -l returns >= 11
    - grep -rn "import.*ErrorState.*from.*@/components/ui/ErrorState" frontend/src/pages/HomeLab.tsx frontend/src/pages/Calendar.tsx | wc -l returns >= 1
    - npx vitest run exits with code 0
  </acceptance_criteria>
  <done>
    - At least 11 page files import and render EmptyState from @/components/ui/EmptyState
    - At least 1 page file imports and renders ErrorState from @/components/ui/ErrorState
    - All inline empty state strings in Plan 04 scope files have been replaced with EmptyState component
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Adopt EmptyState, ErrorState, and Button across Pipeline sub-pages and missions sub-components</name>
  <files>
    frontend/src/pages/Pipeline.tsx
    frontend/src/pages/pipeline/PipelineIdeas.tsx
    frontend/src/pages/pipeline/PipelineShipLog.tsx
    frontend/src/pages/pipeline/PipelineRetros.tsx
    frontend/src/pages/pipeline/PipelineNotes.tsx
    frontend/src/pages/pipeline/PipelineStale.tsx
    frontend/src/pages/missions/AccordionBody.tsx
    frontend/src/pages/calendar/MonthView.tsx
  </files>
  <read_first>
    frontend/src/components/ui/EmptyState.tsx
    frontend/src/components/ui/ErrorState.tsx
    frontend/src/components/ui/Button.tsx
    frontend/src/pages/Pipeline.tsx
    frontend/src/pages/pipeline/PipelineIdeas.tsx
    frontend/src/pages/pipeline/PipelineShipLog.tsx
    frontend/src/pages/pipeline/PipelineRetros.tsx
    frontend/src/pages/pipeline/PipelineNotes.tsx
    frontend/src/pages/pipeline/PipelineStale.tsx
    frontend/src/pages/missions/AccordionBody.tsx
    frontend/src/pages/calendar/MonthView.tsx
  </read_first>
  <action>
Replace inline empty/error states and inline styled buttons in Pipeline sub-pages, missions/AccordionBody, and calendar/MonthView with shared UI components.

**EmptyState replacements:**

1. `pipeline/PipelineIdeas.tsx` line ~202: Replace `'No ideas match filters'` / `'No ideas yet'` with `<EmptyState icon={Lightbulb} title={hasActiveFilters ? 'No ideas match filters' : 'No ideas yet'} description={hasActiveFilters ? 'Try adjusting your filters.' : 'Capture your first idea to get started.'} />`
2. `pipeline/PipelineShipLog.tsx` line ~250: Replace `"No entries yet. Start logging what you ship."` with `<EmptyState icon={Rocket} title="No entries yet" description="Start logging what you ship." />`
3. `pipeline/PipelineRetros.tsx` line ~28: Replace `"No retrospectives yet."` with `<EmptyState icon={ArrowCounterClockwise} title="No retrospectives yet" description="Retrospectives will appear after completed sprints." />`
4. `pipeline/PipelineNotes.tsx` line ~66: Replace `"No notes yet."` with `<EmptyState icon={Notepad} title="No notes yet" description="Pipeline notes will appear here." />`
5. `pipeline/PipelineStale.tsx` line ~70: Replace the "All clear" div with `<EmptyState icon={CheckCircle} title="All clear" description="No stale items found." />`
6. `calendar/MonthView.tsx` line ~149: Replace `"No events"` with `<EmptyState icon={CalendarBlank} title="No events" />`

**Button replacements:**
- `pipeline/PipelineIdeas.tsx`: Replace inline styled action buttons (e.g., filter buttons, add idea buttons) with `<Button variant="ghost">` or `<Button variant="primary">` as appropriate
- `pipeline/PipelineShipLog.tsx`: Replace inline styled action buttons with `<Button>` variants
- `pipeline/PipelineStale.tsx`: Replace inline styled buttons with `<Button>` variants

**Important rules:**
- Import Phosphor icons: `import { Lightbulb, Rocket, ArrowCounterClockwise, Notepad, CheckCircle, CalendarBlank } from '@phosphor-icons/react'` (only import what each file needs -- many of these files already import Phosphor icons from Plan 03 migration)
- For compact card contexts (like pipeline sub-pages which may render in smaller panels), keep the EmptyState but consider if padding needs wrapping adjustment.
- Do NOT change any data fetching logic, filtering logic, or state management.
- Do NOT touch test files.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/Projects/mission-control/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "import.*EmptyState.*from.*@/components/ui/EmptyState" frontend/src/pages/pipeline/ frontend/src/pages/calendar/MonthView.tsx | wc -l returns >= 6
    - grep -rn "import.*Button.*from.*@/components/ui/Button" frontend/src/pages/pipeline/ | wc -l returns >= 1
    - npx vitest run exits with code 0
  </acceptance_criteria>
  <done>
    - All 5 pipeline sub-pages + calendar/MonthView import and render EmptyState
    - At least 1 pipeline sub-page uses the shared Button component
    - All inline empty state strings in pipeline/, missions/AccordionBody, and calendar/MonthView have been replaced
    - All tests pass
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "import.*EmptyState.*from.*@/components/ui/EmptyState" frontend/src/pages/ | wc -l` should return >= 17 (from Plan 04 scope alone)
2. `grep -rn "import.*ErrorState.*from.*@/components/ui/ErrorState" frontend/src/pages/ | wc -l` should return >= 1
3. `cd frontend && npx vitest run` should exit with code 0
4. The following ad-hoc patterns should be greatly reduced in Plan 04 scope files:
   - `grep -rn "No todos yet\|No sessions found\|No VMs found\|No peers found\|No missions\|No ideas\|No entries\|No retrospectives\|No notes yet\|No events\|All clear" frontend/src/pages/Todos.tsx frontend/src/pages/Missions.tsx frontend/src/pages/dashboard/SessionsCard.tsx frontend/src/pages/HomeLab.tsx frontend/src/pages/Status.tsx frontend/src/pages/pipeline/ frontend/src/pages/personal/ frontend/src/pages/calendar/MonthView.tsx` should return 0 matches
</verification>

<success_criteria>
- Shared EmptyState component is imported and rendered by at least 17 page files in Plan 04's scope
- Shared ErrorState component is imported and rendered by at least 1 page file where data fetching can fail
- Shared Button component is adopted for at least 3 primary/secondary actions across Plan 04 pages
- Zero inline empty state ad-hoc strings remain in Plan 04 scope files
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-responsive-layout-visual-polish/01-04-SUMMARY.md`
</output>
