---
phase: 58-audit-allow-dead-code-annotations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/supabase.rs
  - src-tauri/src/routes/media.rs
  - src-tauri/src/routes/auth.rs
  - src-tauri/src/routes/bjorn.rs
  - src-tauri/src/routes/terminal.rs
  - src-tauri/src/routes/pipeline/helpers.rs
  - src-tauri/src/routes/pipeline/agents.rs
autonomous: true
requirements: [RUST-01]

must_haves:
  truths:
    - "Every #[allow(dead_code)] annotation has been individually reviewed and either removed or justified"
    - "No struct-level #[allow(dead_code)] hides genuinely unused fields"
    - "cargo clippy produces zero dead_code warnings after the audit"
    - "All 231 existing Rust tests still pass"
  artifacts:
    - path: "src-tauri/src/supabase.rs"
      provides: "health_check() annotation removed (it is actively used); rpc/rpc_url justified"
      contains: "Justification:"
    - path: "src-tauri/src/routes/media.rs"
      provides: "Struct-level annotations removed; unused fields removed or justified at field level"
    - path: "src-tauri/src/routes/auth.rs"
      provides: "SignupBody annotation justified"
      contains: "Justification:"
    - path: "src-tauri/src/routes/bjorn.rs"
      provides: "BridgeBody.args annotation justified"
      contains: "Justification:"
    - path: "src-tauri/src/routes/terminal.rs"
      provides: "PtyCleanup.master annotation justified"
      contains: "Justification:"
    - path: "src-tauri/src/routes/pipeline/helpers.rs"
      provides: "clean_env() annotation justified"
      contains: "Justification:"
    - path: "src-tauri/src/routes/pipeline/agents.rs"
      provides: "PENDING constant annotation justified"
      contains: "Justification:"
  key_links:
    - from: "src-tauri/src/supabase.rs"
      to: "src-tauri/src/sync.rs"
      via: "health_check() method call"
      pattern: "health_check\\(\\)"
    - from: "src-tauri/src/routes/media.rs"
      to: "Plex/Sonarr/Radarr API responses"
      via: "serde::Deserialize"
      pattern: "derive.*Deserialize"
---

<objective>
Audit all 13 `#[allow(dead_code)]` annotations across 7 Rust files, removing annotations on genuinely used or serde-constructed code, and adding `// Justification:` comments to annotations that are intentionally kept.

Purpose: Eliminate false dead-code suppressions that hide genuinely unused code, making future dead code visible to the compiler immediately.
Output: Clean Rust codebase where every remaining `#[allow(dead_code)]` has an explicit justification comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-audit-allow-dead-code-annotations/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove incorrect and unnecessary #[allow(dead_code)] annotations</name>
  <read_first>
    - src-tauri/src/supabase.rs
    - src-tauri/src/routes/media.rs
  </read_first>
  <files>
    src-tauri/src/supabase.rs,
    src-tauri/src/routes/media.rs
  </files>
  <action>
Process 5 annotations that must be REMOVED (not just commented):

**supabase.rs -- Annotation 3 (line ~447): `health_check()` method**
- REMOVE the `#[allow(dead_code)]` line above `pub async fn health_check()`. The method IS actively called from `sync.rs` at lines ~107 and ~477. The annotation was wrong.
- Do NOT touch annotations 1 and 2 (rpc_url/rpc) -- those are handled in Task 2.

**media.rs -- Annotation 8 (line ~145): `SonarrEpisode` struct**
- REMOVE the `#[allow(dead_code)]` line. All fields (series, title, air_date_utc, season_number, episode_number) are accessed after deserialization. Serde handles construction.

**media.rs -- Annotation 6 (line ~107): `PlexSession` struct**
- REMOVE the struct-level `#[allow(dead_code)]` line.
- ADD field-level annotation on the `player` field only:
  `#[allow(dead_code)] // Justification: deserialized from Plex API; reserved for future player state display`
- All other fields (title, grandparent_title, media_type, view_offset, duration, user) are actively read.

**media.rs -- Annotation 7 (line ~129): `PlexPlayer` struct**
- KEEP the `#[allow(dead_code)]` but change from struct-level to a comment explaining why:
  `#[allow(dead_code)] // Justification: models Plex API player state; only used as PlexSession.player type -- reserved for future enhancement`

**media.rs -- Annotation 9 (line ~158): `SonarrSeries` struct**
- REMOVE the struct-level `#[allow(dead_code)]` line.
- REMOVE the `added` field entirely (it is never read after deserialization; serde ignores unknown fields by default since `deny_unknown_fields` is NOT set on this struct).

**media.rs -- Annotation 10 (line ~168): `RadarrMovie` struct**
- REMOVE the struct-level `#[allow(dead_code)]` line.
- REMOVE the `date_added` field entirely (it is never read after deserialization; same reasoning as SonarrSeries).

After all changes, run `cd src-tauri && cargo check` to verify compilation. If any removed field causes a compilation error (struct literal uses it somewhere), add it back with a field-level `#[allow(dead_code)]` and justification instead of removing.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '#\[allow(dead_code)\]' src-tauri/src/supabase.rs returns 2 (down from 3 -- health_check annotation removed)
    - grep -c '#\[allow(dead_code)\]' src-tauri/src/routes/media.rs returns 2 (down from 5 -- 3 struct-level removed, PlexSession.player field-level added, PlexPlayer kept)
    - grep 'added:' src-tauri/src/routes/media.rs returns 0 (SonarrSeries.added field removed)
    - grep 'date_added:' src-tauri/src/routes/media.rs returns 0 (RadarrMovie.date_added field removed)
    - cargo check succeeds with exit code 0
  </acceptance_criteria>
  <done>
    5 incorrect/unnecessary struct-level annotations removed. 2 unused struct fields removed. health_check() annotation removed. PlexSession.player has field-level annotation with justification. cargo check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add justification comments to all kept annotations and verify audit</name>
  <read_first>
    - src-tauri/src/supabase.rs
    - src-tauri/src/routes/auth.rs
    - src-tauri/src/routes/bjorn.rs
    - src-tauri/src/routes/terminal.rs
    - src-tauri/src/routes/pipeline/helpers.rs
    - src-tauri/src/routes/pipeline/agents.rs
    - src-tauri/src/routes/media.rs
  </read_first>
  <files>
    src-tauri/src/supabase.rs,
    src-tauri/src/routes/auth.rs,
    src-tauri/src/routes/bjorn.rs,
    src-tauri/src/routes/terminal.rs,
    src-tauri/src/routes/pipeline/helpers.rs,
    src-tauri/src/routes/pipeline/agents.rs
  </files>
  <action>
Add `// Justification:` inline comments to all 8 remaining `#[allow(dead_code)]` annotations. Use the format: `#[allow(dead_code)] // Justification: {reason}`

**supabase.rs -- Annotation 1 (line ~98): `rpc_url()` method**
Change to: `#[allow(dead_code)] // Justification: SupabaseClient API surface -- available for RPC calls from future routes`

**supabase.rs -- Annotation 2 (line ~423): `rpc()` method**
Change to: `#[allow(dead_code)] // Justification: SupabaseClient API surface -- available for RPC calls from future routes`

**auth.rs -- Annotation 5 (line ~598): `SignupBody` struct**
Change to: `#[allow(dead_code)] // Justification: serde::Deserialize constructs from JSON; fields unused because signup is disabled (validates request shape)`

**bjorn.rs -- Annotation 4 (line ~553): `BridgeBody.args` field**
Change to: `#[allow(dead_code)] // Justification: deserialized from JSON request; will be used when bridge proxy is implemented (see TODO above)`

**terminal.rs -- Annotation 12 (line ~184): `PtyCleanup.master` field**
Change to: `#[allow(dead_code)] // Justification: accessed via Arc&lt;Mutex&lt;Option&lt;PtyCleanup&gt;&gt;&gt; for resize operations; compiler cannot track cross-scope field access`

**pipeline/helpers.rs -- Annotation 11 (line ~208): `clean_env()` function**
Change to: `#[allow(dead_code)] // Justification: intended replacement for clean_env_from_env() once spawn_agent_process accepts &amp;AppState`

**pipeline/agents.rs -- Annotation 13 (line ~15): `PENDING` constant**
Change to: `#[allow(dead_code)] // Justification: completes status enum for consistency with frontend constants; used in database but not yet matched in Rust`

**media.rs -- Verify Task 1 results**: Confirm PlexSession.player and PlexPlayer annotations from Task 1 have justification comments. If not, add them.

After all changes, run full verification:
1. `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo check` -- must succeed
2. `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo clippy 2>&1 | grep dead_code` -- must return empty
3. `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo test` -- all 231 tests must pass
4. Verify every remaining `#[allow(dead_code)]` has a `// Justification:` comment on the same line
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo clippy 2>&1 | grep -c dead_code && echo "FAIL: dead_code warnings found" || echo "PASS: no dead_code warnings"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn 'allow(dead_code)' src-tauri/src/ | grep -cv 'Justification:' returns 0 (every annotation has a justification)
    - grep -rc 'allow(dead_code)' src-tauri/src/ | awk -F: '{s+=$2}END{print s}' returns 8 (down from 13 -- 5 removed)
    - cargo clippy 2>&1 | grep -c dead_code returns 0 (no new warnings)
    - cargo test succeeds with exit code 0
  </acceptance_criteria>
  <done>
    All 8 kept annotations have inline justification comments. Total annotation count reduced from 13 to 8. cargo clippy reports zero dead_code warnings. All 231 Rust tests pass. Phase 58 audit is complete.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn 'allow(dead_code)' src-tauri/src/` -- exactly 8 results, each with `// Justification:` on the same line
2. `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo clippy 2>&1 | grep dead_code` -- zero results
3. `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo test` -- all tests pass
4. Manual check: no struct-level `#[allow(dead_code)]` remaining on structs where all fields are used (SonarrEpisode, SonarrSeries, RadarrMovie no longer have it)
</verification>

<success_criteria>
- 13 annotations audited individually (5 removed, 8 kept with justification)
- 2 unused struct fields removed (SonarrSeries.added, RadarrMovie.date_added)
- 1 wrong annotation removed (health_check is actively used)
- 2 struct-level annotations moved to field-level (PlexSession.player)
- Every remaining #[allow(dead_code)] has a `// Justification:` comment
- cargo clippy shows zero dead_code warnings
- All existing Rust tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/58-audit-allow-dead-code-annotations/58-01-SUMMARY.md`
</output>
