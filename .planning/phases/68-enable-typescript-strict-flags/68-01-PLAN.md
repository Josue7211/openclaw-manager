---
phase: 68-enable-typescript-strict-flags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tsconfig.app.json
  - frontend/src/components/GuidedTour.tsx
  - frontend/src/lib/dashboard-defaults.ts
  - frontend/src/lib/dashboard-store.ts
  - frontend/src/lib/home-store.ts
  - frontend/src/lib/theme-store.ts
  - frontend/src/pages/dashboard/__tests__/WidgetWrapper.test.tsx
  - frontend/src/pages/missions/MissionCard.tsx
  - frontend/src/pages/settings/SettingsKeybindings.tsx
autonomous: true
requirements: [DEAD-04]

must_haves:
  truths:
    - "tsc --noEmit --project tsconfig.app.json reports zero TS6133/TS6196 violations"
    - "tsconfig.app.json has noUnusedLocals and noUnusedParameters set to true"
    - "No runtime behavior changes from dead code removal"
  artifacts:
    - path: "frontend/tsconfig.app.json"
      provides: "Strict unused flags enabled"
      contains: "noUnusedLocals.*true"
    - path: "frontend/src/lib/dashboard-store.ts"
      provides: "Dead private functions removed"
  key_links:
    - from: "frontend/tsconfig.app.json"
      to: "all .ts/.tsx files"
      via: "TypeScript compiler enforcement"
      pattern: "noUnusedLocals.*true"
---

<objective>
Enable `noUnusedLocals: true` and `noUnusedParameters: true` in tsconfig.app.json so the TypeScript compiler catches unused locals and parameters as errors, preventing future dead code accumulation.

Purpose: After Phase 67 cleaned unused imports, this phase enables compiler-level enforcement so unused code cannot silently accumulate again. This is the final gate in the dead code strip pipeline (DEAD-04).

Output: tsconfig.app.json with strict flags enabled, 13 TS6133 violations fixed across 8 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/tsconfig.app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 13 unused locals across 8 files</name>
  <files>
    frontend/src/components/GuidedTour.tsx
    frontend/src/lib/dashboard-defaults.ts
    frontend/src/lib/dashboard-store.ts
    frontend/src/lib/home-store.ts
    frontend/src/lib/theme-store.ts
    frontend/src/pages/dashboard/__tests__/WidgetWrapper.test.tsx
    frontend/src/pages/missions/MissionCard.tsx
    frontend/src/pages/settings/SettingsKeybindings.tsx
  </files>
  <read_first>
    Each file at the specific line numbers listed below to understand surrounding context before removing.
  </read_first>
  <action>
    Remove all 13 TS6133 violations. Each is genuinely unused dead code (not just underscore-prefixed intentional ignores):

    1. **frontend/src/components/GuidedTour.tsx line 11**: Remove `React` from the import. The JSX transform handles React automatically. Keep other imports from that line (`useEffect`, `useState`, `useCallback`, `useRef`).

    2. **frontend/src/lib/dashboard-defaults.ts line 187**: Delete the entire `_generateXsLayout` function (it just delegates to `generateSmLayout` and is never called). Find where the function ends and remove the full block.

    3. **frontend/src/lib/dashboard-store.ts lines 162, 239, 435, 447**: Delete four unused private functions:
       - `_setDotIndicatorsEnabled` (line 162, ~5 lines)
       - `_reorderPages` (line 239, ~10 lines)
       - `_resetPageLayout` (line 435, ~11 lines)
       - `_resetAllLayouts` (line 447, ~8 lines)
       These are module-private (not exported) and have zero callers.

    4. **frontend/src/lib/home-store.ts lines 116, 283, 293**: Delete three unused private functions:
       - `_setHomeWobbleEnabled` (line 116, ~5 lines)
       - `_redoHome` (line 283, ~8 lines)
       - `_resetHomeLayout` (line 293, ~9 lines)
       These are module-private (not exported) and have zero callers.

    5. **frontend/src/lib/theme-store.ts line 46**: Remove the `_lastClickEvent` variable declaration AND its write at line 76 (`_lastClickEvent = clickEvent`). It is written but never read.

    6. **frontend/src/pages/dashboard/__tests__/WidgetWrapper.test.tsx line 121**: Remove the `_resolveWidget` variable declaration (line 121) and its assignment inside the promise callback (line 133: `_resolveWidget = resolve`). The resolve callback is captured but never invoked. If removing the assignment breaks the promise structure, replace with a no-op: just remove the outer `let _resolveWidget` line and change `_resolveWidget = resolve` to just `resolve` or discard it.

    7. **frontend/src/pages/missions/MissionCard.tsx line 26**: Remove the `_barColor` variable and its entire ternary expression (lines 26-30). It computes a value that is never used in the JSX.

    8. **frontend/src/pages/settings/SettingsKeybindings.tsx line 15**: Remove the `_modKey` variable and its `useSyncExternalStore` call. The `modList` variable on line 16 is what's actually used.

    IMPORTANT: After each file edit, verify the file still parses correctly. Do NOT remove any code that IS used -- only the specific unused locals listed above.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --noUnusedLocals --noUnusedParameters --project tsconfig.app.json 2>&1 | grep -c 'TS6133\|TS6196' | xargs -I{} test {} -eq 0 && echo "PASS: zero unused local/param violations" || echo "FAIL: still has violations"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn '_generateXsLayout' frontend/src/lib/dashboard-defaults.ts returns nothing
    - grep -rn '_setDotIndicatorsEnabled\|_reorderPages\|_resetPageLayout\|_resetAllLayouts' frontend/src/lib/dashboard-store.ts returns nothing
    - grep -rn '_setHomeWobbleEnabled\|_redoHome\|_resetHomeLayout' frontend/src/lib/home-store.ts returns nothing
    - grep -rn '_lastClickEvent' frontend/src/lib/theme-store.ts returns nothing
    - grep -rn '_barColor' frontend/src/pages/missions/MissionCard.tsx returns nothing
    - grep -n '_modKey' frontend/src/pages/settings/SettingsKeybindings.tsx returns nothing
    - npx vitest run passes (no runtime regressions)
  </acceptance_criteria>
  <done>All 13 TS6133 violations eliminated. Zero unused locals remain. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enable noUnusedLocals and noUnusedParameters in tsconfig.app.json</name>
  <files>frontend/tsconfig.app.json</files>
  <read_first>frontend/tsconfig.app.json</read_first>
  <action>
    In `frontend/tsconfig.app.json`, change both flags from `false` to `true`:

    ```json
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    ```

    These are on lines 17-18 of the current file. Change `false` to `true` for both.

    After editing, run `tsc --noEmit --project tsconfig.app.json` to verify no new TS6133/TS6196 errors appear (pre-existing non-unused errors are expected and not in scope).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c 'TS6133\|TS6196' | xargs -I{} test {} -eq 0 && echo "PASS: no unused violations with flags enabled" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'noUnusedLocals.*true' frontend/tsconfig.app.json returns a match
    - grep 'noUnusedParameters.*true' frontend/tsconfig.app.json returns a match
    - tsc --noEmit --project tsconfig.app.json reports zero TS6133 or TS6196 errors
  </acceptance_criteria>
  <done>tsconfig.app.json has both strict unused flags enabled. TypeScript compiler will reject future unused locals and parameters at compile time.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep 'TS6133\|TS6196'` returns empty (zero violations)
2. `cd frontend && grep '"noUnusedLocals": true' tsconfig.app.json` matches
3. `cd frontend && grep '"noUnusedParameters": true' tsconfig.app.json` matches
4. `cd frontend && npx vitest run` passes (no runtime regressions from dead code removal)
5. `cd frontend && npx vite build` succeeds (production build unaffected)
</verification>

<success_criteria>
- tsconfig.app.json has `"noUnusedLocals": true` and `"noUnusedParameters": true`
- Zero TS6133/TS6196 errors from `tsc --noEmit --project tsconfig.app.json`
- All existing tests pass
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/68-enable-typescript-strict-flags/68-01-SUMMARY.md`
</output>
