---
phase: 59-strip-unused-crate-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
autonomous: true
requirements:
  - RUST-02

must_haves:
  truths:
    - "Cargo.toml contains no crate dependencies that are never imported in source"
    - "cargo build compiles successfully after removal"
    - "cargo test passes after removal"
    - "False positives are documented in cargo-machete ignore list"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "Cleaned dependency list with cargo-machete ignore metadata"
      contains: "cargo-machete"
  key_links:
    - from: "src-tauri/Cargo.toml"
      to: "src-tauri/src/**/*.rs"
      via: "use/extern crate statements"
      pattern: "use (axum|reqwest|serde|tokio)"
---

<objective>
Remove unused crate dependencies from Cargo.toml and document false positives.

Purpose: Reduce compile time, binary size, and dependency surface area by stripping crates that are never imported.
Output: Cleaned Cargo.toml with only used crates, cargo-machete ignore list for false positives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src-tauri/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install cargo-machete, audit, and remove unused crates</name>
  <files>src-tauri/Cargo.toml</files>
  <read_first>src-tauri/Cargo.toml</read_first>
  <action>
1. Install cargo-machete: `cargo install cargo-machete` (use CARGO_TARGET_DIR=/tmp/mc-target for all cargo commands in this plan to prevent NFS hangs).

2. Run `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo machete` to get its report.

3. Remove these 3 unused dependencies from `[dependencies]` in Cargo.toml (confirmed via grep -- zero imports in any .rs file):
   - `axum-extra` (line 19) -- no TypedHeader or other axum-extra types used anywhere
   - `tokio-stream` (line 32) -- zero imports; StreamExt comes from futures crate instead
   - `tower` (line 21) -- zero direct imports; tower-http is used but pulls tower as a transitive dep

4. Add a `[package.metadata.cargo-machete]` section at the end of Cargo.toml (before `[profile]` sections) to document false positives that cargo-machete may flag but are actually used:
   ```toml
   [package.metadata.cargo-machete]
   ignored = ["async-stream"]
   # async-stream: used via async_stream::stream! macro in messages.rs, chat.rs, events.rs
   ```
   Note: Only add crates to `ignored` if cargo-machete actually flags them. If machete does NOT flag async-stream, do not add the ignore section. The ignore list must match reality.

5. Do NOT remove any crate that grep found usage for. Every other crate in Cargo.toml is confirmed used.
  </action>
  <acceptance_criteria>
    - grep -c "axum-extra" src-tauri/Cargo.toml returns 0
    - grep -c "tokio-stream" src-tauri/Cargo.toml returns 0
    - grep -c 'tower = ' src-tauri/Cargo.toml returns 0 (tower-http must still be present)
    - grep -c "tower-http" src-tauri/Cargo.toml returns 1
  </acceptance_criteria>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/src-tauri && grep -c "axum-extra" Cargo.toml && exit 1 || true && grep -c "tokio-stream" Cargo.toml && exit 1 || true && grep 'tower-http' Cargo.toml</automated>
  </verify>
  <done>Three unused crates removed from Cargo.toml, false positive ignore list added if needed</done>
</task>

<task type="auto">
  <name>Task 2: Verify build, tests, and cargo-machete clean report</name>
  <files>src-tauri/Cargo.toml</files>
  <read_first>src-tauri/Cargo.toml</read_first>
  <action>
1. Run `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo build` -- must succeed with no errors.

2. Run `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo test` -- all 231 tests must pass.

3. Run `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo machete` -- must report zero unused dependencies (or only items listed in the ignored metadata).

4. If any step fails:
   - Build failure: a removed crate was actually needed. Add it back, re-check grep to understand what uses it.
   - Test failure: same approach -- restore and verify.
   - Machete reports additional unused: evaluate each one with grep. If genuinely unused, remove it. If false positive (macro usage, re-export, build.rs), add to ignore list with comment.

5. Run `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo clippy -- -D warnings` to ensure no new warnings.
  </action>
  <acceptance_criteria>
    - CARGO_TARGET_DIR=/tmp/mc-target cargo build exits 0
    - CARGO_TARGET_DIR=/tmp/mc-target cargo test exits 0
    - CARGO_TARGET_DIR=/tmp/mc-target cargo machete reports 0 unused (or only ignored)
    - CARGO_TARGET_DIR=/tmp/mc-target cargo clippy -- -D warnings exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/josue/Documents/projects/mission-control/src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo build 2>&1 | tail -5 && CARGO_TARGET_DIR=/tmp/mc-target cargo test 2>&1 | tail -10 && CARGO_TARGET_DIR=/tmp/mc-target cargo machete 2>&1</automated>
  </verify>
  <done>cargo build succeeds, all tests pass, cargo-machete reports clean, clippy has no warnings</done>
</task>

</tasks>

<verification>
1. `grep -c "axum-extra" src-tauri/Cargo.toml` returns 0
2. `grep -c "tokio-stream" src-tauri/Cargo.toml` returns 0
3. `grep 'tower = ' src-tauri/Cargo.toml` returns no matches (tower-http still present)
4. `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo build` succeeds
5. `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo test` passes
6. `cd src-tauri && CARGO_TARGET_DIR=/tmp/mc-target cargo machete` reports zero unused
</verification>

<success_criteria>
- Cargo.toml has 3 fewer dependencies (axum-extra, tokio-stream, tower removed)
- Build and all tests pass
- cargo-machete reports clean (zero unused, false positives documented in metadata)
- cargo clippy passes with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/59-strip-unused-crate-dependencies/59-01-SUMMARY.md`
</output>
